.git
.venv
.cache
contract-samples
**/tests
**/__pycache__
**/*.pyc
node_modules
backend/
dist/
.env
.env.*

# Bản giả verifier + fixture test: không bao giờ vào ảnh chạy (BE-00 §2, K).
# Ngoại lệ: uv.lock khoá cả thành viên packages/testing, nên tầng dựng cần
# thấy pyproject.toml của nó để `uv sync --locked` không hỏng (ghi-chu-be.md
# §8 test 2–3) — Lệch có lý do, không phải sơ suất.
packages/testing
!packages/testing/pyproject.toml
