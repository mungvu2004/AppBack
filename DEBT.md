# DEBT — Sổ nợ kỹ thuật

> **Mọi nợ phát sinh trong một phiên phải có một dòng ở đây trước khi phiên kết thúc** (`RULE-CODE.md` R-34).
> Nợ = việc biết là còn thiếu/còn sai mà lần này không làm: lỗi của module khác, test chập chờn,
> giới hạn đã chấp nhận, tối ưu hoãn lại, `ponytail:`/`TODO` trong mã.
>
> **Ô `✔` là nguồn sự thật:**
>
> | Ô | Nghĩa | Bắt buộc kèm |
> |---|---|---|
> | `⬜` | mở, chưa ai làm | — |
> | `🔧` | đang sửa | ai/phiên nào đang giữ |
> | `✅` | **đã giải quyết** | ngày đóng + commit sửa (mã `FIX-<nnn>`, hoặc `sha 7` nếu đóng ở commit sau) |
> | `➖` | chấp nhận, không sửa | lý do đứng được + đường nâng cấp nếu đổi ý |
> | `❌` | bỏ, không còn đúng | lý do |
>
> Luật: Id `NO-<nnn>` tăng dần, **không dùng lại**. Sửa xong là **tích ngay trong chính commit sửa**
> (ghi mã `FIX-<nnn>`; sha điền sau nếu cần) — đừng để phiên sau phải đoán. Mức theo `RULE.md` §1. **Không bao giờ xoá dòng**:
> sổ này là lịch sử, không phải hàng đợi. Nợ `P0`/`P1` còn `⬜`/`🔧` thì chặn merge (R-38).

| ✔ | Id | Mở | Đóng | Nợ | Nguyên nhân gốc | Chủ | Mức | Ghi chú đóng |
|---|---|---|---|---|---|---|---|---|
| ✅ | NO-001 | 2026-09-20 | 2026-09-20 | `bash tools/verify/run.sh lock` hỏng `FileExistsError: /src-out/lock`; phải lock bằng cách mount `/src-out` thủ công | `run.sh` bind-mount host **đúng vào** `/src-out/<việc>`; `rmtree` xoá được nội dung nhưng không gỡ được chính mount point (EBUSY, `ignore_errors` nuốt), nên `mkdir` không `exist_ok` ném `FileExistsError`. Ba việc `lock`, `openapi`, `merge-heads` cùng đi qua `_clean_dir` | B0-01 (`tools/verify/*`) | P2 | **FIX-001** — `_clean_dir` `mkdir(..., exist_ok=True)` (`tools/verify/steps.py:243`), test chặn tái phát `test_lock_thư_mục_ra_là_mount_point_không_gỡ_được` (đỏ trước, xanh sau). `run.sh lock` thoát 0, `uv.lock` không đổi |
| ✅ | NO-002 | 2026-09-20 | 2026-09-20 | `test_migrate_check.py::test_downgrade_leaving_table_fails` và vài test DB khác hỏng ~1/7 lượt `coverage run -m pytest` | **Không phải rò trạng thái test.** Đường `verify-container → host.docker.internal:<cổng testcontainer>` thỉnh thoảng kẹt ~68 s rồi tự thông (đo: `socket.create_connection` 67,9 s rồi THÀNH CÔNG, Postgres rảnh 6/100 kết nối); asyncpg bỏ cuộc ở **trần 60 s mặc định của thư viện** — không file nào đặt trần tường minh (vi phạm R-24) — nên cổng kết tội nhầm bước migration bằng `TimeoutError()` trần | B0-03 (`packages/db/**`) | P1 | **FIX-002** — trần bắt tay tường minh: `db_connect_timeout_s=10` cho đường phục vụ request, `GATE_CONNECT_TIMEOUT_S=180` cho đường cổng (`engine.py`, `migrate_check.py`, `migrations/env.py`, `fixtures/db.py`). Chứng minh: 10/10 lượt test đó xanh, cả file xanh, `run.sh verify` thoát 0. Giới hạn: kẹt > 180 s vẫn đỏ → xem NO-007 |
| ➖ | NO-003 | 2026-09-20 | 2026-09-20 | `LocalDiskStorage`: object và `<key>.meta.json` là hai lần `os.replace`, giữa hai lần đó `stat` có thể trả `sha256` của bản trước | Khuôn lưu trữ tách metadata ra file riêng; không có API đổi tên hai file nguyên tử | B0-04 | P3 | Chấp nhận: chỉ là kho máy dev; staging/production dùng S3 nơi `put_object` gắn metadata trong cùng lượt ghi. Giới hạn đã ghi trong docstring `packages/storage/local.py`. Đổi ý thì phải nhúng metadata vào chính object (đổi khuôn lưu trữ) |
| ➖ | NO-004 | 2026-09-20 | 2026-09-20 | `S3Storage.list_prefix` gọi `stat` cho **từng** object (N+1) | `ListObjectsV2` của AWS không trả `x-amz-meta-*`, mà `ObjectInfo` hứa có `sha256`/`kind` | B0-04 | P2 | Chấp nhận: chỉ lịch dọn rác nền gọi. Nâng cấp khi thành điểm nóng: `list_prefix` trả `key` + `last_modified`, bỏ `stat` (~3 dòng, `packages/storage/s3.py:186`) |
| ➖ | NO-005 | 2026-09-20 | 2026-09-20 | `S3Storage._put_cors` dùng API riêng `Minio._execute` | `minio` 7.2.20 chưa phơi `set_bucket_cors` | B0-04 | Nit | Chấp nhận: gỡ khi nâng `minio` lên bản có API CORS công khai (`packages/storage/s3.py:227`) |
| ✅ | NO-006 | 2026-09-20 | 2026-09-22 | Chưa có nơi khai `STORAGE_*`, `S3_*` cho dev/staging/production | Dự án cố ý không dùng `.env` (`env_file=None`, BE-00 §5); compose của app là việc của B0-08, `deploy/` mới chỉ có `verify.yml` | B0-08 (`deploy/**`) | P2 | mở — B0-04 chỉ khai biến trong `packages/storage/settings.py`, không tự tạo file dưới `deploy/` (K27) · **đóng** — B0-08 `bf50968`: `deploy/compose/env.example` và `&app-env` (`base.yml`) khai `STORAGE_BACKEND`, `STORAGE_LOCAL_ROOT`, `S3_*` cho dev/ci/prod; review lượt 1–3 xác nhận |
| ✅ | NO-007 | 2026-09-20 | 2026-09-21 | Chặng `host.docker.internal` giữa container verify và testcontainers thỉnh thoảng kẹt ~68 s, làm cổng đỏ giả | Docker Desktop chuyển tiếp cổng qua host; FIX-002 chỉ nâng sức chịu lên 180 s chứ không bỏ được chặng này | B0-01 (`deploy/compose/verify.yml`, `tools/verify/*`) | P2 | **FIX-009** — đo trong container verify 2026-09-21, Postgres testcontainer, 10 lượt × 20 000 lần nối TCP (16 song song) + 500 lần nối asyncpg mỗi lượt: đường `host.docker.internal` (cổng host 54581, trong dải cổng tạm 49152+ của Windows) có 4 986–19 989 lần bị từ chối mỗi lượt và 6 lần kẹt 69,4–69,8 s (tái hiện đúng triệu chứng); đường IP bridge của container, cùng tải: 0 lỗi, lâu nhất 1,09 s (SYN thử lại vì hàng đợi `listen` của Postgres). Gateway `172.17.0.1` bị từ chối (Docker Desktop không mở cổng ở đó). Sửa: `verify.yml` đặt `TESTCONTAINERS_CONNECTION_MODE=bridge_ip` (testcontainers 4.13 nối thẳng IP container + cổng trong; CI ngoài container không đặt nên không đổi). Cả bộ test chạy ở chế độ này: 1146 xanh, chỉ 3 test "bản đã dừng" của `test_services` đổi dạng lỗi (hết giờ/113 thay cho 111) → `_STOPPED` nhận đủ các dạng. Test `test_container_verify_nối_thẳng_ip_container_dịch_vụ` (đỏ → xanh). Người điều phối cần thêm biến này vào ENV §2, BE-00 §12 · **FIX-009 (bổ sung 2026-09-22, sau gộp `main`)** — `test_ephemeral_redis_dừng_xong_bản_dùng_chung_vẫn_chạy` "DID NOT RAISE" 2/3 lượt cả file (0/5 chạy lẻ): bridge mặc định cấp lại **ngay** IP vừa giải phóng (dừng A `172.17.0.11` → container kế nhận `172.17.0.11`), redis-py 6.4 thử lại 1–17 s sau khi dừng, 7/12 lượt thăm dò gặp 111 ở IP cũ (container của phiên khác đã nhận; khi là Redis thì ping đạt). Mọi test C13 dừng `ephemeral_*` đều hở (Postgres/MinIO dùng chung mật khẩu mặc định). Sửa: `ephemeral_*` đi `host.docker.internal` + cổng map (`packages/testing/fixtures/services.py:_via_mapped_port`), bản dùng chung giữ IP bridge; bản đã dừng hỏng ngay 101/111 (9/9 lượt, 0,0 s), `_STOPPED` bỏ 113. Test `test_ephemeral_dừng_xong_địa_chỉ_không_về_tay_bản_dựng_sau[redis\|postgres\|minio]` (đỏ 3 failed `('172.17.0.5', 6379) != ('172.17.0.5', 6379)` → xanh, 3/3 lượt cả file 14 passed); test Redis: bản còn chạy trần cổng, bản đã dừng 5 s. Còn lại: client của test C13 sản phẩm tự đặt trần theo settings; kẹt ~69 s của chặng host rơi đúng lượt nối **trước** khi dừng thì test đó đỏ giả (hiếm: vài kết nối mỗi bản ephemeral) · trước: chữa dứt điểm: cho container verify và testcontainers dùng chung một docker network, hoặc khai Postgres thành service của `verify.yml` |
| ✅ | NO-008 | 2026-09-20 | 2026-09-21 | `_clean_dir` vẫn `rmtree(..., ignore_errors=True)`: lỗi xoá **file** bên trong thư mục ra bị nuốt im lặng | Có từ trước FIX-001; `merge-heads` chép `*.py` nên có thể chép nhầm file cũ còn sót | B0-01 (`tools/verify/*`) | Nit | **FIX-006** — `_clean_dir` xoá **từng mục con** (`rmtree` cho thư mục, `unlink` cho file), không `ignore_errors`, giữ chính mount point (NO-001); test `test_clean_dir_không_nuốt_lỗi_xoá_mục_con` (đỏ `DID NOT RAISE` → xanh), `test_clean_dir_xoá_cả_thư_mục_con_giữ_gốc` · trước: duyệt và xoá từng mục con, để lỗi nổi lên |
| ✅ | NO-009 | 2026-09-20 | 2026-09-21 | S3/MinIO trả 5xx **có thân XML** (`SlowDown`, `InternalError`, `ServiceUnavailable`) → `S3Error` thô lọt ra ngoài → API sẽ trả 500 thay vì 503 + `Retry-After` (C13) | `_s3_errors` chỉ bắt `urllib3.HTTPError` và `ServerError`; `minio._url_open` chỉ dựng `ServerError` khi 5xx **không** có thân. `urllib3.Retry` cũng có `status_forcelist` rỗng nên 5xx không được thử lại | B0-04 (`packages/storage/s3.py:67`) | P2 | **FIX-011** — `_s3_errors` đổi thêm `S3Error` có `response.status >= 500` thành `DEPENDENCY_UNAVAILABLE` (503 + `Retry-After`), `S3Error` 4xx vẫn nổi lên. Test `test_server_error_with_xml_body_returns_503[500-InternalError, 503-SlowDown]` chạy trên MinIO thật sau proxy tiêm lỗi `packages/storage/tests/fault_proxy.py` (đỏ trước: `S3Error` thô, xanh sau). Không thêm `status_forcelist`: thử lại 5xx là đổi hành vi, ngoài phạm vi |
| ✅ | NO-010 | 2026-09-20 | 2026-09-21 | `S3Storage._delete_under` xoá **từng** object bằng `remove_object` trong vòng lặp (N+1) | Viết vòng lặp để tránh nhánh không phủ được khi duyệt lỗi của `remove_objects`; `Minio.remove_objects` gom 1000 khoá mỗi request | B0-04 (`packages/storage/s3.py:244`) | P2 | **FIX-012** — `_delete_under` đưa danh sách lười của `list_objects` vào `remove_objects` (≤ 1000 khoá/lượt `DeleteObjects`); lỗi từng object trong `DeleteResult` gom lại và ném `RuntimeError` (tổng số + 5 khoá đầu). Test `test_delete_prefix_deletes_in_batches` đếm request qua proxy (đỏ trước: `['GET','DELETE','DELETE','DELETE']`, xanh sau: 1 `POST ?delete`), `test_delete_prefix_reports_objects_it_could_not_delete` |
| ✅ | NO-011 | 2026-09-20 | 2026-09-21 | `signed_url(inline)` luôn tốn một `HEAD` S3 vì lối tắt `kind` chỉ mở cho khoá ảnh đại diện | `avatar_kind` chỉ khớp `users/*/avatar/*`, trong khi `…/pages/{i}.png` cũng do server đặt tên sau khi đã kiểm magic bytes (cùng lý lẽ K15) | B0-04 (`packages/storage/port.py:140`, `keys.py:27`) | P2 | **FIX-013** — `keys.avatar_kind` thành `keys.server_chosen_kind`: khớp ảnh đại diện **và** `projects/…/uploads/…/pages/{i}.png`, không bao giờ khớp `original.<đuôi>`; `resolve_kind` dùng nó, nên ký `inline` ảnh trang với `kind="png"` không tốn `HEAD`. Test `test_signed_url_with_kind_does_not_stat[local/s3-page_key]` (đỏ trước: `ValueError` "chỉ hợp lệ cho khoá ảnh đại diện"), `test_signed_url_rejects_kind_outside_server_named_keys` (đuôi lệch, `original.png`), `test_server_chosen_kind_only_for_keys_the_server_names`. Luật `kind=None` (đọc metadata, 404 khi thiếu) giữ nguyên |
| ✅ | NO-012 | 2026-09-20 | 2026-09-21 | `LocalDiskStorage.delete_prefix` dùng `shutil.rmtree(..., ignore_errors=True)`: xoá hỏng bị nuốt, hàm vẫn báo thành công; `_disk_errors()` bọc quanh thành mã chết | Chọn `ignore_errors` cho gọn thay vì phân biệt `FileNotFoundError` với lỗi thật | B0-04 (`packages/storage/local.py:156`) | P3 | **FIX-014** — `rmtree(..., onexc=_ignore_missing)`: chỉ bỏ qua `FileNotFoundError` (tiền tố chưa có, mục bị xoá đồng thời), lỗi khác qua `_disk_errors` (đĩa đầy/chỉ đọc → 503) hoặc nổi lên nguyên vẹn. Test `test_delete_prefix_raises_instead_of_reporting_success` (tiền tố là một tệp → `NotADirectoryError`; đỏ trước: "DID NOT RAISE"), `test_delete_prefix_of_a_missing_prefix_is_silent` |
| ✅ | NO-013 | 2026-09-20 | 2026-09-21 | `list_prefix` nạp **toàn bộ** danh sách vào RAM trước khi yield (`list(list_objects(...))`, `sorted(rglob("*"))`) | Gom trước cho dễ đưa vào `asyncio.to_thread`; chưa phân trang | B0-04 (`packages/storage/s3.py:240`, `local.py:243`) | P3 | **FIX-015** — `port.next_batch` kéo ≤ `LIST_BATCH` (1000) mục mỗi lượt từ bộ duyệt đồng bộ trong luồng riêng; S3 dùng thẳng bộ duyệt lười của `list_objects` (bỏ `_list_under`), local duyệt `os.scandir` lười từng thư mục, xếp thư mục như `tên/` nên thứ tự vẫn là thứ tự khoá đầy đủ như S3. Test `test_list_prefix_streams_the_tree_in_batches` (lô 1: object ghi sau mục đầu vẫn hiện; đỏ trước: thiếu `pages/1.png`), `test_list_prefix_orders_by_full_key[local, s3]` (`a.b` < `a/b` < `a0`). Không thêm `limit`/`continuation_token`: chỉ lịch dọn rác gọi, chưa ai cần phân trang |
| ✅ | NO-014 | 2026-09-20 | 2026-09-21 | `S3Storage.put` ghi đệm xuống đĩa khi vượt 16 MiB nhưng `OSError` của `buffer.write` không được bọc → đĩa đầy ra 500, trong khi bản local ra 503 | `_disk_errors` chỉ nằm ở `local.py`, hai bộ điều hợp không dùng chung | B0-04 (`packages/storage/s3.py:110`) | P3 | **FIX-016** — `_disk_errors`/`_UNAVAILABLE_ERRNOS` của `local.py` lên `port.py` thành `disk_errors`/`UNAVAILABLE_ERRNOS` (R-07); `S3Storage.put` bọc vòng đệm bằng nó, nên đĩa đầy khi bộ đệm tràn xuống đĩa → 503 + `Retry-After` như bản local. Test `test_put_on_a_full_spool_disk_returns_503` (tiêm `SpooledTemporaryFile` ghi `ENOSPC`; đỏ trước: `OSError` thô, xanh sau; không để lại object) |
| ✅ | NO-015 | 2026-09-20 | 2026-09-21 | `ensure_bucket` nuốt **mọi** `S3Error` của `PutBucketCors` thành một dòng `INFO`; `AccessDenied` hay `NoSuchBucket` biến mất, bucket production chạy tiếp với CORS sai | Viết một nhánh duy nhất để tránh nhánh không phủ được (MinIO luôn trả `NotImplemented`) | B0-04 (`packages/storage/s3.py:88`) | P3 | **FIX-017** — `ensure_bucket` chỉ bỏ qua `S3Error` có `code == "NotImplemented"` (hằng `CORS_UNSUPPORTED`, vẫn ghi `INFO`); mã khác ném lại (fail-closed, R-17: app không lên với CORS sai). Test `test_ensure_bucket_raises_when_cors_is_refused` (proxy trả 403 `AccessDenied` cho `PUT ?cors`; đỏ trước: "DID NOT RAISE"), test MinIO `NotImplemented` cũ vẫn xanh |
| ✅ | NO-016 | 2026-09-20 | 2026-09-21 | Hai test phụ thuộc đồng hồ thật: `test_stat_uses_filesystem_time` (cửa sổ 60 s) và `test_list_prefix_older_than` (trộn `datetime.now` thật với `fake_clock`) | `last_modified` của kho là giờ thật, không đi qua `Clock` tiêm được | B0-04 (`tests/test_local.py:171`, `tests/test_contract.py:136`) | P3 | **FIX-018** — sửa chính hai test (FIX.md luật 2: test sai với TEST-02): `test_stat_uses_filesystem_time` ghim `st_mtime` bằng `os.utime` rồi khẳng định **bằng** đúng mốc đó; `test_list_prefix_older_than` lấy mốc từ `stat()` vừa đọc của chính kho (`older_than = mốc + 1 s` → có, `= mốc` → không), không còn `datetime.now`. Không có trạng thái đỏ tất định (lỗi là đỏ chập chờn khi máy tải) |
| ✅ | NO-017 | 2026-09-20 | 2026-09-21 | Token tệp local **không** mang `kind`, nên luật `inline` chỉ được kiểm lúc ký; `/api/files/{token}` **phải** tự đọc `kind` trong metadata lúc phục vụ | `resolve_kind` trả `kind` nhưng `LocalDiskStorage.signed_url` vứt giá trị (chỉ dùng tác dụng phụ ném lỗi) | B0-04 khai · **B0-06** phải tuân | Nit | mở — review merge 2026-09-20 finding #11. Gom luôn finding #10 (khẳng định test nên nói "không chứa khoá dạng thô") và #12 (chú thích `_message` ghi `BE-00 §8` nhưng bản tin có 4 trường `k\|e\|d\|n`) · 2026-09-21: phần **B0-06** đã tuân (`eb40a3c`) — `apps/api/files/router.py:_disposition` đọc `info.kind` của metadata lúc phục vụ, `inline` chỉ khi token ghi `inline` **và** `kind` là PNG/JPEG. Còn mở phần B0-04 (#10, #12) · **FIX-019** (phần B0-04): docstring `FileGrant` ghi rõ token không mang `kind`, nơi phục vụ phải tự đọc metadata (#11); `signed_url` chú thích vì sao bỏ giá trị `resolve_kind`; `_message` ghi bốn trường `k\|e\|d\|n` và lý do thêm `n` so với BE-00 §8 (#12); `test_signed_url_goes_through_the_files_route` nói đúng ý "không chứa khoá **dạng thô**", thân token là base64url không mã hoá (#10). Chỉ tài liệu và lời test: không có trạng thái đỏ |
| ➖ | NO-018 | 2026-09-20 | 2026-09-21 | Năm commit của phiên này có dòng trống giữa `Prompt:`/`Fix:` và `Co-Authored-By:`, nên `git log --format=%(trailers:key=Prompt)` trả rỗng (`git log --grep "Prompt: B0-04"` vẫn ra) | Khối trailer bị tách đôi: git chỉ coi đoạn **cuối cùng** là trailer | phiên B0-04 | Nit | mở — không viết lại lịch sử vì `DEBT.md` và bản phán quyết đã trỏ tới sha `c5bf22e`, `68cdebe`. Luật R-36b chặn tái phát cho commit sau · 2026-09-21 chấp nhận khi dọn nợ (FIX-006..028): sửa là viết lại lịch sử đã có sổ và phán quyết trỏ tới; tra chủ năm commit đó bằng `git log --grep "Prompt: B0-04"`. Đổi ý thì chỉ thêm `git notes`, không rebase |
| ✅ | NO-019 | 2026-09-20 | 2026-09-21 | `tools/tests/test_case_gate.py::test_real_task_names` khẳng định `_real_task_names() == []`, và `test_main_đạt_khi_chưa_có_thao_tác` khẳng định `main() == 0`; cả hai đỏ ngay khi **bất kỳ** prompt nào khai task sản phẩm đầu tiên (B5-01) | Hai test này chốt trạng thái "chưa có task nào" của repo làm bất biến, trong khi `registered_tasks()` là sổ toàn tiến trình và sẽ có dòng thật | B0-01 (`tools/tests/test_case_gate.py`) | P2 | mở — **tái hiện được**: tạo `apps/worker/probe/tasks.py` khai một task qua `define_task` → `registered_tasks()` trả `['probe_run']`, hai test trên `2 failed in 0.32s`; xoá gói đi thì xanh lại. B0-05 chỉ hoãn được bằng cách loại task khai trong module test khỏi sổ (`_declared_in_tests`), không chữa được gốc vì `tools/**` ngoài quyền sở hữu (K27). Chữa: cho hai test tự dựng sổ giả (`monkeypatch` `registered_tasks`) thay vì dựa vào trạng thái thật · **FIX-003** (`cdee48c`) — hai test tự dựng sổ task bằng `monkeypatch`, cùng NO-031 |
| ➖ | NO-020 | 2026-09-20 | 2026-09-20 | `registered_tasks()` trả `list[str]` (tên hàm) chứ không `list[TaskInfo]` như prompt B0-05 [2] mô tả | `tools/case_gate.py` (đã hợp nhất, B0-01) làm `set(task_names)` rồi so trực tiếp với `cases.toml.fn` và khoá của `_task_found`; trả dataclass là mọi task đều báo thiếu J01/J06. CASE §2.3 cũng viết "`fn` là tên hàm như `registered_tasks()` trả" | B0-05 | P3 | Chấp nhận: hiến chương thắng prompt (CLAUDE.md). Thông tin (tên task, hàng, module) vẫn lấy được qua `task_entries()` + `queue_for()`. Đổi ý thì phải sửa `tools/case_gate.py` cùng lượt |
| ✅ | NO-021 | 2026-09-20 | 2026-09-22 | Chưa có nơi khai `REDIS_BROKER_URL`, `REDIS_CACHE_URL`, `CELERY_VISIBILITY_TIMEOUT_S`, `TASK_*`, `STREAM_MAXLEN` cho dev/staging/production | Cùng gốc với NO-006: compose của app thuộc B0-08, `deploy/` mới chỉ có `verify.yml` | B0-08 (`deploy/**`) | P2 | mở — B0-05 chỉ khai biến trong `packages/messaging/settings.py`. Compose phải dựng **hai** instance Redis tách nhau (`noeviction` cho broker, `allkeys-lru` cho cache) — `assert_broker_policy` sẽ chặn worker nếu dựng sai · **đóng** — B0-08 `bf50968`: hai Redis tách (`redis-broker` noeviction + AOF, `redis-cache` allkeys-lru 256mb); `REDIS_*_URL`, `CELERY_VISIBILITY_TIMEOUT_S`, `TASK_*`, `STREAM_MAXLEN` đi qua `&app-env` và cả `ml`/`ml-gpu` (review lượt 1 #10, lượt 2 #17) |
| ✅ | NO-022 | 2026-09-20 | 2026-09-21 | Chưa test được đường giao lại **thật** khi worker chết (`WORKER_LOST` đi qua `task_reject_on_worker_lost`) | `celery.contrib.testing.worker.start_worker` chạy `pool="solo"` trong một luồng của chính tiến trình test; giết nó là giết luôn pytest | B0-05 (`packages/messaging/tests/test_tasks.py`) | P3 | mở — prompt [8] đã cho phép ghi "chưa chạy". Hiện chỉ test được nhánh đếm (đặt sẵn bộ đếm giao = 3). Chữa: dựng worker bằng `subprocess` rồi `SIGKILL`, hoặc để B0-08 kiểm ở test khói compose · **FIX-020** — `tests/test_worker_lost.py::test_a_worker_killed_mid_task_is_redelivered_until_worker_lost`: worker prefork thật trong tiến trình con (`tests/worker_probe.py`), `SIGKILL` tiến trình con đang chạy task 3 lần → mỗi lần thông điệp quay lại hàng, lượt thứ 4 báo `WORKER_LOST` và thân không chạy nữa (~5 s). Đột biến `task_reject_on_worker_lost=False` → đỏ (lượt giao 2 không bao giờ tới) |
| ✅ | NO-023 | 2026-09-20 | 2026-09-21 | Mỗi lượt chạy task tốn **hai** lượt đi-về Redis cho bộ đếm giao lại (`INCR` + `EXPIRE`) | Viết hai lệnh rời cho dễ đọc; gộp được bằng một script Lua như `publish_once` | B0-05 (`packages/messaging/tasks.py:_deliveries`) | P3 | mở — review merge 2026-09-20 L2-6. Không chỉ là chi phí: `INCR` xong mà `EXPIRE` hỏng thì bộ đếm sống mãi và tăng nhanh hơn `retries`, đủ để một `task_id` dùng lại bị gán nhầm `WORKER_LOST` sớm. Gộp hai lệnh thành một script Lua như `publish_once` là xong cả hai · **FIX-021** — `_deliveries` gửi một `EVAL` (`INCR` + `EXPIRE` trong Lua, `_COUNT_DELIVERY`): một lượt đi-về, không có khe giữa hai lệnh. Test `test_the_delivery_count_and_its_ttl_are_one_atomic_command` quan sát lệnh qua client thật: đỏ `['INCRBY', 'EXPIRE'] == ['EVAL']` trước sửa, xanh sau |
| ✅ | NO-024 | 2026-09-20 | 2026-09-21 | `runner()` dựng một `asyncio.Runner` mỗi tiến trình và **không bao giờ đóng**; vòng sự kiện sống tới lúc tiến trình thoát | Worker sống suốt đời tiến trình nên không cần đóng; nhưng tiến trình test cũng giữ một vòng thừa | B0-05 (`packages/messaging/tasks.py`) | Nit | mở — gắn `worker_process_shutdown` gọi `Runner.close()`, và một hàm `reset_runner()` cho test · **FIX-022** — `reset_runner()` đóng `Runner` của tiến trình rồi quên nó, gắn vào `worker_process_shutdown`; `ProcessLocal.reset()` nay trả tài nguyên vừa quên (không trả tài nguyên dựng trước `fork`). Test `test_the_worker_process_closes_its_event_loop_on_shutdown`, `test_process_local_reset_*`: đỏ trước sửa (vòng không đóng, `reset()` trả `None`), xanh sau |
| ✅ | NO-025 | 2026-09-20 | 2026-09-21 | `SafeLock.hold` báo mất khoá bằng `Task.cancel()`, nên **hai** kiểu thân thoát được: thân nuốt `CancelledError`, và thân chạy một khối đồng bộ dài (không có điểm `await` nào để huỷ tới) | Không có cách nào ném thẳng vào thân của một `@asynccontextmanager`; `asyncio.timeout` cũng dùng đúng cách này | B0-05 (`packages/messaging/locks.py:hold`) | P3 | mở — review merge 2026-09-20 (B0-05). Người giữ khoá cho việc rất dài phải tự gọi `renew()` giữa các bước và kiểm giá trị trả về, không chỉ dựa vào `hold`; ghi câu này vào docstring của `hold` khi sửa · **FIX-023** — hai kiểu thân đó không còn thoát **êm**: lúc thoát `hold` báo `LockLost` khi vòng gia hạn đã thấy mất khoá (thân nuốt lượt huỷ; lượt huỷ được trả lại bằng `uncancel`) hoặc khi `release` (nay trả `bool` từ script so token) thấy khoá không còn là của mình (khối đồng bộ dài). Không ngắt được thân **giữa chừng** vẫn là giới hạn của asyncio, ghi ở docstring của `hold` kèm câu về `renew()`. Test `test_hold_reports_a_lost_lock_even_when_the_body_swallows_the_cancellation`, `test_hold_reports_a_lock_lost_under_a_blocking_body` (đỏ `DID NOT RAISE LockLost` trước sửa), `test_hold_lets_a_body_error_raised_after_losing_the_lock_through` |
| ✅ | NO-026 | 2026-09-20 | 2026-09-21 | `assert_broker_policy` chỉ có bản **đồng bộ**, trong khi B0-06 gọi nó ở `lifespan` của FastAPI (async) | Tín hiệu `worker_init` của Celery chạy trước khi có vòng sự kiện nào, nên bản đồng bộ là bản bắt buộc; làm thêm bản async là trùng lặp (R-07) | B0-05 khai · **B0-06** phải tuân | Nit | mở — **không phải nợ mã**, là ràng buộc bàn giao: B0-06 gọi `await asyncio.to_thread(assert_broker_policy, broker_redis_sync())` lúc khởi động rồi đóng client. `broker_redis_sync` đã được tái xuất ở `packages/messaging/__init__.py` cho đúng đường gọi này (review merge 2026-09-20 finding #8) · Đã tuân ở `eb40a3c`: `apps/api/core/app.py` `lifespan` gọi `await asyncio.to_thread(assert_broker_policy, app.state.broker_sync)`, client đóng ở `finally` |
| ✅ | NO-027 | 2026-09-20 | 2026-09-21 | Test khoá chốt hành vi bằng **đồng hồ thật** với biên hẹp (`TTL_MS = 300`, `RENEW_MS = 80`, `sleep(TTL×3,5)`) → đỏ giả khi máy chạy tải | Hành vi cần kiểm đúng là "hết hạn" và "gia hạn kịp", mà Redis chỉ có TTL theo giờ thật; `Clock` tiêm được không chạm tới `PX`/`PEXPIRE` | B0-05 (`packages/messaging/tests/test_locks.py:12-13`) | P2 | mở — review merge 2026-09-20 finding #5 và L2-5, trái TEST-02. Lượt sửa P1 thêm `test_hold_treats_a_broken_redis_as_a_lost_lock` dùng lại đúng biên đó, nên nay là **ba** test chốt hành vi bằng đồng hồ thật. Cộng dồn với NO-007 (chặng `host.docker.internal` kẹt) thì biên 300 ms là quá mỏng. Nới TTL lên vài giây, hoặc khẳng định quan hệ (`EXISTS` trước/sau) thay vì đếm thời gian · **FIX-024** — tái hiện: 48 vòng bận CPU trong container, `test_an_expired_owner_cannot_renew_or_release_the_new_owner` đỏ 1/12 rồi 2/16 lượt (`exists == 0`: chủ mới TTL 100 ms hết hạn trước khi kịp kiểm). Sửa: "hết hạn" dựng bằng `DEL`; gia hạn khẳng định bằng quan hệ (`PTTL` được đẩy lên lại ≥ 3 lần, `test_hold_keeps_pushing_the_lock_deadline_forward`); thân dài chờ một sự kiện không bao giờ tới (trần 30 s) thay vì `sleep(TTL×4)`; TTL 5 000 ms = 50 × chu kỳ gia hạn. Cùng tải: 16/16 lượt xanh |
| ✅ | NO-028 | 2026-09-20 | 2026-09-21 | `SafeLock.hold`: `await self.release(token)` nằm trong `finally`, ném thì **che mất** ngoại lệ thật của thân | Trả khoá sớm là đúng, nhưng chưa tách đường lỗi của chính việc trả khoá | B0-05 (`packages/messaging/locks.py:118-123`) | P3 | mở — review merge 2026-09-20 finding #6 và L2-1. **Chưa làm, không phải không làm được**: lượt review hai đã dựng được nhánh này và tôi ghi sai nguyên nhân ở bản đầu. Công thức tái hiện: `ttl_ms=5000` + `hold(4000)` để vòng gia hạn không kịp chạy (`lost` giữ `False` nên `finally` chắc chắn vào `release`), trong thân `admin.shutdown(nosave=True)` rồi ném `ZeroDivisionError` → người gọi nhận `AppError DEPENDENCY_UNAVAILABLE`, ngoại lệ thật của thân bị che. Sửa: bọc `release` bằng `try/except` ghi `WARNING` rồi bỏ qua (TTL vẫn dọn hộ), kèm test theo công thức trên · **FIX-025** — trên đường thân đã ném, `hold` trả khoá bằng `_release_quietly`: lỗi phụ thuộc (`AppError` của `redis_errors`) → `WARNING lock_release_failed`, lỗi của thân nổi lên nguyên trạng; lỗi lạ vẫn nổi lên. Đường thân xong bình thường giữ nguyên (release hỏng vẫn ném). Test `test_a_failed_release_never_hides_the_body_error` theo đúng công thức: đỏ (`AppError: DEPENDENCY_UNAVAILABLE`) trước sửa, xanh sau |
| ➖ | NO-029 | 2026-09-20 | 2026-09-21 | Bộ đếm rào `lock:{name}:fence` **không có TTL** và chỉ tăng; trên instance `noeviction` nó không bao giờ được thu hồi | Token rào phải đơn điệu, mà TTL là mất tính đơn điệu; prompt B0-05 [5] cho phép đúng ngoại lệ này | B0-05 (`packages/messaging/locks.py:19-21`) | P3 | mở — review merge 2026-09-20 finding #7 và L2-2, thiếu ngưỡng + đường nâng cấp theo R-05. Ngưỡng thật: mỗi tên khoá tốn ~70 byte, vài chục tên cố định (`gpu:0`, `training:slot`) → vài KB. **Nhưng BE-00 §7 đã khai `training:claim:{job}` — tên khoá theo `job_id`, tức không giới hạn số tên**, nên đây là ràng buộc bàn giao chứ không phải chuyện tương lai: **B6-03a** dùng `SafeLock` cho claim theo job thì bộ đếm rào phải đổi sang `HINCRBY` một hash có TTL (hoặc claim không dùng token rào của gói này). Ghi ngưỡng vào comment của `FENCE_SUFFIX` khi sửa · **FIX-026** — chấp nhận: prompt B0-05 [5] cho phép đúng ngoại lệ này, và mọi tên khoá hiện có là tên cố định (vài KB, không bao giờ cần dọn). Ngưỡng, đường nâng cấp (`HINCRBY` một hash có TTL dài hơn đời job) và ràng buộc bàn giao cho **B6-03a** (không dùng `SafeLock` cho claim theo job) đã ghi cạnh `FENCE_SUFFIX` (R-05). Đổi ý khi có tên khoá theo id không giới hạn |
| ✅ | NO-030 | 2026-09-20 | 2026-09-21 | `_fail` ghi `repr(exc)` của ngoại lệ do `on_failed` (mã module khác) ném vào log | Cần chi tiết để gỡ lỗi, nhưng `repr` của lỗi module khác có thể mang dữ liệu người dùng | B0-05 (`packages/messaging/tasks.py:_fail`) | Nit | mở — review merge 2026-09-20 finding #10. Bộ che của `packages/core/logging.py` đã cắt JWT/Bearer/chữ ký; vẫn nên rút gọn còn `type(exc).__name__` cộng 200 ký tự đầu · **FIX-027** — `_fail` ghi `ERROR` với `error = "<Lớp>: <200 ký tự đầu>"` và **không** stack (`_log.exception` in lại nguyên thông điệp trong traceback). Test `test_an_on_failed_error_is_logged_briefly`: đỏ (`repr` đầy đủ, có đuôi dữ liệu) trước sửa, xanh sau |
| ✅ | NO-031 | 2026-09-20 | 2026-09-20 | `tools/tests/test_case_gate.py::test_real_operations_chưa_có_b0_06_trả_rỗng`, `::test_real_task_names`, `::test_main_đạt_khi_chưa_có_thao_tác` đỏ ngay khi B0-06 hợp nhất (`6 failed` ở bước 5) | Ba test chốt trạng thái "repo chưa có thao tác nào, chưa có task nào" làm bất biến; `_real_operations()` nay trả 3 `Operation` và `_real_task_names()` trả `['purge_expired_idempotency']`. NO-019 đã dự báo hai trong ba test này | B0-01 (`tools/tests/test_case_gate.py`) | P1 | **FIX-003** — ba test dựng đầu vào tại chỗ (`sys.modules`, `monkeypatch`) thay vì đọc trạng thái repo; thêm `test_real_operations_đọc_được_sổ_thật` không khẳng định số lượng. Đỏ 3/3 trước, xanh 3/3 sau; `just verify` bước 1–6, 8 `đạt`. Spec: `docs/fixes.md`
| ✅ | NO-032 | 2026-09-20 | 2026-09-20 | `packages/db/tests/test_new_revision.py::test_creates_revision_with_charter_name` đỏ: khẳng định `down_revision: str \| None = "r20260920_b0_03"` | Test ghim head của cây revision làm hằng; revision `r20260920_b0_06_idempotency` của B0-06 nay là head, và **mọi** prompt thêm migration sau này cũng sẽ làm test này đỏ | B0-03 (`packages/db/tests/test_new_revision.py`) | P1 | **FIX-004** — `single_head(versions)` đọc head thật bằng `ScriptDirectory.get_heads()` trước khi tạo revision, kèm khẳng định cây chỉ có một head. Đỏ trước, xanh sau; `migrate_check` 9/9. Spec: `docs/fixes.md`
| ✅ | NO-033 | 2026-09-20 | 2026-09-20 | `packages/messaging/tests/test_tasks.py::test_registered_tasks_leaves_out_tasks_declared_in_tests` (`registered_tasks() == []`) và `test_worker_main.py::test_the_worker_process_is_ready_to_run_schedules` (`beat == []`) đỏ | Hai test chốt "chưa module nào khai lịch"; B0-06 khai `@periodic("default.idempotency.purge_expired")` theo đúng bảng "Dọn rác" của BE-00 §7, nên sổ task và `beat_schedule()` không còn rỗng | B0-05 (`packages/messaging/tests/**`) | P1 | **FIX-005** — đổi sang đúng bất biến của docstring: sổ **không chứa** task khai trong module test; `app.conf.beat_schedule` **bằng** `beat_schedule()` đọc trong cùng tiến trình con. Đỏ 2/2 trước, xanh 2/2 sau. Spec: `docs/fixes.md`
| ➖ | NO-034 | 2026-09-20 | 2026-09-20 | `create_app` mount route bằng `mount_routers` (đưa thẳng `AppRoute` vào `app.router.routes`) chứ không `app.include_router(prefix="/api")` như khuôn FastAPI thường | Từ FastAPI 0.141, `include_router` không chép route nữa mà bọc chúng trong `_IncludedRouter` và **dựng lại** `dependant` cho đường đã thêm tiền tố. Guard của `AppRoute` (W21, 428, nhận việc idempotency) được gắn vào `self.dependant` sau `super().__init__()` nên sẽ bị bỏ mất, và `app.routes` không còn `APIRoute` để middleware khớp đường | B0-06 (`apps/api/core/routing.py:mount_routers`) | P2 | Chấp nhận: tiền tố `/api` chuyển vào `protected_router`/`public_router`, `mount_routers` dựng lại handler sau khi gắn `dependency_overrides_provider`. Ngưỡng: nâng FastAPI là phải chạy lại `apps/api/core/tests/` trước; nếu bản sau cho `include_router` giữ nguyên `dependant` thì quay về khuôn chuẩn |
| ➖ | NO-035 | 2026-09-20 | 2026-09-21 | `coverage` báo vài dòng của `idempotency.py` là chưa chạy dù test có đi qua: `194` (`return … _Existing(*row)`), `211`/`213` của `begin` (`if claimed is not None:`, `return Claim(...)`) trong khi dòng `212` **giữa** chúng có chạy, và `255–256` của `complete` (`if result.rowcount != 1: raise`) | Sai số đo của `coverage` 7 trên Python 3.12 (`sys.monitoring`): mọi dòng bị báo đều là dòng **ngay sau** một `await` đi qua greenlet của SQLAlchemy | B0-06 | Nit | Chấp nhận: các nhánh đó **có** test đi qua (`test_replay_returns_stored_response`, `test_parallel_same_key_gives_one_in_progress`, `test_expired_lease_is_taken_over`); cổng độ phủ `apps/api/core` dòng 99,46 % · nhánh 98,03 % (đo lại 2026-09-21 sau khi tách `begin`, NO-035 cũ trỏ dòng 165–169). Cấm `pragma: no cover` (K24). Đổi ý thì đo lại sau khi nâng `coverage` |
| ✅ | NO-036 | 2026-09-21 | 2026-09-21 | Fixture `db_sessionmaker` dựng engine với trần bắt tay 10 s của **đường request** (`DatabaseSettings(...)` mặc định `db_connect_timeout_s=10`), trong khi mọi test nối Postgres qua chặng `host.docker.internal` hay kẹt ~68 s | FIX-002 chỉ nâng trần cho `_admin` (tạo/xoá database) và đường cổng; `db_sessionmaker` không được đụng tới. Cùng lớp với NO-007 | B0-03 (`packages/testing/fixtures/db.py:109`) | P2 | **FIX-010** — `db_sessionmaker` dựng `DatabaseSettings(..., db_connect_timeout_s=int(GATE_CONNECT_TIMEOUT_S))`; test `test_db_sessionmaker_chịu_trần_bắt_tay_cổng` đọc trần thật qua sự kiện `do_connect` (đỏ `[10] == [180.0]` → xanh) · trước: review merge B0-06 (2026-09-21) finding #2: cổng đỏ giả ở bước 5 vì app thử của B0-06 cũng dùng 10 s. B0-06 đã sửa phía mình (`api_env` đặt `DB_CONNECT_TIMEOUT_S` = `GATE_CONNECT_TIMEOUT_S`, có test). Chữa phía B0-03: thêm `db_connect_timeout_s=int(GATE_CONNECT_TIMEOUT_S)` vào `DatabaseSettings` của `db_sessionmaker` |
| ✅ | NO-037 | 2026-09-21 | 2026-09-21 | Header `Origin` sai dạng (`http://[`) làm `require_origin`/`reject_foreign_origin` ném `ValueError` → 500 `INTERNAL` kèm stack, thay vì 403 `ORIGIN_MISMATCH` | `_origin_of` đưa header của client thẳng vào `urlsplit`, mà `urlsplit` ném với IPv6 hỏng | B0-06 (`apps/api/core/origin.py`) | P2 | `014ca6d` — `_origin_of` trả `None` khi không phân tích được, `_matches` coi là lệch; test `test_malformed_origin_is_403_not_500` cho cả hai hàm. Review merge 2026-09-21 finding #1 |
| ✅ | NO-038 | 2026-09-21 | 2026-09-21 | Lượt bị huỷ **sau** commit (tắt tiến trình lúc deploy giữa `after_commit_idle`) gọi `discard` và xoá mất dòng idempotency `completed` → lượt lặp chạy handler lần hai | `discard` xoá theo `id` + `claim_token`, mà `complete` không đổi `claim_token` | B0-06 (`apps/api/core/idempotency.py:discard`) | P2 | `014ca6d` — sửa ở chỗ mọi đường đi qua: `discard` thêm `state == 'in_progress'` vào `WHERE`; test `test_discard_never_deletes_a_completed_record`. Review merge 2026-09-21 finding #2 |
| ➖ | NO-039 | 2026-09-21 | 2026-09-21 | Nhánh `feature/b0-06-api-framework` vượt trần 400 dòng logic của MNT-05 (≈ 2 700 dòng mã, ≈ 3 300 dòng test) | B0-06 là **một** prompt dựng khung: `AppRoute` ↔ idempotency ↔ middleware ↔ thân lỗi móc vào nhau, tách ra là đưa vào `main` một nửa khung (route không có guard) | B0-06 | P2 | Chấp nhận: không tách được mà vẫn giữ `main` xanh và đúng hiến chương. Prompt sau mỗi cái là một module nghiệp vụ, nên không lặp lại. Review merge 2026-09-21 finding #3 |
| ✅ | NO-040 | 2026-09-21 | 2026-09-21 | Lượt ghi có `Idempotency-Key` giữ **hai** kết nối của cùng pool: dependency quyền đọc DB qua session request, rồi `begin` mở kết nối thứ hai. Từ `pool_size + max_overflow` (15) lượt ghi đồng thời trở lên, cả loạt chờ nhau tới `pool_timeout` → 503 | `begin`/`discard` dùng `app.state.sessionmaker`, cùng pool với session request đang mở | B0-06 (`apps/api/core/routing.py`, `app.py`) | P2 | `d82745d` — pool riêng `claim_sessionmaker` (`CLAIM_POOL_SIZE = 5`, không overflow) cho `begin`/`discard`; test `test_claim_never_waits_on_the_request_pool` ghim pool request 1 kết nối: `503 == 200` trước sửa, xanh sau sửa. Review merge 2026-09-21 lần 3, finding P2 |
| ➖ | NO-041 | 2026-09-21 | 2026-09-21 | `request_hash` mã hoá bốn phần của BE-00 §7 (`method \| đường thật \| query đã sắp \| thân thô`) bằng **tiền tố độ dài 8 byte** thay cho dấu `\|` trần — lệch chữ hiến chương | Nối bằng `\|` trần thì đường thật chứa `\|` cho hai request khác nhau cùng một hash (review lần 2, finding #9) | B0-06 (`apps/api/core/idempotency.py:request_digest`) · chữ hiến chương thuộc người điều phối | Nit | Chấp nhận: cùng bốn phần, cùng thứ tự, chỉ khác cách ghép; hash chỉ so trong server, FE không tính lại. Đề nghị người điều phối sửa chữ BE-00 §7 thành "ghép có tiền tố độ dài"; đổi ý thì quay về `\|` và chấp nhận va chạm cùng người dùng, cùng khoá |
| ✅ | NO-042 | 2026-09-21 | 2026-09-21 | `tools/tests/test_services.py::_select_one` bắt tay Postgres với trần **5 s** qua chặng `host.docker.internal`, nên `test_ephemeral_postgres_dừng_xong_bản_dùng_chung_vẫn_chạy` đỏ giả khi chặng đó kẹt | Cùng lớp NO-007/NO-036: trần của đường request dùng cho đường cổng. Tái hiện: `run.sh verify` trên `45a73ad` (2026-09-21) bước 5 `1 failed, 1141 passed` — `TimeoutError` ở `test_services.py:33` khi nối **bản dùng chung** (`:98`), trong khi lượt ngay trước trên cùng mã thì xanh | B0-01 (`tools/tests/test_services.py`) | P2 | **FIX-007** — `_select_one` mặc định trần `GATE_CONNECT_TIMEOUT_S`, chỉ lượt nối bản **đã dừng** truyền `_STOPPED_TIMEOUT_S = 5`; `test_postgres_16_lên_thật` cũng mang trần cổng thay cho 60 s mặc định của asyncpg (R-24); test `test_trần_bắt_tay_theo_trạng_thái_bản_postgres` (đỏ `KeyError` → xanh) · trước: chữa: giữ 5 s cho bản **đã dừng** (lỗi `Errno 101/111` tới ngay), dùng `GATE_CONNECT_TIMEOUT_S` cho bản còn chạy. B0-06 không sửa (K27) |
| ✅ | NO-043 | 2026-09-21 | 2026-09-21 | Cuối lượt verify, `/tmp/contract-samples` không được chép ra `/src-out/contract-samples` như ENV §2 ghi: thư mục `contract-samples/` của worktree luôn rỗng, người điều phối không xem lại được mẫu golden của lượt vừa chạy | `tools/verify/steps.py` (và `in_container.sh`) không có bước chép nào; bước 7 vẫn đọc đúng mẫu vì `/tmp` mới mỗi container | B0-01 (`tools/verify/steps.py`) | P3 | **FIX-008** — `export_contract_samples()` chạy sau `run_steps` của `verify` (kể cả khi có bước hỏng): `CONTRACT_SAMPLES_DIR` đặt → `_clean_dir(OUT_DIR / "contract-samples")` rồi chép cây nếu có; không đặt → không làm gì; test `test_verify_chép_mẫu_golden_ra_thư_mục_ra`, `test_verify_không_có_mẫu_thì_thư_mục_ra_rỗng` (đỏ → xanh), `test_verify_ngoài_container_không_chép` · trước: phát hiện khi làm B0-07 (không sửa, K27). Chữa: sau `run_steps`, `shutil.copytree("/tmp/contract-samples", OUT_DIR / "contract-samples", dirs_exist_ok=True)` khi thư mục có |
| ✅ | NO-044 | 2026-09-21 | 2026-09-21 | Hai bộ khớp (method, đường thật) → thao tác: `_op_matchers`/`operation_of` ở `packages/testing/fixtures/api.py` và `operation_resolver` ở `packages/testing/golden/recorder.py` (R-07) | B0-07 cần thêm đường khuôn + tham số đường và thay được bảng thao tác cho app thử, mà `api.py` (B0-06) chỉ trả `operationId` từ bảng app thật | B0-06 (`packages/testing/fixtures/api.py`) | P3 | **FIX-028** — `operation_of` trả `.op` của `resolve_operation` (gắn lúc nhập, nên bảng app thử của test bộ ghi không lọt vào vết case); xoá `_op_matchers`. Test `test_case_trace_and_golden_resolve_the_same_operation`, `test_probe_table_does_not_leak_into_case_trace`; test cũ của `trace_case` giữ nguyên, xanh. Sửa cấu trúc thuần (R-07). Review merge B0-07 thêm `test_case_trace_asks_the_golden_resolver`: đỏ trên `api.py` trước sửa, xanh sau sửa |
| ➖ | NO-045 | 2026-09-21 | 2026-09-21 | Nhánh `feature/b3-01-spatial-scale-domain` vượt trần 400 dòng logic của MNT-05 (≈ 833 dòng sản phẩm, ≈ 190 là dữ liệu A14; 772 dòng test) | Một prompt giao hai gói móc vào nhau: `scale` nhập `spatial.area.js_round` và `spatial.model` | B3-01 | P2 | Chấp nhận như NO-039: tách là đưa vào `main` một nửa hợp đồng [2]. Review merge 2026-09-21 finding #2 (`docs/reviews/2026-09-21-feature-b3-01-spatial-scale-domain.md`) |
| ➖ | NO-047 | 2026-09-21 | 2026-09-21 | Nhánh `feature/b0-07-contract-harness` vượt trần 400 dòng logic của MNT-05 (≈ 1 010 dòng mã: 660 Python + 350 TypeScript; 914 dòng test) | B0-07 là **một** prompt: runner Node, `schema-map.ts`, bộ ghi golden, `check.py` và test của chúng móc vào nhau — test nào cũng cần runner thật | B0-07 | P2 | Chấp nhận: tách ra là đưa vào `main` một bước 7 không chạy được. Review merge 2026-09-21 finding #2 (như NO-039) |
| ✅ | NO-048 | 2026-09-21 | 2026-09-22 | Lượt verify lạnh (volume chưa có `CONTRACT_NODE_DIR/<băm lock>`): `npm ci` tải mạng từ `registry.npmjs.org` ngay trong **bước 5** (fixture phiên `contract_build`), vì bước 5 chạy trước bước 7 — trái "không tải mạng trong test" | B0-07 [8] buộc test dùng runner Node thật; chỉ bước chuẩn bị của cổng mới làm ấm `node_modules` trước pytest được. Mỗi băm lock tải một lần | B0-01 (`tools/verify/in_container.sh` hay ảnh verify) | P3 | mở — review merge B0-07 finding #7. Chữa: gọi `tools.contract.runner_client.ensure_node_modules` ở bước chuẩn bị, trước bước 5 · **🔧 FIX-032 · nhánh `fix/b0-01-gate-debts`** (giao 2026-09-22, người điều phối) · **FIX-032** — `tools/verify/steps.py` thêm bước "0" `step_warm_node_modules` đứng đầu chuỗi `verify`: gọi `runner_client.ensure_node_modules(runner_client.node_dir_from_env())` (đúng lời gọi của `build_layout`: cùng thư mục, băm lock, kho npm); chỉ chạy khi `--steps` rỗng hay gồm 5/7; `RunnerError`/`OSError` → bước hỏng "lỗi hạ tầng cổng: …", mọi bước sau "chưa chạy". Test `test_verify_làm_ấm_node_modules_trước_bước_5` ×4, `test_verify_làm_ấm_hỏng_là_lỗi_hạ_tầng_của_cổng` ×2 (đỏ 6 failed → xanh), `test_verify_không_bước_cần_runner_thì_không_làm_ấm` ×2 |
| ✅ | NO-049 | 2026-09-21 | 2026-09-22 | `packages/testing/golden/recorder.py` nhập tên riêng `_TEST_COMMON_RE`, `_TEST_OP_CASE_RE` của `tools/case_gate.py` | Bộ ghi cần đúng **một** nguồn tách tên test với `case_gate` (R-07), mà `case_gate` chưa xuất hàm công khai. B0-01 đổi tên hai hằng thì mọi phiên test hỏng lúc nhập (`api.py` nhập `recorder`) | B0-01 (`tools/case_gate.py`) | P3 | mở — review merge B0-07 finding #8. Chữa: `case_gate` xuất hàm công khai (vd `split_case_test_name(name) -> (op, case, end) \| None`); bộ ghi gọi hàm đó · **🔧 FIX-033, FIX-034 · nhánh `fix/b0-07-contract-tool-debts`** (giao 2026-09-22, người điều phối) · **FIX-033, FIX-034** — `case_gate` xuất `split_case_test_name(name) -> CaseTestName(op, case, tail, common) \| None` (mẫu chung xét trước) và `_found_cases_by_op` dùng chính nó, còn một chỗ tách; bộ ghi gọi hàm đó, bỏ nhập `_TEST_*_RE`. `test_tách_tên_test_case` (7 tham số) đỏ 7/7 → xanh; `test_recorder_imports_no_private_name_of_case_gate` đỏ 1/1 → xanh; test cũ của `case_gate` và `packages/testing/golden/` xanh nguyên |
| ✅ | NO-050 | 2026-09-21 | 2026-09-24 | ENV §2 và BE-00 §12 chỉ ghi `TESTCONTAINERS_HOST_OVERRIDE=host.docker.internal`; `deploy/compose/verify.yml` nay đặt thêm `TESTCONTAINERS_CONNECTION_MODE=bridge_ip` (FIX-009) | Hiến chương chỉ người điều phối sửa; FIX-009 đổi cấu hình cổng mà không được sửa `docs/charter/*` | người điều phối (`docs/charter/ENV.md` §2, `BE-00.md` §12) | P3 | mở — thêm một dòng: container verify nối testcontainers thẳng IP container trên mạng `bridge` (số đo ở NO-007), biến override cũ còn đó và vô hại · **đóng** (DEBT-01, người điều phối) `0a24717`: ENV §2 và BE-00 §12 ghi `TESTCONTAINERS_CONNECTION_MODE=bridge_ip` (bản dùng chung nối thẳng IP container; `ephemeral_*` đi `host.docker.internal` + cổng map; FIX-009) |
| ✅ | NO-051 | 2026-09-21 | 2026-09-24 | `export_contract_samples()` (FIX-008) ghi vào `VERIFY_OUT_DIR` (mặc định `/src-out`) mỗi khi `CONTRACT_SAMPLES_DIR` được đặt; chạy `tools.verify.steps verify` ngoài container với biến đó mà không có `VERIFY_OUT_DIR` ghi được thì hỏng ở `mkdir /src-out` | Mặc định `/src-out` chỉ có trong container verify; CI của B0-09 chạy ngoài container và có đặt `CONTRACT_SAMPLES_DIR` | B0-09 (job CI) | P3 | mở — ràng buộc bàn giao: job CI đặt `VERIFY_OUT_DIR` trỏ vào thư mục ghi được (vd `$RUNNER_TEMP/verify-out`) khi đặt `CONTRACT_SAMPLES_DIR` · FIX-084 (sha 844fea3): job_contract tự đặt VERIFY_OUT_DIR ghi được cùng lúc với CONTRACT_SAMPLES_DIR, hết mkdir /src-out hỏng ngoài container. Test test_job_contract_exports_writable_verify_out_dir đỏ trên main → xanh trên nhánh. Review APPROVE docs/reviews/2026-09-24-fix-debt-01-tooling.md. |
| ✅ | NO-052 | 2026-09-21 | 2026-09-22 | `python -m tools.contract.check` chạy không `--build-dir` để lại `/tmp/contract-*` (≈ 16 MB bản chép `src` của AppFront) sau mỗi lượt, không ai xoá | `main` dựng vào `tempfile.mkdtemp()` (sửa finding #4 của review lượt 1: không còn `rmtree` thư mục có sẵn) mà không dọn lúc xong. Trong cổng vô hại (`/tmp` mới mỗi container); CI hay chạy tay thì tích dần | B0-07 (`tools/contract/check.py:main`) | Nit | mở — review merge B0-07 lượt 2, Nit #1. Chữa: `tempfile.TemporaryDirectory()` khi không có `--build-dir` · **🔧 FIX-035 · nhánh `fix/b0-07-contract-tool-debts`** (giao 2026-09-22, người điều phối) · **FIX-035** — không `--build-dir` → `tempfile.TemporaryDirectory(prefix="contract-")` (qua `ExitStack`), xoá cả khi bước hỏng; có `--build-dir` → giữ lại. `test_main_without_build_dir_leaves_no_scratch_on_early_failure` và `test_main_without_build_dir_removes_built_layout` (bố cục đã dựng, symlink `node_modules`; kho dùng chung còn nguyên) đỏ 2/2 → xanh; test khói thêm assert `--build-dir` được giữ |
| ✅ | NO-053 | 2026-09-21 | 2026-09-21 | Năm test của `apps/api/core/tests/` (`test_app.py` ×4, `test_openapi.py` ×1) đỏ ngay khi B1-01 thêm `apps/api/auth/verifier.py` và `router.py` (`5 failed, 1400 passed` ở bước 5) | Test chốt trạng thái "repo chưa có module auth, đúng hai router, đúng ba thao tác" làm bất biến — cùng lớp NO-031..NO-033 | B0-06 (`apps/api/core/tests/`) | P1 | **FIX-029** — dựng đầu vào tại chỗ (`monkeypatch` vắng module auth), dò router so với đĩa, không ghim số thao tác. Đỏ 5/5 trước, xanh sau (`44 passed`). Spec: `docs/fixes.md` |
| ✅ | NO-054 | 2026-09-21 | 2026-09-21 | `rate_limit(...)` của B0-06 chốt `limit`/`window_s` là số nguyên lúc khai route; B1-01 cần hạn mức đọc **lười** từ `AuthSettings` (test nâng hạn mức bằng biến môi trường, C27; B0-06 nhập mọi module khi chưa có biến môi trường) nên dựng limiter **mỗi request** (`apps/api/auth/router.py:_login_ip_limit`, `_check_total`) | `rate_limit` không nhận hàm đọc cấu hình | B0-06 (`apps/api/core/ratelimit.py`) | Nit | mở — chi phí là một closure mỗi request, hành vi đúng. Chữa: cho `limit`/`window_s` nhận thêm `Callable[[], int]`, rồi B1-01 khai lại bằng `Depends(rate_limit(...))` · **FIX-030** — `limit`/`window_s` nhận `int \| Callable[[], int]`; B1-01 khai limiter một lần. Đỏ 2/2 trước, xanh sau |
| ✅ | NO-055 | 2026-09-21 | 2026-09-21 | Fixture `api_env` chỉ `FLUSHDB` DB cache, không `FLUSHDB` DB an toàn (`redis-broker` DB 2): bộ đếm `store="safe"` của route công khai (mọi lượt ASGI từ `127.0.0.1`) và khoá đăng nhập rò sang test sau | `api_env` phụ thuộc `cache_client` mà không phụ thuộc `safe_client` | B0-06 (`packages/testing/fixtures/api.py`) | P3 | mở — B1-01 tự `FLUSHDB` DB an toàn ở đầu `auth_app`/`probe_app` (và `safe_client` xoá lúc dọn). Chữa: `api_env` phụ thuộc thêm `safe_client` · **FIX-031** — `api_env` nhận thêm `safe_client`; B1-01 bỏ `FLUSHDB` thừa. Đỏ 1/1 trước, xanh sau |
| ➖ | NO-056 | 2026-09-21 | 2026-09-21 | Nhánh `feature/b1-01-auth-login-sessions` vượt trần 400 dòng logic của MNT-05 (≈ 1 270 dòng sản phẩm, ≈ 1 500 dòng test) | Một prompt; đăng nhập, phiên, refresh và verifier móc vào nhau (BE-00 §5) | B1-01 | P2 | Chấp nhận như NO-039/NO-045/NO-047: tách là đưa vào `main` một nửa thuật toán refresh. Review merge 2026-09-21 finding #1 (`docs/reviews/2026-09-21-feature-b1-01-auth-login-sessions.md`); P3 #2–#5 và Nit #6–#9, #11 đã sửa trên nhánh, Nit #10 chấp nhận, Nit #12 điền sha |
| ✅ | NO-057 | 2026-09-21 | 2026-09-22 | Tên `CHECK` của `idempotency_records` trong DB bị ghép tiền tố hai lần (`ck_idempotency_records_ck_idempotency_records_state`, `…_key_format`), lệch tên model (`ck_idempotency_records_state`) | Revision `r20260920_b0_06` truyền tên đầy đủ `name=f"ck_{TABLE}_…"` mà không bọc `op.f(...)`; quy ước `ck_%(table_name)s_%(constraint_name)s` của `Base.metadata` áp thêm một lần. `migrate_check` không so tên `CHECK` nên không bắt | B0-06 (`packages/db/migrations/versions/r20260920_b0_06_idempotency.py`) · **B0-03** (`packages/db/migrate_check.py`: bước "model khớp DB" không so tên `CHECK`, nên revision sau vẫn lặp được lỗi này mà cổng không đỏ) | P3 | mở — review merge B1-01 vòng hai, N2; vòng ba N4 thêm chủ B0-03 (B1-01 đã sửa phần của mình bằng `op.f`, có test so tên với model). Revision đã hợp nhất nên không sửa tại chỗ: đổi tên ràng buộc là `RENAME` (bị lint expand chặn) → revision **contract** của B0-06 ở lần phát hành sau, hoặc chấp nhận vì tên `CHECK` không ai tham chiếu · **🔧 FIX-036, FIX-037 · nhánh `fix/b0-03-check-constraint-names`** (giao 2026-09-22, người điều phối) · **FIX-037** — revision contract `r20260921_b0_06_fix037` (có trong `docs/contracts.toml`) `RENAME CONSTRAINT` hai tên về tên model, `downgrade` đổi ngược; cần FIX-038 (NO-058) để lint cho revision contract được phá huỷ. **FIX-036** — `migrate_check` thêm bước "tên CHECK khớp model" (`pg_constraint` `contype = 'c'` của bảng trong metadata, so với tên `format_constraint` dựng; in tên thừa/thiếu). `test_check_name_with_doubled_prefix_fails`, `test_check_names_matching_model_pass`: đỏ 2/2 trước, xanh sau; bước 6 chỉ có FIX-036 hỏng đúng hai tên của `idempotency_records`, có cả FIX-037 đạt |
| ✅ | NO-058 | 2026-09-22 | 2026-09-22 | `lint_migrations` không miễn revision contract đã đăng ký nên đường contract của BE-00 §6.1 không dùng được: revision `# contract:` có tên trong `docs/contracts.toml` mà `op.execute("… RENAME …")`/`drop_constraint` vẫn hỏng bước 6 | `_lint_file` chỉ kiểm `# contract:` đã đăng ký, rồi luôn gọi `_lint_body` với đủ luật expand; không test nào có revision contract mang thao tác phá huỷ | B0-01 (`tools/lint_migrations.py`) | P2 | Tìm thấy khi làm FIX-037 (đổi tên `CHECK` bằng `RENAME CONSTRAINT`) · **FIX-038** — revision contract đã đăng ký được miễn luật phá huỷ (drop, rename, `NOT NULL`, chuỗi SQL nguy hiểm); vẫn kiểm SQL không hằng, `batch_alter_table`, index khoá bảng, tên revision; chưa đăng ký như cũ. `test_contract_đã_đăng_ký_được_phá_huỷ`, `…_vẫn_kiểm_luật_không_phá_huỷ`: đỏ 2/32 trước, xanh 32/32 sau |
| ✅ | NO-060 | 2026-09-21 | 2026-09-22 | `packages/ml_contracts/payloads.py:check_object_key` chép luật khoá object của `packages.storage.keys.check_key` — hai nguồn cho một luật (R-07) | `ml_contracts` không được nhập `packages.storage` ([9] B5-01), mà luật khoá nằm ở `storage` chứ không ở `core` | B0-04 (`packages/storage/keys.py`) | P3 | mở — test `test_check_object_key_matches_storage_rules` giữ hai bản khớp trên 12 mẫu (rỗng, `..`, `//`, NFD, 1024/1025 byte, đuôi `.meta.json`). Chữa: dời `check_key`/`check_prefix` xuống `packages/core`, `storage` và `ml_contracts` cùng nhập; xoá bản chép · **🔧 FIX-043, FIX-044, FIX-045 · nhánh `fix/b0-04-object-key-debts`** (giao 2026-09-22, người điều phối) · **FIX-043, FIX-044, FIX-045** — luật khoá (`check_key`, `check_prefix`, `is_segment`, `MAX_KEY_BYTES`, `META_SUFFIX`) dời xuống `packages/core/object_keys.py`; `storage.keys` xuất lại tên cũ (`local.py`, `s3.py` không đổi), `ml_contracts.payloads` nhập thẳng, xoá `check_object_key`, `_prefix_key`; thông báo theo bản của `storage`. Test một nguồn thay test khớp hai bản: `test_key_rules_come_from_core` (storage; đỏ trên FIX-043: 1 failed → xanh), `test_object_key_rules_come_from_core` (ml_contracts; đỏ trên FIX-044: 1 failed → xanh). 12 mẫu biên cũ + mẫu của `storage` (thêm NFD — mẫu `khóa` cũ là NFC —, trần byte UTF-8) ở `packages/core/tests/test_object_keys.py` (đỏ: module chưa có → xanh 36 passed) |
| ⬜ | NO-061 | 2026-09-21 | | `onnx_sha256` ghim của `yolov8n`/`yolov8s` là SHA của ONNX mà **torch 2.14.0 + ultralytics 8.4.155 + onnx 1.22.0** (uv.lock hiện tại) xuất ra; sinh lại lock mà đổi một trong ba bản thì `export_pinned` thoát 2 | Xuất ONNX tất định theo bản thư viện, không theo trọng số; người điều phối luôn sinh lại lock khi hợp nhất (BE-00 §13.1) | B6-01 (bàn giao DEBT-01) | P2 | mở — đo lại: `export_yolo` trên `.pt` đã ghim, hai tiến trình cùng SHA, sửa `PINNED` và thêm revision B6-01 cập nhật `checksum_sha256` của bản gốc. `rapidocrRec` cũng đổi khi wheel `rapidocr-onnxruntime` đổi bản (`test_pinned_rapidocr_matches_wheel` báo đỏ ngay) · **bàn giao** 2026-09-24: đã chép vào `prompts/B6-01.md` (khối [6], dòng 110) — xuất lại ONNX, sửa `PINNED`, revision dữ liệu mới cập nhật `checksum_sha256` khi sinh lại lock |
| ✅ | NO-062 | 2026-09-21 | 2026-09-22 | Ảnh `ml` chưa chạy `python -m packages.ml_contracts.pinned fetch --dest /opt/models` rồi `python -m apps.ml.runtime.export_pinned --dest /opt/models` lúc build; chưa khai `ML_*` và biến lõi (`APP_ENV`, `SECRET_KEY`, `PUBLIC_BASE_URL`, `STORAGE_*`) mà `infer_context()` cần | Dockerfile/compose của app thuộc B0-08, `deploy/` mới có `verify.yml` (cùng gốc NO-006, NO-021) | B0-08 (`deploy/**`) | P2 | mở — `ml` chạy trong mạng `internal: true` nên phải tải ở bước build (BE-00 §9); thiếu `/opt/models/<name>.onnx` thì mọi bước ghim hỏng `MODEL_NOT_FOUND` · **đóng** — B0-08 `bf50968`: `ml.Dockerfile` chạy `pinned fetch` + `export_pinned` vào `/opt/models` lúc build; `base.yml` khai biến lõi, `STORAGE_*`, `ML_*` cho `ml`/`ml-gpu` (review lượt 1 xác nhận bản ghim có trong ảnh) |
| ⬜ | NO-063 | 2026-09-21 | | Bộ phân loại góc của `RapidOCR()` lật ngược chuỗi số gần đối xứng (`8.000` → `000'8`): trên trang tổng hợp seed 100-109 đọc đúng 67/122 khi bật, 114/122 khi tắt | Bộ phân loại 0°/180° của PP-OCR đoán sai với chữ 5x7 phóng to; chữ bản vẽ luôn thẳng trục nên nó không giúp gì | B5-04 (bàn giao DEBT-01) | Nit | mở — ràng buộc bàn giao, không phải nợ mã: B5-04 gọi nhận dạng với `use_cls=False` (hay tự xoay theo hướng hộp), đo lại tỉ lệ đọc trên `EVAL_SET_SEEDS` · **bàn giao** 2026-09-24: đã chép vào `prompts/B5-04.md` (khối [6], dòng 141) — `use_cls=False`, đo lại trên `EVAL_SET_SEEDS` |
| ➖ | NO-064 | 2026-09-21 | 2026-09-21 | Nhánh `feature/b5-01-ml-contracts-fakes-loader` vượt trần 400 dòng logic của MNT-05 (≈ 2 050 dòng sản phẩm, ≈ 2 460 dòng test) | Một prompt giao hai gói móc vào nhau: `apps/ml/runtime` nhập payload, artifact, bản ghim của `packages/ml_contracts` | B5-01 | P2 | Chấp nhận như NO-039, NO-045: tách là đưa vào `main` một nửa hợp đồng [2]/[10]. Review merge 2026-09-21 finding #2 (`docs/reviews/2026-09-21-feature-b5-01-ml-contracts-fakes-loader.md`) |
| ✅ | NO-065 | 2026-09-22 | 2026-09-22 | `packages/db/tests/test_new_revision.py`: `test_creates_revision_with_charter_name` và `test_fix_revision_allowed_once` đỏ khi bộ test vắt qua nửa đêm UTC giữa lúc nhập module và lúc test chạy (cổng bước 5 `F.F..........` trên `fix/b0-05-messaging-debts` @ `f4223f2`, máy ngủ 00:50→08:25 giờ VN trong lượt verify) | Test tính `TODAY = datetime.now(UTC)…` **một lần lúc nhập**, còn `new_revision.main` đọc `datetime.now(UTC)` **lúc gọi** (`packages/db/new_revision.py:66`), nên hai ngày lệch nhau là glob `r{TODAY}_…` rỗng. Tái hiện: plugin pytest đặt `TODAY` lùi một ngày ở `pytest_collection_finish` → đúng `2 failed, 11 passed` (`F.F..........`); không giả lập → `13 passed` (ba lượt: chạy riêng, cùng `packages/core packages/db`, thu thập cả repo) | B0-03 (`packages/db/tests/test_new_revision.py`; `packages/db/new_revision.py` nếu tiêm ngày) | P3 | mở — ghi trong phiên W2-MESSAGING (gộp `main` vào nhánh B0-05). Hướng sửa: `new_revision` nhận ngày qua `Clock` tiêm được (R-18) và test ghim bằng `fake_clock`, hoặc test đọc ngày ngay sau `main(...)`; không nới assert (K24) · **🔧 FIX-039 · nhánh `fix/b0-03-migration-tool-debts`** (giao 2026-09-22, người điều phối) · **FIX-039** — `new_revision.main(argv, *, clock=None)` đọc ngày từ `Clock` tiêm được (mặc định `SystemClock`) đúng lúc gọi; test ghim ngày bằng `fake_clock`, bỏ `TODAY` tính lúc nhập. Test mới `test_revision_date_read_from_clock_at_call_time` (đồng hồ 23:59:59Z → +2 s trước khi gọi → `r20260102_…`): `new_revision.py` cũ + test mới `3 failed, 11 passed` → bản sửa `14 passed` |
| ✅ | NO-066 | 2026-09-22 | 2026-09-22 | `apps/api/auth/tests/test_refresh.py::test_auth_refresh__C11_parallel` chập chờn: cổng đầy đủ trên `fix/b0-05-messaging-debts` @ `22dee96` đỏ bước 5 (`1 failed, 1961 passed, 10 skipped`; `At index 20 diff: 401 != 429` — ≥ 21 lượt 401 thay vì đúng `REFRESH_FAIL_LIMIT` = 20) | Chưa xác định. Mọi lượt 401 đi qua `_denied` → `bump` (`MULTI` nguyên khối, không đua được), nên thừa 401 nghĩa là `soft_redis(bump…)` trả `None` (lỗi phụ thuộc Redis → mở) — giả thuyết: một kết nối mới trong loạt 40 kết nối đồng thời chờ quá `CONNECT_TIMEOUT_S` = 2 s qua proxy cổng của Docker Desktop khi máy tải nặng (lượt đỏ chạy dưới `coverage`, song song một lượt verify khác). **Chưa tái hiện**: 14 lượt xanh — 2 lượt `packages/messaging apps/api/{core,auth,health} apps/worker` (583 passed), 6 lượt riêng test, 6 lượt riêng test kèm 6 tiến trình đốt CPU. Loại trừ: gộp nhánh B0-05 (đường này không đi qua mã nhánh đổi — `redis.py` chỉ đổi `ProcessLocal.reset`); đếm không nguyên khối. Lượt đỏ không in log bắt được (`configure_logging` thay handler gốc) nên không thấy có `rate_limit_open` hay không | B1-01 (`apps/api/auth/tests/test_refresh.py`, `apps/api/auth/router.py`) | P2 | mở — ghi trong phiên W2-MESSAGING. Hướng: test bắt sự kiện `rate_limit_open` (Redis hỏng thì báo đúng tên, không thành "đếm sai"), chạy cả bộ dưới `coverage` nhiều lượt để tái hiện; không nới assert, không retry (K24) · **🔧 FIX-048 · nhánh `fix/b1-01-parallel-refresh-flake`** (giao 2026-09-22, người điều phối) · **FIX-048** — **gốc đã xác định**: một số request kẹt ~69 s ở bắt tay Postgres qua chặng `host.docker.internal` (NO-007) → `bump` của chúng rơi sau khi cửa sổ cố định 60 s (`REFRESH_FAIL_WINDOW_S`) của lượt đầu hết → `INCR` đếm lại từ 1 → thừa 401; không phải đếm sai (`router.py` đúng thiết kế, không sửa), không phải Redis hỏng (0 `rate_limit_open`). Tái hiện (base `cf98f29` = `main` `5f39fb2` đã gộp FIX-009; một container, `coverage run`, test thăm dò = luồng C11_parallel + giờ từng lượt + bộ đếm cuối loạt + `caplog`): đường cũ (`env -u TESTCONTAINERS_CONNECTION_MODE`) **3/300 đỏ** — thăm dò 2/100 không tải (22 và 26 × 401, lượt cuối xong 69,07 / 69,74 s, bộ đếm cuối 2 / 6 TTL 60 = cửa sổ thứ hai) + test gốc 1/50 kèm 4 tiến trình đốt CPU; đường mới (bridge IP) 0/150 không tải, 0/150 có tải (loạt ≤ 3,6 s). FIX-009 đã chữa đường hạ tầng; FIX-048 làm test không phụ thuộc và báo đúng tên: C11_parallel đặt cửa sổ `BURST_WINDOW_S` = 2 × `GATE_CONNECT_TIMEOUT_S` (360 s, dài hơn kẹt mà cổng cố ý chịu), `caplog.at_level(WARNING)` rồi `_assert_exact_fail_limit` kiểm theo thứ tự: 0 `rate_limit_open` ("redis-cache hỏng … không phải đếm sai") → bộ đếm cuối ≥ trần ("cửa sổ thứ hai") → đúng 20 × 401 + 20 × 429 (assert đếm giữ nguyên). Test tất định mới `test_a_denial_after_the_fail_window_counts_from_one` (39 lượt, `DEL` khoá bộ đếm, một lượt muộn → 21 × 401, bộ đếm = 1). Đỏ trước: C11_parallel cũ dưới điều kiện đó `At index 20 diff: 401 != 429` (1 failed); test mới với bộ kiểm cũ `Regex pattern did not match` (1 failed) → xanh. Sau sửa: 100/100 xanh đường mới + 4 đốt CPU, 100/100 xanh đường cũ trong đó 3 lượt kẹt 69,3–70,0 s. Lượt đỏ gốc không in log vì logger gốc bị Celery để ở ERROR → NO-078 |
| ✅ | NO-067 | 2026-09-22 | 2026-09-22 | Bộ lọc của `_found_cases_by_op` (`tools/case_gate.py:347-348`) không test nào chạm: không có tên không phải test case hay dạng chung với case không phải case chung (`test_common__C01[op]`); dòng `continue` 348 chưa chạy lần nào, đột biến bỏ bộ lọc case chung vẫn `58 passed` | Test cũ chỉ dựng tên khớp (bản `afa29ed` thiếu nhánh `326→328`, `330→319`); FIX-033 gộp hai nhánh thiếu thành một dòng | B0-01 (`tools/tests/test_case_gate.py`) | P3 | mở — review merge 2026-09-22 finding #1 (`docs/reviews/2026-09-22-fix-b0-07-contract-tool-debts.md`). Chữa: một test `evaluate` với `test_common__C01[x_create]` và `test_x_create_is_public` (`passed`, có vết) → không vào `found`/`common` · **🔧 FIX-049 · nhánh `fix/b0-05-quiet-release-case-filter`** (giao 2026-09-22, người điều phối) · **FIX-049** — test `test_dạng_chung_với_case_riêng_và_tên_lạ_không_được_tính` đưa `test_common__C01[x_create]` và `test_x_create_is_public` (`passed`, có vết `op="x_create"`, 200) qua `evaluate` → `found` rỗng; đỏ trên `case_gate.py` đột biến M1 (bỏ vế lọc case chung, bản trong `.cache/`): `1 failed, 58 passed` (`assert {'C01'} == set()`) → xanh trên mã hiện tại `59 passed`; `tools/case_gate.py` không đổi |
| ✅ | NO-068 | 2026-09-22 | 2026-09-22 | `tools/lint_migrations.py:_lint_body` vượt R-08: 78 dòng, CC 28, 25 nhánh, 55 lệnh (trên `main` 69 dòng, CC 28); FIX-038 thêm 9 dòng | Mọi luật của BE-00 §6.1 viết nối tiếp trong một vòng lặp qua node | B0-01 (`tools/lint_migrations.py`) | P3 | mở — review merge 2026-09-22 `fix/b0-03-check-constraint-names` finding #1. Chữa: mỗi luật một hàm `_check_<luật>(node, …)`; tách `_DESTRUCTIVE_CALL_NAMES` khỏi `batch_alter_table`, bỏ `Report()` dùng một lần (finding #4) · **🔧 FIX-042 · nhánh `fix/b0-03-migration-tool-debts`** (giao 2026-09-22, người điều phối) · **FIX-042** — mỗi luật một hàm (`_check_banned_call`, `_check_alter_column`, `_check_add_column`, `_check_raw_sql`, `_check_create_index`, chọn bằng `_check_call`), mỗi vi phạm mang cờ `destructive`; `_lint_body` bỏ `Report()` dùng một lần (`if not (contract and finding.destructive)`), bỏ tham số `source` không dùng; `_DESTRUCTIVE_CALL_NAMES` tách khỏi `_BATCH_ALTER_TABLE`; `_const_str`/`_is_const`/`_keywords` thay năm chỗ chép tay; `_in_autocommit_block` bỏ tham số thừa, lồng 4 → 0. Đo: `_lint_body` 78 dòng, CC 28, lồng 5 → 18 dòng, CC 5, lồng 3; hàm mới ≤ 12 dòng, CC ≤ 5. Vi phạm (cả thứ tự) của lint cũ và mới trùng nhau trên 168 thân revision thử (thường/contract đã/chưa đăng ký). `tools/tests/test_lint_migrations*.py` `32 passed` trước → `34 passed` sau; test chốt mới `test_tên_thêm_vào_destructive_chỉ_được_miễn_cho_contract[True|False]` trên lint cũ `2 failed` (`AttributeError`) |
| ✅ | NO-069 | 2026-09-22 | 2026-09-22 | `packages/db/migrate_check.py:run_checks` vượt R-08: 82 dòng, CC 13 (trên `main` 77 dòng, CC 12); FIX-036 thêm một closure bước | Mười bước khai thành closure lồng trong `run_checks` | B0-03 (`packages/db/migrate_check.py`) | P3 | mở — review merge 2026-09-22 `fix/b0-03-check-constraint-names` finding #1. Chữa: đưa các bước lên mức module, nhận `engine`/`config`/`target` qua tham số; `run_checks` chỉ còn vòng lặp và dọn engine · **🔧 FIX-040 · nhánh `fix/b0-03-migration-tool-debts`** (giao 2026-09-22, người điều phối) · **FIX-040** — mười bước lên mức module (`_one_head`, `_alembic(action, config, revision)`, `_seed_twice`, `_only_version_table`, `_metadata_matches`, `_check_names_match`, `_run_step`), danh sách bước ở `_steps(config, engine, target, seed_runner)`; `run_checks` chỉ còn vòng lặp và dọn engine. Đo (`ruff --isolated --select C901`, AST): `run_checks` 82 dòng, CC 13, lồng 3 → 25 dòng, CC 3, lồng 3; hàm mới ≤ 14 dòng, CC ≤ 2. Tên bước, thứ tự, mã thoát không đổi; `packages/db/tests` `96 passed` trước và sau |
| ✅ | NO-070 | 2026-09-22 | 2026-09-22 | Bước "tên CHECK khớp model" của `migrate_check` đỏ giả với CHECK gắn kiểu mà DDL Postgres bỏ qua: `Boolean(create_constraint=True)` làm bước ném `InvalidRequestError`, `Enum(…, create_constraint=True)` native làm bước báo "thiếu trong DB" | `_check_name_drift` lấy mọi `CheckConstraint` của bảng, không xét `_create_rule` của dialect | B0-03 (`packages/db/migrate_check.py`) | P3 | mở — review merge 2026-09-22 `fix/b0-03-check-constraint-names` finding #2, probe trên postgres:16-alpine. Chưa model nào dùng (`grep create_constraint`, `Enum(` rỗng). Chữa: bỏ ràng buộc có `_create_rule` trả `False` với DDL compiler, comment ngưỡng theo R-05; tối thiểu ghi giới hạn vào docstring kèm test chốt · **🔧 FIX-041 · nhánh `fix/b0-03-migration-tool-debts`** (giao 2026-09-22, người điều phối) · **FIX-041** — `_check_name_drift` chỉ xét `CHECK` mà `_should_create_for_compiler` (cùng cổng `DDLCompiler.create_table_constraints` dùng, xét `_create_rule`/`ddl_if`) cho qua với DDL compiler của dialect; comment R-05 ghi ngưỡng (đọc mã SQLAlchemy 2.0.54) và đường nâng cấp. Test mới `test_check_bound_to_native_type_passes[boolean|enum]`, `…_keeps_table_check_drift[boolean|enum]`: `migrate_check.py` cũ `4 failed` (boolean ném `InvalidRequestError`, enum báo thiếu `ck_thing_kind`) → bản sửa `packages/db/tests` `96 passed` · **FIX-041 (bổ sung)** — cổng chỉ áp cho `CHECK` mức bảng: `visit_create_column` phát mọi `CHECK` trong `Column(...)` không qua `_should_create_for_compiler`, nên lọc cả mức cột làm `CHECK` cột có `ddl_if` lệch dialect thành "thừa trong DB". Test `test_column_check_with_ddl_if_is_still_expected`: bản trước `1 failed` (`thừa trong DB: ['ck_thing_n_positive']`) → `packages/db/tests` `98 passed` |
| ✅ | NO-071 | 2026-09-22 | 2026-09-24 | BE-00 §6.1 "Vòng kiểm" chưa có bước "tên CHECK khớp model" (FIX-036); gạch "Revision contract" chưa nói revision đã đăng ký được miễn đúng các luật phá huỷ (FIX-038); §12 dòng 6 chưa liệt kê bước mới | Hiến chương viết trước FIX-036, FIX-038 | người điều phối (`docs/charter/BE-00.md`) | P3 | mở — review merge 2026-09-22 `fix/b0-03-check-constraint-names` finding #3. Chữa: "… → model khớp DB → tên CHECK khớp model"; thêm vào gạch contract: miễn `drop_*`, `rename_table`, `alter_column` đổi tên/`nullable=False`/`type_`, chuỗi SQL phá huỷ, `add_column` NOT NULL không default; vẫn kiểm SQL không hằng, `batch_alter_table`, index khoá bảng, tên revision · **đóng** (DEBT-01, người điều phối) `0a24717`: BE-00 §6.1 vòng kiểm thêm "→ tên CHECK khớp model" (FIX-036); gạch "Revision contract" ghi miễn luật phá huỷ, vẫn kiểm SQL không hằng, `batch_alter_table`, index khoá bảng, tên revision (FIX-038); §12 bước 6 liệt kê bước mới |
| ✅ | NO-072 | 2026-09-22 | 2026-09-22 | `test_delete_prefix_deletes_in_batches` chỉ ghi 3 object nên không chạm biên 1000 khoá/lượt `DeleteObjects` mà FIX-012 [6] yêu cầu ("xoá > 1 lô") | Chọn cỡ nhỏ cho test nhanh; "≤ 1000 khoá/lượt" là hành vi của `minio.remove_objects`, chưa test nào chứng minh | B0-04 (`packages/storage/tests/test_s3.py:186`) | P3 | mở — review merge 2026-09-22 finding #1 (`docs/reviews/2026-09-22-fix-b0-04-storage-debts.md`). Chữa: 1001 object ghi song song, khẳng định đúng 2 `POST ?delete`, 0 `DELETE`, tiền tố rỗng · **🔧 FIX-047 · nhánh `fix/b0-04-object-key-debts`** (giao 2026-09-22, người điều phối) · **FIX-047** — `test_delete_prefix_deletes_in_batches` ghi 1001 ảnh trang qua proxy (trần 8 lượt ghi song song, dưới `maxsize=10` của pool), khẳng định 0 `DELETE`, đúng 2 `POST ?delete`, 2 `POST` tổng, tiền tố rỗng sau khi xoá. Đỏ trên `_delete_under` trước FIX-012 (`git show 6c256d4^`): `assert 1001 == 0` (1001 `DELETE`), 1 failed → xanh 3/3 lượt (6–12 s) trên MinIO thật |
| ✅ | NO-073 | 2026-09-22 | 2026-09-22 | `keys._SERVER_NAMED_RE` chép tay luật id tầng `L-[0-9A-Z]{10,64}` của `packages/core/ids.py:61` — hàm dựng khoá và `server_chosen_kind` đọc luật từ hai nguồn (R-07) | Một regex cho gọn thay vì kiểm từng đoạn bằng `is_id`/`is_spatial_id` như hàm dựng khoá | B0-04 (`packages/storage/keys.py:30-34`) | P3 | mở — review merge 2026-09-22 finding #2; cùng họ NO-060. Chữa: tách khoá theo `/` và kiểm từng đoạn bằng `is_id`/`is_spatial_id`; hoặc test khoá trang với id tầng dài 10 và 64 · **🔧 FIX-046 · nhánh `fix/b0-04-object-key-debts`** (giao 2026-09-22, người điều phối) · **FIX-046** — xoá `_SERVER_NAMED_RE`; `server_chosen_kind` tách khoá theo `/` (`match`) và hỏi chính hàm dựng `avatar`/`upload_page` (tức `is_id`/`is_spatial_id` của `core/ids.py`): khoá do server đặt ⇔ hàm dựng sinh lại đúng khoá đó. Hai luật id tầng trùng khít (dài 10/64 đạt, 9/65 không: `test_server_chosen_kind_follows_level_id_length` xanh cả trước lẫn sau), nên chốt một nguồn bằng đột biến: `test_server_chosen_kind_reads_id_rules_of_core` (vá `keys.is_spatial_id`/`keys.is_id` thành luôn sai → khoá thôi là khoá server đặt; đỏ trước 2 failed). Hẹp hơn có chủ ý: `pages/007.png` không còn là khoá server đặt (hàm dựng không sinh số 0 đứng đầu, regex cũ nhận; đỏ trước 1 failed). Tổng: đỏ 3 failed → xanh 40 passed |
| ✅ | NO-074 | 2026-09-22 | 2026-09-22 | Logic "trả khoá; Redis hỏng thì `WARNING` rồi bỏ qua (TTL dọn hộ), không che lỗi của thân" có hai bản: `SafeLock._release_quietly` (FIX-025) và `_Keeper._release` của `apps/ml/runtime/gpu.py` (B5-01) | Hai nhánh viết song song (`fix/b0-05-messaging-debts` và `feature/b5-01`), gặp nhau ở lần gộp `c03b8c1`; bản của B0-05 là riêng tư nên B5-01 không gọi được | B0-05 (`packages/messaging/locks.py:103-112`) · B5-01 (`apps/ml/runtime/gpu.py:129-136`) | P3 | mở — review merge 2026-09-22 finding #1 (`docs/reviews/2026-09-22-fix-b0-05-messaging-debts.md`), trái R-07. Chữa: B0-05 công khai `release_quietly(token)`; B5-01 gọi `runner.run(lock.release_quietly(token))` (FIX cho B5-01, K27) · **🔧 FIX-050, FIX-051 · nhánh `fix/b0-05-quiet-release-case-filter`** (giao 2026-09-22, người điều phối) · **FIX-050, FIX-051** — FIX-050: `SafeLock._release_quietly` thành `release_quietly(token)` công khai (docstring nói khi nào dùng thay `release`), `hold` gọi nó; test `test_release_quietly_turns_a_dead_redis_into_one_warning` (Redis chết → đúng một `WARNING lock_release_failed` từ `packages.messaging.locks`, không ném) đỏ `1 failed, 22 passed` (`AttributeError: … no attribute 'release_quietly'`) → xanh. FIX-051: `gpu.py` gọi `runner.run(lock.release_quietly(token))`, xoá `_Keeper._release`; tên sự kiện đổi `gpu_lock_release_failed` → `lock_release_failed` (trường `lock=gpu:0` vẫn phân biệt GPU; không test/tài liệu nào chốt tên cũ; tham số tên sự kiện cho một người gọi trái R-10). `test_gpu_slot_release_survives_a_dead_redis` (thân ném `ZeroDivisionError` sau khi Redis chết → lỗi thân nổi lên, đúng một `WARNING` từ `packages.messaging.locks`) và `test_gpu_release_has_no_catch_of_its_own` (AST: `gpu.py` chỉ gọi `release_quietly`, không `try` nào bọc) đỏ `2 failed, 10 passed` → xanh; `packages/messaging` + `apps/ml/runtime` + `test_case_gate` `324 passed` |
| ✅ | NO-075 | 2026-09-22 | 2026-09-22 | `cmd_verify` chép mẫu golden (`export_contract_samples`) trước khi in bảng và không bắt lỗi: thư mục ra thiếu hay không ghi được → traceback, không có bảng cổng, lượt 8/8 đạt vẫn thoát 1 | FIX-008 đặt lời gọi trước `_print_table`; `OSError` của `_clean_dir`/`copytree` nổi thẳng ra khỏi `cmd_verify` | B0-01 (`tools/verify/steps.py:265-269`) | P3 | mở — review merge 2026-09-22 `fix/b0-01-gate-debts` finding #1 (probe: `VERIFY_OUT_DIR=/proc/rv` → `FileNotFoundError`, 0 dòng bảng). Chữa: in bảng trước, bọc chép mẫu trong `try/except OSError` → in lỗi ra stderr và thoát 1 (hay thêm dòng bảng "chép mẫu golden"), kèm test OUT_DIR không ghi được · **🔧 FIX-052 · nhánh `fix/b0-01-gate-log-debts`** (giao 2026-09-22, người điều phối) · **FIX-052** — `cmd_verify` bọc `export_contract_samples()` trong `except OSError` → thêm dòng bảng `— | chép mẫu golden | hỏng | lỗi hạ tầng cổng: …` (lỗi đầy đủ ra stderr), bảng luôn in, thoát 1; dòng lỗi dựng chung với bước 0 qua `_infra_failure` (không chép lần hai). Test `tools/tests/test_steps_commands.py::test_verify_chép_mẫu_hỏng_vẫn_in_đủ_bảng_và_thoát_1` (thư mục ra nằm dưới một file → `NotADirectoryError`): đỏ trên mã cũ (1 failed) → xanh |
| ✅ | NO-076 | 2026-09-22 | 2026-09-22 | `packages/storage/keys.py:26` `_ULID_RE` chép thân ULID `[0-9A-HJKMNP-TV-Z]{26}` của `packages/core/ids.py:35` `_ULID_BODY` — hàm dựng `avatar` (và qua nó `server_chosen_kind`) đọc luật thân ULID từ bản chép (R-07) | `core/ids.py` chỉ phơi `is_id(prefix, value)` (thân ULID kèm tiền tố); tên ảnh đại diện là ULID trần không tiền tố nên `storage` tự chép mẫu | B0-02 (`packages/core/ids.py`) · B0-04 (`packages/storage/keys.py`) | P3 | mở — tìm thấy khi làm FIX-046 (W7-KEYS), cùng họ NO-060, NO-073. Chữa: B0-02 phơi hàm kiểm thân ULID không tiền tố ở `core/ids.py`, B0-04 dùng trong `avatar` và xoá `_ULID_RE` · **🔧 FIX-054, FIX-055 · nhánh `fix/b0-04-key-layout-debts`** (giao 2026-09-22, người điều phối) · **FIX-054** — `core/ids.py` phơi `is_ulid(value)` (thân ULID trần; mẫu biên dịch sẵn `_ULID_RE` thay chuỗi `_ULID_BODY`), `is_id` kiểm thân qua nó: `test_is_ulid` (13 ca: rỗng, chữ thường, 25/27 ký tự, I/L/O/U, xuống dòng cuối, chữ số toàn khổ, có tiền tố) và `test_is_id_reads_ulid_rule_of_is_ulid` (vá `ids.is_ulid` thành luôn sai → `is_id` sai); đỏ 14 failed → xanh 14 passed · **FIX-055** — `storage.keys.avatar` gọi `is_ulid`, xoá `_ULID_RE`: `test_avatar_name_follows_ulid_rule_of_core` (đột biến `ids._ULID_RE` → tên ảnh đại diện bị từ chối); đỏ 1 failed → xanh 1 passed |
| ✅ | NO-077 | 2026-09-22 | 2026-09-22 | `packages/ml_contracts/payloads.py:115` `_upload_prefix` dựng lại bố cục `projects/{prj}/floors/{L-…}/uploads/{upl}/` của `packages/storage/keys.py:64` `upload_prefix` — bố cục khoá BE-00 §8 có hai nguồn (R-07) | `ml_contracts` không được nhập `packages.storage` ([9] B5-01) mà hàm dựng bố cục khoá nằm ở `storage`; FIX-043 chỉ dời luật khoá chung xuống `core`, không dời bố cục | B0-04 (`packages/storage/keys.py`) · B5-01 (`packages/ml_contracts/payloads.py`) | P3 | mở — tìm thấy khi làm FIX-045 (W7-KEYS). Chữa: dời phần bố cục dùng chung (tiền tố lượt tải lên) xuống `packages/core`, `storage` và `ml_contracts` cùng gọi; hoặc `➖` kèm lý do · **🔧 FIX-056, FIX-057, FIX-058 · nhánh `fix/b0-04-key-layout-debts`** (giao 2026-09-22, người điều phối) · **FIX-056** — `core/object_keys.py` nhận `project_prefix`, `upload_prefix` và bộ tách `upload_prefix_of(key)` (đọc id ở vị trí đoạn rồi dựng lại bằng `upload_prefix`, chỉ nhận khi trùng đầu khoá từng byte); `core/ids.py` nhận `check_id(prefix, value)` (dạng ném lỗi của `is_id`, phán quyết người điều phối: luật id ở `ids.py`). Chỉ dời tiền tố dự án + lượt tải lên, các hàm dựng khoá khác ở lại `storage` (phần lặp còn lại: NO-081). Test `test_check_id_*`, `test_upload_prefix_*`, `test_upload_prefix_of_*` (bố cục, id sai nêu trường, đột biến `is_spatial_id`/`is_id`/`upload_prefix`); đỏ 27 failed → xanh 27 passed · **FIX-057** — `storage.keys` xuất lại `project_prefix`/`upload_prefix` giữ tên, xoá `_entity_id`/`_level_id` và gọi `check_id`: `test_upload_layout_comes_from_core` (`is` đúng hàm lõi) và `test_server_chosen_kind_reads_id_rules_of_core` đổi đích đột biến sang `object_keys.is_spatial_id`/`ids.is_id` (luật nay đọc ở lõi); đỏ 4 failed → xanh 4 passed · **FIX-058** — `ml_contracts` gọi `upload_prefix_of`, xoá `_upload_prefix`: `test_upload_layout_comes_from_core` (đột biến `object_keys.upload_prefix` → payload hợp lệ bị từ chối); đỏ 1 failed → xanh 1 passed |
| ✅ | NO-078 | 2026-09-22 | 2026-09-22 | Sau các test `packages/messaging` trong thứ tự cổng, logger gốc nằm ở mức 40 (ERROR) với `StreamHandler` của Celery: mọi `WARNING` của test chạy sau (`apps/**`, `tools/**`) không vào mục "Captured log call" của báo cáo đỏ, trừ khi test tự `caplog.at_level` — chính vì vậy lượt đỏ NO-066 gốc không cho thấy có `rate_limit_open` hay không | `start_worker(...)` dùng `loglevel` mặc định `"error"` của `celery.contrib.testing.worker`; `app.log.setup` chiếm logger gốc (thay handler, đặt mức) và không trả lại khi worker dừng | B0-05 (`packages/testing/fixtures/messaging.py:149`) | P2 | mở — phát hiện trong FIX-048 (2026-09-22). Đo: cổng theo thứ tự (`packages` + `apps/api/auth`), đường cũ, `coverage run`: test thăm dò in `root.level 40 handlers [<StreamHandler <stderr>>, LogCaptureHandler × 2]`; `caplog.at_level(WARNING)` vẫn bắt được `rate_limit_open`. Chữa: fixture lưu mức + handler của logger gốc và trả lại sau `start_worker` (hoặc nối một receiver vào `celery.signals.setup_logging` cho app thử — `worker_hijack_root_logger=False` KHÔNG đủ trên Celery 5.6.3, review `fix/b1-01-parallel-refresh-flake` probe P9), kèm test chốt mức gốc không đổi sau worker · **🔧 FIX-059 · nhánh `fix/b0-01-gate-log-debts`** (giao 2026-09-22, người điều phối) · **FIX-059** — `celery_worker_factory` nối receiver `_keep_root_logger` vào `celery.signals.setup_logging` trong lúc dựng worker thử (gỡ ở `finally`): có receiver thì `app.log.setup` bỏ hẳn phần cấu hình logger gốc. Test `packages/messaging/tests/test_tasks.py::test_the_worker_factory_leaves_the_root_logger_alone` (mức + handler gốc trước = trong = sau worker; `WARNING` sau worker vào `caplog` không cần `at_level`): đỏ trên fixture cũ (1 failed, `(40, [StreamHandler]) != (30, [_LiveLoggingNullHandler…])`; chạy sau test worker khác vẫn đỏ vì handler bị thay) → xanh |
| ⬜ | NO-079 | 2026-09-22 | | BE-00 §2.2 "Thành phần dùng chung → chủ" chưa có dòng cho luật khoá object ở `packages/core/object_keys.py` (`check_key`, `check_prefix`, `is_segment`, `MAX_KEY_BYTES`, `META_SUFFIX`) mà FIX-043 dời xuống lõi; `prompts/B2-04.md:128` (lịch `purge_upload_orphans`) còn tự viết regex bố cục + id của khoá lượt tải lên | Hiến chương và prompt viết trước FIX-043 | B2-04 (phần regex khoá lượt tải; bàn giao DEBT-01) | P3 | mở — review merge 2026-09-22 `fix/b0-04-object-key-debts` finding #1; cùng họ NO-073, NO-076, NO-077. Chữa: thêm dòng §2.2 "Luật khoá object … \| packages/core/object_keys.py \| B0-02 · gọi: B0-04, B5-01"; B2-04 [6] lọc khoá bằng hàm của `packages.storage.keys` thay regex · Mở rộng 2026-09-22 (review merge `fix/b0-04-key-layout-debts` finding #7): FIX-054/056 thêm vào lõi dùng chung `packages/core/object_keys.py` `project_prefix`, `upload_prefix`, `upload_prefix_of` và `packages/core/ids.py` `is_ulid`, `check_id` — dòng §2.2 cần ghi cả năm tên (chủ B0-02 · gọi: B0-04, B5-01) và luật đặt: lõi giữ phần bố cục mà gói không được nhập `storage` phải kiểm, hàm dựng khoá ở `storage` · Mở rộng 2026-09-22 (review merge `fix/b0-04-layout-and-c11-debts` finding #1): FIX-061 thêm vào lõi dùng chung `packages/core/object_keys.py` `run_prefix(upload_root, run, step)` (kèm luật bước ∈ `PIPELINE_STEPS`, dời từ `storage`), `model_prefix(model)` và hằng `MODELS_PREFIX` — dòng §2.2 ghi thêm ba tên (chủ B0-02 · gọi: B0-04, B5-01). Lõi nay giữ tiền tố của 4/7 dạng khoá BE-00 §8 (original, pages qua `upload_prefix`; `runs/`; `ml/models/`); `library/`, `ml/datasets/`, `users/…/avatar/` và mọi hàm dựng khoá đầy đủ ở `packages/storage/keys.py` — dời nốt hay giữ đường cắt "theo ai cần" là quyết định của dòng này · phần BE-00 §2.2 **xong** `0a24717` (dòng "Luật khoá object và bố cục tiền tố dùng chung", chủ B0-02, 12 tên của `object_keys.py` + `ids.py`). **Bàn giao** phần regex: đã chép vào `prompts/B2-04.md` (khối [6], dòng 131) |
| ✅ | NO-080 | 2026-09-22 | 2026-09-22 | `bash tools/verify/run.sh verify`: khi shell bọc bị cắt (Claude Code thu hồi shell nền lúc máy thiếu RAM), output bước 4–8 mất hẳn — container `verify-run` tự xoá khi thoát (AutoRemove) và output chỉ đi qua stdout của client `docker compose run` đã chết, nên `docker logs` trả `no such object`; chỉ còn mã thoát qua `docker wait`, không có số `passed/failed/skipped` bước 5, không có độ phủ (lượt W6-DBTOOLS trên `fix/b0-03-migration-tool-debts` @ `e5d0247`, 2026-09-22T04:21Z→04:28:57Z, `docker wait` = 0) | Log cổng không được ghi ra thư mục mount của host (`/src-out`), chỉ ra stdout của container | B0-01 (`tools/verify/run.sh`, `tools/verify/steps.py`, `deploy/compose/verify.yml`) | P3 | mở — ghi trong phiên W6-DBTOOLS (id do người điều phối cấp). Hướng sửa: `steps.py` ghi bảng cổng, tóm tắt pytest và `coverage_gate` vào `/src-out/verify/<lượt>.log` (mount host) song song stdout; tạm thời: chạy `docker logs -f <container>` ra file ngay sau khi container lên · **🔧 FIX-053 · nhánh `fix/b0-01-gate-log-debts`** (giao 2026-09-22, người điều phối) · **FIX-053** — `run.sh verify` mount `.cache/src-out/verify` của worktree vào `/src-out/verify` (như `lock`/`openapi`, không đổi `verify.yml`), đặt `VERIFY_LOG_FILE=/src-out/verify/<UTC>-<sha12>.log` và in đường dẫn host lúc bắt đầu; `steps.cmd_verify` thay fd 1, 2 bằng hai ống + luồng chép sang fd gốc và file (cả output tiến trình con: ruff, pytest, `coverage_gate`), stdout/stderr vẫn tách, thêm dòng `mã thoát: N`; đợi luồng xả xong trước khi thoát (trần `LOG_DRAIN_TIMEOUT_S`, tiến trình con mồ côi không treo cổng), đích ghi hỏng thì bỏ đích đó và báo stderr, không mở được log → `OSError` trước bước đầu. Test `tools/tests/test_steps_commands.py` (`…ghi_cả_output_của_lượt_ra_file_log`, `…log_nối_thêm_không_đè_lượt_trước`, `…log_không_mở_được_thì_hỏng_trước_bước_đầu`, `…tiến_trình_con_mồ_côi_không_treo_cổng`, `test_write_each_bỏ_đích_hỏng_giữ_đích_còn_lại`): đỏ trên mã sau FIX-052 (5 failed) → xanh |
| ✅ | NO-081 | 2026-09-22 | 2026-09-22 | Bố cục khoá BE-00 §8 còn hai nguồn ngoài tiền tố lượt tải lên: đoạn `runs/{run}/{step}/` (`packages/storage/keys.py:63` `run_artifact` · `packages/ml_contracts/payloads.py:132` `InferStepPayload._consistent`) và `ml/models/{mdl}/` (`packages/storage/keys.py:75` `model_artifact` · `packages/ml_contracts/payloads.py:26` `MODELS_PREFIX`, dùng ở `:110`, `:297`) (R-07) | `ml_contracts` không được nhập `storage` ([9] B5-01); FIX-056 chỉ dời tiền tố dự án + lượt tải lên xuống `packages/core/object_keys.py` theo phán quyết người điều phối, không kéo các hàm dựng khoá khác | B0-02 (`packages/core/object_keys.py`) · B0-04 (`packages/storage/keys.py`) · B5-01 (`packages/ml_contracts/payloads.py`) | P3 | mở — tìm thấy khi làm FIX-058 (W11-KEYLAYOUT), cùng họ NO-077; id do người điều phối cấp. Chữa (đợt sau): dời tiền tố artifact của lượt chạy và tiền tố phiên bản mô hình xuống `packages/core/object_keys.py`, `storage` và `ml_contracts` cùng gọi · **🔧 FIX-061, FIX-062, FIX-063 · nhánh `fix/b0-04-layout-and-c11-debts`** (giao 2026-09-22, người điều phối) · **FIX-061** — `core/object_keys.py` nhận `run_prefix(upload_root, run, step)` (`{upload_root}runs/{run}/{step}/`, kiểm bước ∈ `PIPELINE_STEPS` — dời từ `storage` — và `check_id('run')`), `model_prefix(model)` và hằng `MODELS_PREFIX`. Test `test_run_prefix_layout` (6 bước), `test_model_prefix_layout`, `test_run_and_model_prefix_reject_wrong_parts` (7 ca), `test_run_and_model_prefix_read_id_rule_of_core_ids` (đột biến `ids.is_id`); đỏ 16 failed → xanh 16 passed · **FIX-062** — `run_artifact`/`model_artifact` dựng qua `run_prefix`/`model_prefix`, xoá `_STEP_IDS` riêng: `test_run_and_model_layout_comes_from_core` (`is` đúng hàm lõi + đột biến tên → khoá đổi theo); đỏ 2 failed → xanh 2 passed · **FIX-063** — `ml_contracts` kiểm `artifact_prefix` bằng `run_prefix`, `weights_key` bằng `model_prefix`, gốc `.onnx` bằng `MODELS_PREFIX` của lõi (xoá hằng riêng); `_id_of` = `AfterValidator(partial(check_id, prefix))` (Nit #5). Test `test_run_and_model_layout_comes_from_core` (3 tên) và `test_id_rules_come_from_core` (4 kiểu id, đột biến `ids.is_id`); đỏ 7 failed → xanh 7 passed |
| ✅ | NO-082 | 2026-09-22 | 2026-09-22 | Bộ kiểm `_assert_exact_fail_limit` của C11_parallel gọi một lỗi đếm thật là "cửa sổ thứ hai …, không phải đếm sai": `bump` đọc-rồi-ghi (không nguyên khối) → `AssertionError "bộ đếm mở cửa sổ thứ hai giữa loạt ((7, 360)), không phải đếm sai: Counter({401: 40})"` (test vẫn đỏ, chỉ sai thông báo) | Kiểm (2) suy "bộ đếm cuối < trần ⇒ cửa sổ mở lại" từ tiền đề INCR nguyên khối — chính tính chất test đang kiểm | B1-01 (`apps/api/auth/tests/test_refresh.py:506-515`) | P3 | mở — review merge 2026-09-22 `fix/b1-01-parallel-refresh-flake` finding #1. Chữa: chỉ báo cửa sổ thứ hai khi `counter[0] < trần` và `statuses.count(401) − trần == counter[0]`; thêm ca thuần (`[401]×40`, `[]`, `(7, 360)`, 20) không khớp "cửa sổ thứ hai"; hoặc bỏ vế "không phải đếm sai" · **🔧 FIX-064 · nhánh `fix/b0-04-layout-and-c11-debts`** (giao 2026-09-22, người điều phối) · **FIX-064** — kiểm (2) chỉ báo "cửa sổ thứ hai" khi `0 < counter[0] < trần` **và** `statuses.count(401) − trần == counter[0]` (thêm vế `0 <`: khoá vắng `(0, -2)` không phải cửa sổ thứ hai); bỏ vế "không phải đếm sai"; còn lại rơi xuống so số với thông báo "tầng thất bại đếm sai (…)". Test thuần `test_fail_limit_checker_names_the_cause` (M1 `[401]×40`/`(7, 360)`, khoá vắng + 503, 401 thừa lệch giá trị → "đếm sai"; 21/`(1, 360)`, 26/`(6, 360)` → "cửa sổ thứ hai") và `test_fail_limit_checker_passes_an_exact_split`; đỏ 4 failed → xanh 6 passed |
| ➖ | NO-083 | 2026-09-22 | 2026-09-24 | Ảnh `web` không dựng `public/models/` (mô hình `.glb` CC0): `scripts/fetch-models.mjs` của AppFront tải từ Poly Haven, build ảnh không tải mạng cho phần này — thiếu tệp thì FE thay bằng món thủ tục | `public/models/` bị `.gitignore` của AppFront (`.gitignore:90-92`), `git archive` @ `APPFRONT_SHA` không có tệp; B0-08 [6] cố ý không dựng | AppFront / B0-09 (CI dựng và ghim bộ mô hình) | P3 | mở — B0-08 (`deploy/docker/web.Dockerfile`). Chữa: dựng bộ `.glb` một lần ở CI, ghim SHA-256, chép vào `dist/models/` ở tầng 1 của `web.Dockerfile` · Chấp nhận, không sửa: `public/models/` bị `.gitignore` của AppFront loại nên `git archive` không có tệp; kéo Poly Haven trong `docker build` là tải mạng không ghim được, trái BE-00 §13.1. Đường nâng cấp: CI (B0-09) dựng bộ `.glb` một lần, ghim SHA-256, đẩy artifact/ảnh nền phụ rồi `COPY` vào `dist/models/` ở tầng 1 của `web.Dockerfile`. Review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md` §5. |
| ➖ | NO-084 | 2026-09-22 | 2026-09-24 | Dịch vụ `ml` chỉ nối mạng `ml-internal` (`internal: true`), nên **không** tới được S3 ngoài (AWS): v1 chỉ chạy `ml` với MinIO tự dựng (`prod.yml` profile `minio`); khoá `S3_ML_*` cho S3 ngoài do người vận hành cấp | BE-00 §9: `ml` nạp trọng số từ ngoài nên không có đường ra Internet; `YOLO_OFFLINE=1` không chặn tải khi có mạng | B0-08 (v2: proxy ra chỉ cho host S3) | P3 | mở — B0-08 (`deploy/compose/base.yml` dịch vụ `ml`, `ml-gpu`). Chữa v2: proxy egress chỉ cho phép host của `S3_ENDPOINT`, nối `ml` vào mạng của proxy · Chấp nhận, không sửa: `ml` chỉ nối `ml-internal` là cố ý (BE-00 §9 — tiến trình nạp trọng số ngoài không có đường ra Internet); proxy egress cho `S3_ENDPOINT` là tính năng v2, không phải đóng nợ. Đường nâng cấp: proxy chỉ cho phép host của `S3_ENDPOINT`, nối `ml` vào mạng của proxy. Review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md` §5. |
| ✅ | NO-085 | 2026-09-22 | 2026-09-24 | Container `ml` buộc phải nhận `SECRET_KEY`, `PUBLIC_BASE_URL` (vì `apps/ml/celery_main.py` gọi `get_core_settings()` chỉ để đọc `APP_ENV`) và `REDIS_CACHE_URL` (vì `MessagingSettings` bắt buộc, dù `ml` không tới được `redis-cache`) — tiến trình nạp trọng số ngoài cầm khoá gốc ký JWT, trái mục đích tách quyền của BE-00 §2.1/§9 | `CoreSettings` và `MessagingSettings` gộp mọi trường bắt buộc vào một lớp; `ml` không có lớp cấu hình tối thiểu | B5-01 (`apps/ml/celery_main.py`), B0-05 (`packages/messaging/settings.py`) | P2 | mở — phát hiện khi dựng compose B0-08 (`deploy/compose/base.yml` truyền đủ để `ml` lên được). Chữa: `check_backend` đọc `APP_ENV` qua một lớp cấu hình riêng không có `SECRET_KEY`; `redis_cache_url` tuỳ chọn cho tiến trình không dùng cache; rồi bỏ ba biến khỏi `ml`/`ml-gpu` trong compose · phần mã xong FIX-089 (`19648c3`)/FIX-091 (`f7abc9f`); còn gỡ 3 biến SECRET_KEY, PUBLIC_BASE_URL, REDIS_CACHE_URL khỏi ml/ml-gpu trong compose (nhánh deploy) · **đóng** — phần compose: FIX-086 `0e1e709` gỡ `SECRET_KEY`, `PUBLIC_BASE_URL`, `REDIS_CACHE_URL` khỏi `ml`/`ml-gpu` (dev/ci/prod), `b77af7c` prod `ml` dùng tệp riêng `/etc/appback/ml.env` thay `appback.env` (người dùng duyệt; runbook FIX-087 `f672677`); phần mã còn thiếu phát hiện khi làm compose: FIX-105 `2739918` (`StorageSettings` không đọc `CoreSettings`, luật khác origin BE-00 §8 chuyển vào `create_storage` khi có `CoreSettings`), FIX-091 `c2f3c72` (`tasks_util` dựng kho không cần `CoreSettings`). Test `apps/ml/runtime/tests/test_tasks_util.py::test_infer_context_builds_without_the_api_secrets` (tiến trình mới, môi trường `ml` prod) đỏ `ValidationError secret_key` → xanh; reviewer thấy test bắt riêng từng nửa. Review lượt 2 APPROVE 4,75/5 `docs/reviews/2026-09-24-fix-debt-01-deploy-round-2.md`, cổng đầy đủ thoát 0 (3840 passed, 99,44 % / 98,34 %) |
| ✅ | NO-086 | 2026-09-22 | 2026-09-24 | FE @ `APPFRONT_SHA` (`9cf0b0b`) mở SSE ở `/api/notifications/stream`, trong khi BE-BIND S2 là `GET /api/streams/notifications` — nginx (B0-08) chỉ tắt đệm cho `/api/streams/`, nên đường FE hiện tại sẽ bị đệm nếu BE có mount | Hợp đồng FE viết trước BE-BIND | FE (prompt F-* sở hữu `src/api` luồng thông báo) / B4-01 | P3 | mở — quan sát khi khảo sát B0-08 (`ghi-chu-fe.md` mục 7). H5 (B4-01) sẽ bắt khi S1/S2 được mount. Chữa: FE đổi sang `/api/streams/notifications` theo BE-BIND · **FIX-099** (F-01b, người dùng chốt chủ): `src/api/endpoints.ts` luồng thông báo → `/api/streams/notifications` (BE-BIND S2); cookie `appback_stream` `Path=/api/streams` xác nhận đường cũ không bao giờ mang được cookie. Test `src/api/__tests__/notifications.test.ts` đỏ trên `master` → xanh. AppFront nhánh `fix/debt-01-fe` (`885556d`, cherry-pick của `b60c7be`), review lượt 1 ĐẠT phần này `docs/reviews/2026-09-24-fix-debt-01-fe.md`, lượt 2 giữ nguyên `…-fe-round-2.md` |
| ✅ | NO-087 | 2026-09-22 | 2026-09-22 | Worker Celery thử vẫn để dấu cấp tiến trình sau khi dừng, kể cả sau FIX-059: `os.environ` giữ `CELERY_LOG_LEVEL`, `CELERY_LOG_FILE`, `_MP_FORK_LOGLEVEL_`, `_MP_FORK_LOGFILE_`, `_MP_FORK_LOGFORMAT_`; `logging.captureWarnings(True)`; `warnings.filterwarnings('always', CDeprecationWarning/CPendingDeprecationWarning)` | `start_worker` → `setup_app_for_worker` → `app.log.setup` (Celery 5.6.3): các dòng này chạy **ngoài** nhánh `if not receivers`, nên receiver `setup_logging` (FIX-059) không chặn được. Ảnh hưởng đọc được ~0: pytest bọc `warnings` quanh mỗi test (`catch_warnings` trả lại `showwarning`, bộ lọc), worker con (`test_worker_lost`) tự đặt lại các biến môi trường trong `app.log.setup` của chính nó trước khi fork | B0-05 (`packages/testing/fixtures/messaging.py`) | Nit | mở — phát hiện trong FIX-059 (W10-GATELOG, 2026-09-22; id do người điều phối cấp). Chữa: fixture lưu và trả `os.environ` các khoá `CELERY_LOG_*`/`_MP_FORK_*` (vd `monkeypatch`) cùng trạng thái `captureWarnings` khi worker dừng, kèm test trước = sau; hoặc `➖` nếu chứng minh vô hại · **🔧 FIX-067 · nhánh `fix/b0-01-gate-log-followups`** (giao 2026-09-22, người điều phối) · **FIX-067** — `celery_worker_factory` bọc `start_worker` trong `_restoring_process_logging()`: lưu `CELERY_PROCESS_ENV` (5 khoá, probe trong container xác nhận đúng 5 khoá Celery đổi) rồi trả lại khi worker dừng; `warnings.catch_warnings()` trả bộ lọc và `showwarning`; `logging.captureWarnings(False)` chỉ khi chính worker bật capture (`showwarning` đổi), capture bật từ trước để nguyên. Test `packages/messaging/tests/test_tasks.py::test_the_worker_factory_leaves_no_process_logging_state[clean,preset]` (cả `os.environ`, `showwarning`, bộ lọc trước = sau; trong lúc chạy phải khác): đỏ trên fixture cũ (2 failed) → xanh (2 passed; `packages/messaging packages/testing` 230 passed) |
| ✅ | NO-088 | 2026-09-22 | 2026-09-22 | `packages/core/object_keys.py:74` `upload_prefix_of` (hàm công khai, docstring "khoá không tin") chỉ kiểm phần đầu: khoá có đuôi `../x` hay `//` vẫn ra tiền tố, không lỗi (probe review: `…/uploads/{upl}/../../../../x` → tiền tố) | Tách từ `_upload_prefix` riêng của `ml_contracts` (người gọi đã qua `ObjectKey`) thành API lõi mà không thêm điều kiện trước | B0-02 (`packages/core/object_keys.py`) | P3 | mở — review merge 2026-09-22 `fix/b0-04-key-layout-debts` finding #1. Chữa: `check_key(key)` ở dòng đầu + ca `…/../x` trong `test_upload_prefix_of_rejects`; hoặc docstring ghi rõ "`key` phải đã qua `check_key`" · **🔧 FIX-060 · nhánh `fix/b0-04-layout-and-c11-debts`** (giao 2026-09-22, người điều phối) · **FIX-060** — `upload_prefix_of` gọi `check_key(key)` ở dòng đầu (fail-closed), khoá hợp lệ không đổi hành vi; tiền tố trần (`/` cuối) không phải khoá nên nay bị từ chối (ca đó dời từ `test_upload_prefix_of_accepts` sang `_rejects`), khoá rỗng báo "khoá rỗng". `test_upload_prefix_of_rejects` thêm `…/../x`, `…/../../../../x`, `…/pages//0.png`, `…/pages/./0.png`, ký tự lạ, đuôi `.meta.json`; đỏ 8 failed → xanh 19 passed (`-k upload_prefix_of`) |
| ✅ | NO-089 | 2026-09-22 | 2026-09-22 | Lượt verify kết thúc bằng ngoại lệ (bước ném lỗi lạ, vd `FileNotFoundError` khi thiếu công cụ trong venv) thì file log host của FIX-053 dừng ở dòng output cuối: không traceback, không dòng "mã thoát" — đúng lúc client compose đã chết người đọc không biết vì sao | `_tee_output` trả fd 1/2 trong `finally` rồi ngoại lệ mới nổi lên; interpreter in traceback ra stderr gốc, ngoài log | B0-01 (`tools/verify/steps.py:295-297`) | P3 | mở — review merge 2026-09-22 `fix/b0-01-gate-log-debts` finding #1 (probe P3: bước ném `RuntimeError` → log chỉ có "trước khi nổ"). Chữa: trong khối `with`, `except Exception: traceback.print_exc(); rc = 1`, vẫn in "mã thoát"; test log có `Traceback` và "mã thoát: 1" · **🔧 FIX-065 · nhánh `fix/b0-01-gate-log-followups`** (giao 2026-09-22, người điều phối) · **FIX-065** — `steps.cmd_verify`: trong khối `with _tee_output`, `except Exception` → `traceback.print_exc()` khi fd 1/2 còn nối log (traceback vào cả log lẫn stderr), `rc = 1`, vẫn in `mã thoát: 1`; không nuốt lỗi. Test `tools/tests/test_steps_commands.py::test_verify_bước_ném_lỗi_lạ_vẫn_có_traceback_và_mã_thoát_trong_log`: đỏ trên mã cũ (1 failed — `RuntimeError` thoát khỏi `main`) → xanh (`tools/tests/test_steps*.py` 74 passed) |
| ✅ | NO-090 | 2026-09-22 | 2026-09-22 | Log cổng `.cache/src-out/verify/*.log` (FIX-053) không bao giờ được dọn: mỗi lượt `run.sh verify` thêm một file (≈ 21 KB lượt đạt), `gc` không đụng tới, `in_container.sh` chép cả `.cache` vào `/tmp/w` mỗi lượt; checkout sống lâu (`F:/AppBack`) dồn mãi | FIX-053 chọn một file mỗi lượt để giữ lượt trước, chưa đặt trần | B0-01 (`tools/verify/run.sh:93-97`) | P3 | mở — review merge 2026-09-22 `fix/b0-01-gate-log-debts` finding #2; tác giả đã nêu ở "Lệch khỏi prompt" (4). Chữa: `run.sh verify` giữ 20 file mới nhất (`ls -1t "$log_dir"/*.log \| tail -n +21 \| xargs -r rm --`) trước khi tạo file mới · **🔧 FIX-066 · nhánh `fix/b0-01-gate-log-followups`** (giao 2026-09-22, người điều phối) · **FIX-066** — `run.sh`: hằng `VERIFY_LOG_KEEP=20` (số log tối đa sau một lượt, tính cả log của lượt đó); `run.sh verify` xoá mọi `*.log` trừ 19 file mới nhất theo mtime (`find -printf '%T@' \| sort -rn \| tail \| xargs -r -d '\n' rm --`) trước khi đặt tên log mới, file khác trong thư mục để nguyên; `in_container.sh` không đổi (bản chép `.cache` đã có trần). Test `tools/tests/test_run_sh.py::test_verify_giữ_log_mới_nhất_dưới_trần` (chạy `run.sh` thật trong repo git tạm, `docker` giả; 0, 19, 20, 23 log sẵn): đỏ trên `run.sh` cũ (2 failed [20], [23]) → xanh (4 passed) |
| ➖ | NO-091 | 2026-09-22 | 2026-09-22 | Nhánh `feature/b0-08-docker-compose` vượt trần 400 dòng logic của MNT-05 (+3 081 dòng: ≈ 1 319 dòng Python bộ đọc + test tĩnh, ≈ 1 700 dòng Dockerfile/compose/nginx) | B0-08 là **một** prompt: test tĩnh đọc chung bốn phần (ảnh, compose, nginx, MinIO); đã dựng bằng 4 nhánh con rồi mới gộp | B0-08 | P2 | Chấp nhận như NO-039/NO-047/NO-056: tách là đưa vào `main` nửa bộ ảnh không có compose/nginx chạy được. Review merge 2026-09-22 finding #6 (`docs/reviews/2026-09-22-feature-b0-08-docker-compose.md`) |
| ✅ | NO-092 | 2026-09-22 | 2026-09-22 | `run.sh verify` thoát trước khi chạy cổng nếu bước dọn log cũ (FIX-066) hỏng: `find`/`rm` dưới `set -euo pipefail`, `rm` gặp log cũ đang bị giữ (Windows "Device or resource busy") → rc 123, docker không được gọi | Việc phụ dọn dẹp nằm trong đường chính của nhánh `verify`, không có `\|\|` cảnh báo | B0-01 (`tools/verify/run.sh:101-102`) | P3 | mở — review merge 2026-09-22 `fix/b0-01-gate-log-followups` finding #1 (probe P2x: `rm` giả hỏng → rc 123; .NET `File.Open ReadWrite` giữ file → `rm` Git Bash "Device or resource busy"). Chữa: `{ find … \| xargs -r -d '\n' rm --; } \|\| echo "dọn log cổng cũ hỏng — bỏ qua" >&2`; test `rm` giả hỏng → docker vẫn được gọi, rc 0 · **🔧 FIX-068 · nhánh `fix/b0-01-gate-log-guard`** (giao 2026-09-22, người điều phối) · **FIX-068** — `run.sh`: đường ống dọn log cũ bọc trong `{ …; } \|\| echo "dọn log cổng cũ hỏng — bỏ qua, cổng vẫn chạy" >&2` — `find`/`rm` hỏng chỉ cảnh báo (stderr của `rm` vẫn in), `docker` vẫn được gọi, mã thoát là của cổng (probe: `docker` giả thoát 3 → `run.sh` thoát 3); hành vi khác của `run.sh` không đổi. Test `tools/tests/test_run_sh.py::test_verify_vẫn_chạy_cổng_khi_dọn_log_cũ_hỏng` (20 log sẵn, `rm` giả trên `PATH` in "Device or resource busy" rồi thoát 1): đỏ trên `run.sh` cũ (1 failed; `run.sh` thoát 123) → xanh (5 passed; `tools/tests/` 259 passed) |
| ✅ | NO-093 | 2026-09-22 | 2026-09-22 | Test FIX-067 `test_the_worker_factory_leaves_no_process_logging_state` không chốt phần `captureWarnings` của `_restoring_process_logging`: bỏ `captureWarnings(False)` hay bỏ điều kiện "showwarning đã đổi" test vẫn xanh (`catch_warnings` đã trả `showwarning`; trạng thái logging tự nhớ không được kiểm) | Test so `showwarning`/bộ lọc — cả hai do `catch_warnings` trả — thay vì hành vi của `captureWarnings` sau worker | B0-05 (`packages/messaging/tests/test_tasks.py:539-561`) | P3 | mở — review merge 2026-09-22 `fix/b0-01-gate-log-followups` finding #2 (probe P3m: đột biến M1, M2 sống, 2 passed). Chữa: sau worker, ca clean — `captureWarnings(True)` phải đổi `warnings.showwarning`; ca preset — `captureWarnings(False)` phải trả hàm gốc lưu trước khi bật · **🔧 FIX-069 · nhánh `fix/b0-01-gate-log-guard`** (giao 2026-09-22, người điều phối) · **FIX-069** — `test_the_worker_factory_leaves_no_process_logging_state` kiểm thêm bằng hành vi, chỉ qua API công khai: lưu `warnings.showwarning` gốc (sau `captureWarnings(False)`) trước khi đặt capture đầu; sau worker đảo capture (`captureWarnings(not preset)`) — ca clean `showwarning` phải khác hàm gốc, ca preset phải đúng hàm gốc. Đột biến fixture chỉ trong `.cache/`: M1 (bỏ `captureWarnings(False)`) → test cũ 2 passed, test mới 1 failed (`clean`); M2 (bỏ điều kiện) → test cũ 2 passed, test mới 1 failed (`preset`); fixture hiện tại → 2 passed (`test_tasks.py` 47 passed) |
| ✅ | NO-094 | 2026-09-22 | 2026-09-24 | Server chuyển hướng `listen 8080` của nginx prod (`return 301 https://…`) dùng `access_log` mặc định của ảnh nền: client gọi `http://…/api/files/<token>` hoặc URL ký S3 bằng `http://` để lại token/chữ ký trong `docker logs web` | Server 301 không qua map `$appback_access_log`, không `access_log off` như vhost MinIO | B0-08 (`deploy/nginx/templates/prod/app.conf.template`) | P3 | mở — review merge 2026-09-22 lượt 3 finding #21 (`docs/reviews/2026-09-22-feature-b0-08-docker-compose-round-3.md`). Chữa: `access_log off;` trong server 8080 của template prod + test tĩnh · FIX-086 (`47cdab2`): `access_log off;` trong server `listen 8080` (chỉ `return 301`) của template prod. Probe thật 7 dạng URI méo qua cổng 8080 → 301, `grep -c <token>` trong `docker logs` = 0 (dev, prod), đối chứng `/api/projects/1` vẫn vào log. Review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md`. |
| ✅ | NO-095 | 2026-09-22 | 2026-09-24 | Map `$appback_access_log` đoán dạng URI thô bằng regex: `/api/./files/…`, `/api/x/../files/…`, `/%61pi/files/…`, `/api%2Ffiles/…` vẫn vào `location /api/files/` (nginx chuẩn hoá `$uri`) nhưng access log ghi token | Quyết định tắt log trên `$request_uri` thô thay vì ở mức location đã chuẩn hoá (vá triệu chứng, R-19) | B0-08 (`deploy/nginx/templates/{dev,prod}/app.conf.template`, `deploy/nginx/snippets/app_locations.conf`) | P3 | mở — review lượt 3 finding #22. Chữa: `access_log off;` trong `location /api/files/` + `error_page … /__errors/503-files` với location lỗi riêng `access_log off` (tách thân 503 thành snippet), bỏ map; probe 7 dạng méo `grep -c` = 0 · FIX-086 (`47cdab2`): tắt log chuyển từ `map $request_uri` sang `location /api/files/` trên `$uri` chuẩn hoá, kèm `error_page` `-files` riêng cũng `access_log off`. Probe 7 dạng URI méo (dev+prod) đều tới location này, `grep -c <token>` = 0 cả hai bản — tác giả và reviewer đo độc lập cùng kết quả. Review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md`. |
| ✅ | NO-096 | 2026-09-22 | 2026-09-24 | Đổi access key ID (`S3_ACCESS_KEY`, `S3_ML_ACCESS_KEY`) không thu hồi user MinIO cũ: `minio-init` chỉ thêm user cho khoá hiện tại, user cũ vẫn giữ `app-policy`/`ml-policy` và secret cũ vẫn dùng được | `deploy/minio/init.sh` không gỡ danh tính cũ khi xoay khoá | B0-08 (`deploy/minio/init.sh`, `deploy/compose/env.example`) | P3 | mở — review lượt 3 finding #23. Chữa: `init.sh` gỡ mọi user đang giữ `app-policy`/`ml-policy` mà không phải khoá hiện tại (`mc admin policy entities`), hoặc tối thiểu comment trong `env.example` hướng dẫn `mc admin user remove` · FIX-086 (`47cdab2`): `deploy/minio/init.sh` thêm `revoke_stale_users()` gỡ mọi danh tính còn giữ `app-policy`/`ml-policy` không phải khoá hiện tại. Đo thật ba lượt trên MinIO: khoá cũ bị gỡ và hết đọc được bucket, khoá mới dùng được, lượt lặp lại idempotent. Review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md`. |
| ✅ | NO-097 | 2026-09-22 | 2026-09-24 | `Role`/`ROLES` khai hai nơi: `apps/api/core/auth.py:19-20` và `packages/domain/permissions/matrix.py` (`Role`, `PermissionKey`, `ROLES`, gương H3 của B1-02) | `packages/domain` không được nhập `apps` (`.importlinter` `domain-vision-isolated`) và B1-02 không được sửa `apps/api/core` (khối [12]), nên gương quyền khai lại kiểu vai | B0-06 (`apps/api/core/auth.py:19-20`) | Nit | mở — người điều phối B1-02. Chữa: `apps/api/core/auth.py` nhập `Role`, `ROLES` từ `packages.domain.permissions` (cạnh `apps/api → domain` hợp lệ, BE-00 §13.1), bỏ khai riêng · Đã sửa — FIX-090 (`301bf67`) + FIX-103 (`e2ebeda`, B1-01, đã xin phép trước). apps/api/core/auth.py nhập Role, ROLES từ packages.domain.permissions (bỏ khai riêng, __all__ để mypy --strict cho phép re-export); cast thừa hệ quả ở apps/api/auth/sessions.py:585 dọn trong FIX-103. Test test_core_auth_reuses_the_role_mirror_of_the_domain đỏ → xanh; mypy --strict đỏ (redundant-cast) trước FIX-103 → xanh. Review lượt 2 APPROVE docs/reviews/2026-09-24-fix-debt-01-core-round-2.md (4,77/5). Gương thứ ba ở packages/db/models/auth.py:29 + hai _role gần giống: NO-188 (đã mở). |
| ➖ | NO-098 | 2026-09-22 | 2026-09-22 | `require_role(...)` với tập vai khác `{"admin"}` mang khoá `role:<vai,…>`; gắn vào route có dòng BE-BIND thì chỉ `apps/api/core/tests/test_routes.py:73-80` bắt (AssertionError ở bước 5), không ném `ValueError` lúc khai như B1-02 [2] mô tả | Khung route của B0-06 không có móc so `permission_key` với BE-BIND lúc dựng app; đọc BE-BIND lúc chạy không hợp lệ (tài liệu không nằm trong ảnh) | B0-06 (`apps/api/core/openapi.py`, `routing.py`) | P3 | Chấp nhận: bước 5 của cổng chặn trước mọi merge nên khoá lệch không tới được `main`; `test_role_key_on_bind_route_fails_the_route_scan` (B1-02) chốt hành vi. Nâng cấp nếu muốn hỏng sớm: `create_app` so `permission_key` với một bảng khoá sinh sẵn từ BE-BIND lúc build |
| ✅ | NO-099 | 2026-09-22 | 2026-09-24 | FE `UserManagement` chỉ có nhãn cho `kind` `floor.upload` (`src/screens/admin/UserManagement/useUserManagement.ts:174-186`); 26 `kind` còn lại của `ActivityKind` (`apps/api/access/kinds.py`) hiện câu dự phòng | FE viết nhãn trước khi BE chốt bảng mã `kind` (B1-02 [2]) | AppFront (`src/screens/admin/UserManagement/useUserManagement.ts`) | P3 | mở — người điều phối B1-02 (prompt cấm sửa FE ở đây). Chữa: thêm nhãn tiếng Việt cho đủ 27 `kind` theo `apps/api/access/kinds.py` · **FIX-098** (F-02): `useUserManagement.ts` nhãn đủ 27 `kind` của `apps/api/access/kinds.py` (reviewer tự đếm máy: 27/27, không thiếu/thừa/trùng). Test mới đỏ trên `master` → xanh. AppFront `fix/debt-01-fe` (`831be4b`, cherry-pick của `70f614e`), review lượt 1 ĐẠT |
| ✅ | NO-100 | 2026-09-22 | 2026-09-22 | `record_activity` nhận `project_id` tuỳ ý (`"rác"`, `""`, `"prj_123"`) và ghi thẳng vào `activity_log.project_id`, trong khi W4 chốt `prj_`+ULID | Spec khối [6] của B1-02 không liệt kê kiểm `project_id`; các việc W1–E làm đúng spec, không đối chiếu W4 (BE-00 §3) | B1-02 (`apps/api/access/activity.py`) | P3 | mở — người điều phối B1-02 giao sub-agent (R-35), nhánh `feature/b1-02-project-id-check` · **đóng** — B1-02 `02450eb` (squash; trên nhánh là `abfc0ad`): `activity._check_project_id`: `None` hoặc `is_id("prj", …)`, sai → `ValueError` trước `db.add`; test `test_record_activity_rejects_bad_project_id` (rác, rỗng, `prj_123`, tiền tố `usr_`): đỏ trước sửa (4 failed / 19 passed) → xanh (23 passed); verify 8/8 đạt (2210 passed) |
| ✅ | NO-101 | 2026-09-22 | 2026-09-24 | `bash tools/verify/run.sh gc` luôn thoát 1 (`tr: range-endpoints of '_-\012' are in reverse collating sequence order`) trước khi gọi docker: venv/mypy cache của worktree đã xoá không bao giờ được dọn | `tr -c 'a-z0-9_-\n' '-'` ở `tools/verify/run.sh:150`: `_-\n` bị hiểu là khoảng ký tự `_`→`\n` (ngược thứ tự); dòng 29 cùng mẫu nhưng `-` đứng cuối nên không lỗi | B0-01 (`tools/verify/run.sh:150`) | P3 | mở — người điều phối B1-02 gặp khi dọn 7 worktree sau merge `02450eb`. Hỏng an toàn: `set -e` dừng trước `docker compose`, chưa xoá gì. Chữa: `tr -c 'a-z0-9_\n-' '-'` (đưa `-` về cuối; đã thử tay: `printf 'b1-02-x\n' \| tr -c 'a-z0-9_\n-' '-'` → `b1-02-x`) + test `tools/tests/test_run_sh.py` cho `gc` với tên worktree có `-`, `_`, chữ hoa · FIX-083 (sha 531c690): tools/verify/run.sh:150 đổi tr -c cho ký tự '-' đứng cuối tập (tránh bị đọc thành dải ngược 'range-endpoints ... reverse collating sequence order'). Test test_gc_lowercases_and_normalizes_worktree_names đỏ trên main (run.sh gc thoát 1) → xanh trên nhánh. Review APPROVE docs/reviews/2026-09-24-fix-debt-01-tooling.md. |
| ✅ | NO-102 | 2026-09-22 | 2026-09-24 | `trivy image web` (job `build` của CI, B0-09) có 2 lỗ **CRITICAL có bản sửa**: `CVE-2026-31789` (OpenSSL heap buffer overflow), `libssl3`/`libcrypto3` `3.3.5-r0` → sửa ở `3.3.7-r0` | Ảnh nền `nginxinc/nginx-unprivileged:1.28.0-alpine@sha256:c97ff0bf…` ghim digest từ trước khi Alpine phát hành bản vá OpenSSL | B0-08 (`deploy/docker/web.Dockerfile:15`) | P1 | mở — B0-09 phát hiện (review `docs/reviews/2026-09-22-chore-b0-09-ci-codeql-dependabot.md`); job `build` của CI đỏ tới khi sửa. Chữa: nâng digest ảnh nền lên bản có `libssl3 ≥ 3.3.7-r0` (hoặc `apk upgrade --no-cache libssl3 libcrypto3` trong tầng cuối) rồi `trivy image` lại · **waiver của người dùng 2026-09-22**: gộp B0-09 (`55f75bc`) trước FIX-070; FIX-070 (`fix/b0-08-web-openssl-cve`) đang sửa, review lượt 1 REQUEST CHANGES (`docs/reviews/2026-09-22-fix-b0-08-web-openssl-cve.md`) · **FIX-070** (DEBT-01 T2, nhánh `fix/b0-08-web-openssl-cve` `b232603`): nền `web` → `nginxinc/nginx-unprivileged:1.30.5-alpine@sha256:4714e0b1…` (dòng stable, digest tự tra `imagetools` 2026-09-24; digest `04a3275f…` của lượt 1 đã bị tag ghi đè). Ảnh build thật: Alpine 3.24.2, `libssl3`/`libcrypto3` 3.5.8-r0; `trivy` 0.74.0 ghim `--severity CRITICAL --ignore-unfixed --exit-code 1` mã thoát 0, mọi mức 0 lỗ hổng. Review lượt 2 APPROVE `docs/reviews/2026-09-24-fix-b0-08-web-openssl-cve-round-2.md` |
| ✅ | NO-103 | 2026-09-22 | 2026-09-24 | `tools/tests/test_services.py::test_ephemeral_dừng_xong_địa_chỉ_không_về_tay_bản_dựng_sau` (3 tham số redis/postgres/minio) dựng Testcontainers thật nhưng `--ci-split` xếp vào nhóm `unit` | Bộ phân loại (`packages/testing/fixtures/ci_split.py`, B0-09) suy theo `item.fixturenames`; test nhận factory qua `parametrize` nên không có tên fixture dịch vụ nào | B0-01 (`tools/tests/test_services.py`) | P2 | mở — không sai kết quả (runner CI có Docker), chỉ sai nhóm. Chữa: gắn `@pytest.mark.ci_integration` (luật marker tường minh của `ci_split.py`) · FIX-083 (sha 531c690): gắn @pytest.mark.ci_integration cho test_ephemeral_dừng_xong_địa_chỉ_không_về_tay_bản_dựng_sau (_explicit_group_marker ưu tiên marker tường minh). Không sai kết quả, chỉ sai nhóm phân loại --ci-split; test nay xếp đúng ci_integration. Review APPROVE docs/reviews/2026-09-24-fix-debt-01-tooling.md. |
| ✅ | NO-104 | 2026-09-22 | 2026-09-24 | Build ảnh `ml` hỏng chập chờn ở bước tải mô hình ghim từ Hugging Face: lượt 1 `TimeoutError`, lượt 3 `SHA-256 lệch bản ghim` (tệp tải dở) — B0-09 `job.sh build` trên máy | `python -m packages.ml_contracts.pinned fetch` tải một lần, không thử lại, không tải tiếp; `job.sh build` chạy `docker builder prune -f` sau mỗi ảnh (prompt B0-09 [6]) nên trên máy mất cache lớp mô hình (CI có cache `type=gha`) | B5-01 (`packages/ml_contracts/pinned.py`), B0-08 (`deploy/docker/ml.Dockerfile:60`) | P2 | mở. Chữa: thử lại có backoff và trần lần, tải vào tệp tạm, kiểm SHA rồi mới `os.replace` · Đã sửa — FIX-091 (`f7abc9f`). packages/ml_contracts/pinned.py: _fetch_file thử lại tới FETCH_ATTEMPTS=3 với backoff nhân đôi (1,0s → trần 8,0s) cho cả lỗi mạng lẫn SHA lệch do tệp tải dở; .part + os.replace giữ nguyên (nguyên tử), lượt cuối chạy ngoài vòng lặp. 4 test (bộ mở giả FlakyOpener, không tải mạng) đỏ (ImportError FETCH_ATTEMPTS) trên main → xanh. Review lượt 2 APPROVE docs/reviews/2026-09-24-fix-debt-01-core-round-2.md (4,77/5). phần mã xong FIX-091; ml.Dockerfile không cần đổi (T2). |
| ✅ | NO-105 | 2026-09-22 | 2026-09-24 | CI chạy bước 8 (OpenAPI) ở **chế độ xuất**, không so với bản tham chiếu (`tools/ci/job.sh` job `typecheck`, chú thích ở `ci.yml`) | Hiến chương tự mâu thuẫn trên CI: BE-00 §12 bước 8 nhánh tích hợp so với `openapi.json` đã commit, nhưng §13.2 cấm commit `openapi.json` (có trong `.gitignore`) → chế độ so luôn đỏ trên `main` | người điều phối (BE-00 §12, §13.2) | P2 | mở — người dùng đã duyệt phương án xuất cho B0-09 (2026-09-22). Chữa: chốt một bản OpenAPI tham chiếu được commit (vd `docs/contracts/openapi.json`), cho `steps.py` so với nó, bật lại chế độ so ở CI · FIX-101 (sha ad9ecab) + FIX-084 (sha 844fea3): bước 8 (tools/verify/steps.py) và job typecheck (tools/ci/job.sh, ép VERIFY_BRANCH=integration) so với docs/contracts/openapi.json thay vì openapi.json bị .gitignore. Test test_bước_8_integration_so_với_bản_commit + test_job_sh_typecheck_compares_committed_openapi_reference đỏ trên main → xanh; reviewer chạy lại chế độ so sánh thật, mã thoát 0. Người dùng duyệt 2026-09-24; bản tham chiếu docs/contracts/openapi.json (0a24717). Review APPROVE docs/reviews/2026-09-24-fix-debt-01-tooling.md. |
| ✅ | NO-106 | 2026-09-22 | 2026-09-24 | Ảnh ghim của H2 (`tools/ci/h2.py:73-75`: Postgres, Redis, MinIO) chép tay từ `packages/testing/fixtures/services.py:29-32` | R-28 cấm mã không phải test (`h2.py`) nhập `packages.testing`, mà hằng ảnh chỉ nằm trong gói đó | B0-01 (`packages/testing/fixtures/services.py`), B0-09 (`tools/ci/h2.py`) | P3 | mở — giảm nhẹ bằng test chống lệch trong `tools/ci/tests/test_h2.py` (vòng sửa review lượt 1). Chữa gốc: dời hằng ảnh ra module không phải test (vd `packages/core`), cả hai nơi nhập · FIX-083 (sha 531c690) + FIX-084 (sha 844fea3): hằng ảnh ghim dời sang tools/pinned_images.py (mới), services.py và h2.py cùng nhập một nguồn — hết nguy cơ lệch khi nâng ảnh. Test tools/tests/test_pinned_images.py (100% dòng+nhánh) đỏ trên main (ImportError) → xanh trên nhánh; test_h2.py cũ vẫn xanh nhưng nay chỉ còn bảo đảm cấu trúc (xem nợ mới NO-179). Review APPROVE docs/reviews/2026-09-24-fix-debt-01-tooling.md. |
| ⬜ | NO-107 | 2026-09-22 | | Chưa có seed thật (`packages/db/seeds` chỉ khung) nên id admin mà H2 gửi (`fake:usr_…:…:admin`) không có trong DB | B0-03 chỉ dựng khung seed; chưa prompt nào thêm dữ liệu seed | B2-06 (prompt đầu tiên thêm dữ liệu seed thật; bàn giao DEBT-01) | P3 | mở — hiện vô hại (`FakeTokenVerifier` không tra DB). Khi có seed và route bảo vệ đọc principal từ DB, H2 phải dùng id seed thật · **bàn giao** 2026-09-24: đã chép vào `prompts/B2-06.md` (khối [6], dòng 159) — khi route bảo vệ đọc principal từ DB, H2 dùng id admin seed thật |
| ✅ | NO-108 | 2026-09-22 | 2026-09-24 | Mỗi worktree có một volume riêng `appback-verify-<tên>_appback-work` (~2 GB, `uv-cache` riêng), thay vì **một** volume chung `appback-work` như ENV §2 mô tả; hardlink `UV_LINK_MODE=hardlink` mất tác dụng | `deploy/compose/verify.yml:40-41` khai `volumes: appback-work:` không có `name:` nên compose gắn tiền tố project `appback-verify-<VERIFY_NAME>` | B0-01 (`deploy/compose/verify.yml`) | P2 | mở — người điều phối B0-09 dọn 21 volume mồ côi (~43 GB) ngày 2026-09-22. Chữa: `name: appback-work`; kiểm hai lượt verify song song cùng làm ấm `/work/contract-node/<hash>` không giẫm nhau · FIX-083 (sha 531c690): deploy/compose/verify.yml ghim image: appback-verify:local + volumes.appback-work.name: appback-work — một ảnh + một volume dùng chung mọi worktree. Test tools/tests/test_verify_yml.py đỏ trên main → xanh; reviewer đo thật hai lượt verify song song trên volume chung, venv/mypy tách theo VERIFY_NAME, không giẫm nhau. Review APPROVE docs/reviews/2026-09-24-fix-debt-01-tooling.md. |
| ➖ | NO-109 | 2026-09-22 | 2026-09-22 | Nhánh `chore/b0-09-ci-codeql-dependabot` vượt trần 400 dòng logic của MNT-05 (~1 800 dòng không phải test: 3 workflow, `job.sh`, 6 module `tools/ci`, plugin `ci_split`) | B0-09 là **một** prompt; đã dựng bằng 5 nhánh con theo `so_huu`, workflow và `job.sh` gọi chéo mọi module | B0-09 | P2 | Chấp nhận, như NO-091: tách thêm không giảm rủi ro review vì các phần phải kiểm cùng nhau |
| ✅ | NO-110 | 2026-09-22 | 2026-09-24 | `ci.yml` khai `pull_request` không có `types`, nên sửa tiêu đề PR (sự kiện `edited`) không chạy lại job `commits`; bấm re-run vẫn dùng payload cũ. Cách vòng qua NO của finding #4 (rút tiêu đề PR Dependabot hệ `docker` về ≤ 72 ký tự) vì vậy không làm check xanh | Trigger mặc định của `pull_request` là `opened`, `synchronize`, `reopened` | B0-09 (`.github/workflows/ci.yml:9-10`, `tools/ci/README.md:70-74`) | P3 | mở — review lượt 2 (`docs/reviews/2026-09-22-chore-b0-09-ci-codeql-dependabot-round-2.md`, N1, mức tin PLAUSIBLE). Chữa: `types: [opened, synchronize, reopened, edited]` (hoặc chạy `commits` bằng workflow riêng nghe `edited`) + test quét YAML · FIX-084 (sha 844fea3): ci.yml pull_request.types thêm edited — sửa tiêu đề/thân PR tự chạy lại job commits (đọc PR_TITLE từ payload mới, không phải no-op). Test test_ci_yml_pull_request_types_include_edited đỏ trên main (KeyError: 'types') → xanh. Review APPROVE docs/reviews/2026-09-24-fix-debt-01-tooling.md. |
| ✅ | NO-111 | 2026-09-22 | 2026-09-24 | Hai hành vi của `tools/ci/job.sh` chưa có test chốt: chặn gitleaks khi quét 0 commit (`job.sh:106-109`) và mount thư mục SARIF cho trivy (`job.sh:326-342`) | Vòng sửa review lượt 1 kiểm hai hành vi này bằng tay (probe trong container, `docker run` trực tiếp), chưa thành test | B0-09 (`tools/ci/tests/test_workflows.py`) | P3 | mở — review lượt 2 (N2). Chữa: test `source job.sh` với `git`/`docker` giả trên `PATH` (cùng kiểu test pip-audit của #1) · FIX-084 (sha 844fea3): thêm test chốt hai hành vi đã đúng từ trước của job.sh — job_lint_gitleaks hỏng-an-toàn khi quét 0 commit, job_build_trivy mount thư mục SARIF host qua RUNNER_TEMP. Hai test xanh trên cả main lẫn nhánh (đúng kỳ vọng cho nợ thiếu-test, không phải lỗi hành vi). Review APPROVE docs/reviews/2026-09-24-fix-debt-01-tooling.md. |
| ✅ | NO-112 | 2026-09-22 | 2026-09-24 | Ảnh `web` chạy nginx 1.28.0, trong dải bị ảnh hưởng của CVE-2026-42945 (heap overflow `ngx_http_rewrite_module`, có thể thực thi mã; nginx.org: 0.6.27–1.30.0, vá ở 1.30.1+) và CVE-2026-42533 (map regex, vá ở 1.30.4+) | Ảnh nền `nginx-unprivileged:1.28.0-alpine` ghim trước khi có bản vá; dòng 1.28/1.29 không có tag vá | B0-08 (`deploy/docker/web.Dockerfile:15`) | P1 | đang sửa — FIX-070 vòng 2 đổi sang `1.30.5-alpine` (dòng stable). Waiver của người dùng 2026-09-22 cùng NO-102 để gộp B0-09 trước · **FIX-070** (`b232603`): `nginx -v` trong ảnh = 1.30.5; advisory nginx.org 2026-09-24: CVE-2026-42945 vá 1.30.1+, CVE-2026-42533 vá 1.30.4+, CVE-2026-90439 (ngx_http_v3) vá 1.30.5+/1.31.6+ — 1.31.5 mainline mà dependabot `44b299f` đưa lên `main` còn dính, 1.30.5 sạch mọi mục (reviewer đọc 63 mục). Review lượt 2 APPROVE |
| ✅ | NO-113 | 2026-09-22 | 2026-09-24 | Job `build` của CI (`tools/ci/job.sh` `job_build_trivy`) xanh không chứng minh ảnh `web` sạch CVE của **nginx**: trivy 0.74.0 không báo CVE nào của gói `nginx` (gói nginx.org cài bằng apk) ở cả 1.28.0 lẫn 1.29.8, dù hai bản đó nằm trong dải >10 CVE nginx năm 2026 | CSDL của trivy không ánh xạ gói nginx của kho nginx.org | B0-09 (`tools/ci/job.sh`), ảnh B0-08 | P2 | mở — review FIX-070 (D1). Chữa: CI kiểm `nginx -v` của ảnh so với bản vá tối thiểu (hằng ghim, có test) hoặc thêm nguồn quét biết gói nginx.org · FIX-084 (sha 844fea3): job.sh thêm job_build_check_nginx_version so nginx -v của ảnh web với NGINX_MIN_VERSION=1.30.4 (sort -C -V), bù trivy không ánh xạ CVE nginx.org. Test test_job_build_check_nginx_version_passes_at_minimum/_passes_above_minimum đỏ trên main (command not found, 127) → xanh; kèm nợ mới NO-181 (assert _fails_below_minimum còn yếu). Review APPROVE docs/reviews/2026-09-24-fix-debt-01-tooling.md. |
| ✅ | NO-114 | 2026-09-23 | 2026-09-23 | `restore.sh`/`deploy.sh` cố kéo `ghcr.io/mungvu2004/appback-api:ci` dù `drill.sh` đặt `IMAGE_REGISTRY=""` để buộc dùng ảnh cục bộ — `denied` khi không có mạng/registry | `deploy/scripts/lib.sh:9` dùng `: "${IMAGE_REGISTRY:=ghcr.io/mungvu2004}"`; toán tử `:=` coi chuỗi rỗng như chưa đặt, ghi đè lại registry mặc định | B0-10 (`deploy/scripts/lib.sh`) | P1 | đóng — commit `0457ce0` nhánh `feature/b0-10-cd-backup-restore`: bỏ dấu `:` (`${IMAGE_REGISTRY=...}`) để giữ chuỗi rỗng cố ý; phát hiện khi chạy `deploy/backup/restore.sh` thật (kiểm nghiệm thu B0-10 việc 1) |
| ✅ | NO-115 | 2026-09-23 | 2026-09-23 | `smoke.sh` gọi qua `restore.sh`/`deploy.sh`/`drill.sh` trên Git Bash (Windows) báo hỏng mọi kiểm (`curl: (23) client returned ERROR on write`) dù server trả 200 thật | `MSYS_NO_PATHCONV=1` (`drill.sh`/`deploy.sh` cần cho `docker run -v`) bị kế thừa sang tiến trình `smoke.sh`; `curl.exe` (không phải MSYS) nhận thẳng đường POSIX `-o`/`-D` không dịch, ghi hỏng | B0-10 (`deploy/scripts/smoke.sh`) | P1 | đóng — commit `0457ce0`: `smoke.sh` tự `unset MSYS_NO_PATHCONV` đầu file (không gọi `docker` nên không cần biến này) |
| ✅ | NO-116 | 2026-09-23 | 2026-09-23 | `deploy.sh <tag>` đo bằng vòng `curl` 100 ms tới `/api/health` cho `mã 000` (từ chối kết nối) đúng lúc `swap_api` xoá container `api` cũ, dù container mới đã healthy | nginx template (`resolver 127.0.0.11 valid=10s`) cache DNS "api" tới 10 s; xoá container cũ trước khi nginx tự dịch lại làm request đang bay bị từ chối, dù thứ tự 6 bước (hop-dong.md §2) đúng | B0-10 (`deploy/scripts/lib.sh` `swap_api`) | P1 | đóng — commit `0457ce0`: thêm `sleep "${APPBACK_API_SWAP_SETTLE_S:-11}"` giữa "mới healthy" và "xoá cũ"; đo lại 3 lượt `deploy.sh ci2` thật (poll 100 ms) không còn `000` ở đoạn đổi `api` |
| ✅ | NO-117 | 2026-09-23 | 2026-09-24 | Đổi phiên bản bằng `deploy.sh` chưa đạt "không gián đoạn" (B0-10 [8] mục 2) trên máy kiểm Windows/Docker Desktop: poll 100 ms bằng client trong container trên mạng compose đo được 7 phản hồi hỏng qua 2 lượt (treo ~2 s, không phải từ chối) | Đối chiếu từng lỗi với `docker events` (REPORT B0-10 vòng sửa 2): 4/7 trùng trong ≤130 ms với `network disconnect` khi `swap_workers` tái tạo `worker`/`beat` — cả `web` không bị đụng cũng nghẽn, nghi Docker Desktop tái lập bridge/iptables; 3/7 chưa gán được nguyên nhân. Gate bí danh `api` trong `swap_api` không làm được: Docker từ chối thêm alias cho endpoint đang nối ("endpoint already exists in network") | B0-10 (`deploy/scripts/lib.sh`), có thể B0-08 (`deploy/nginx/**`) | P1 | mở — **waiver của người dùng 2026-09-23**: gộp B0-10 với nợ mở. Trước M1: đo lại bằng đúng cách trên runner/VPS Linux; còn lỗi → FIX B0-08: nginx `upstream{}` + `proxy_next_upstream error timeout` + `proxy_connect_timeout` ngắn (review lượt 2 N1) · FIX-086 (`47cdab2`) + FIX-087 (`f363377`): `proxy_connect_timeout` 1s + `proxy_next_upstream`. Tác giả đo 2 lượt đổi phiên bản (387 + 398 mẫu, 0 phản hồi hỏng); reviewer tự đo độc lập 517 mẫu, 0 phản hồi hỏng. Không dùng `upstream{}` — reviewer chấp nhận phương án, nhưng lý do nêu "nginx OSS không phân giải lại" sai từ bản 1.27.3 → nợ mới; không có runner Linux CI (Actions bị khoá billing). Review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md`. |
| ✅ | NO-118 | 2026-09-23 | 2026-09-24 | `api` trong `deploy/compose/ci.yml:87` publish cổng host cố định (`127.0.0.1:${API_HOST_PORT:-8000}:8000`) nên `docker compose up --scale api=2` trên `ci.yml` hỏng vì trùng cổng; kiểm "không gián đoạn" của B0-10 [8] phải thêm override `ports: !reset []` tạo lúc chạy | B0-08 dựng `ci.yml` cho e2e/smoke gọi `api` trực tiếp, trước khi B0-10 cần đổi `api` hai bản | B0-08 (`deploy/compose/ci.yml`) | P3 | mở — B0-10 vòng qua bằng override ngoài repo (hop-dong B0-10 §6, người dùng duyệt 2026-09-22). Chữa: bỏ cổng host của `api` trong `ci.yml` (smoke đi qua `web`) hoặc tách sang profile riêng, rồi `drill`/job smoke gọi `/api/*` qua `web` · (2026-09-24) phần job.sh xong ở FIX-084 (sha 844fea3), chờ nhánh deploy. · Phần `job.sh` FIX-084 đã vào `main` `5f948ab`; phần `deploy/**` FIX-086 (`47cdab2`) + FIX-087 (`f363377`, `drill.sh`). Review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md`. |
| ➖ | NO-119 | 2026-09-23 | 2026-09-23 | `deploy/scripts/drill.sh` chạy `python -m packages.db.seeds` lần hai dù dịch vụ `migrate` đã seed | B0-10 [6] `drill.sh` bước 1 đòi seed tường minh | B0-10 (`deploy/scripts/drill.sh`) | P3 | Chấp nhận (review B0-10 lượt 1, P2 R-34): seed phải idempotent; drill đếm dòng sau khi seed nên seed lần hai không làm lệch phép so. Nâng cấp nếu seed thôi idempotent: bỏ bước seed của drill và dựa vào `migrate` |
| ✅ | NO-120 | 2026-09-23 | 2026-09-24 | Test chốt `APPBACK_API_SWAP_SETTLE_S` ≥ `resolver valid` + 1 (`deploy/scripts/tests/test_deploy.py:303`) bắt nhầm dòng kế hoạch `--dry-run` (`lib.sh:86`) thay vì `sleep` thật (`lib.sh:120`): hạ mặc định của `sleep` vẫn xanh | Vòng sửa 2 thêm bước settle vào kế hoạch dry-run (N6) với chuỗi mặc định riêng; hằng `11` nay có 3 bản (`lib.sh:86,120`, `README.md:272`) | B0-10 (`deploy/scripts/lib.sh`, `tests/test_deploy.py`) | P3 | mở — review B0-10 lượt 3 R1/R2. Chữa: khai `: "${APPBACK_API_SWAP_SETTLE_S=11}"` một lần đầu `lib.sh` (không `:=`, bài học NO-114), dùng biến trần ở cả hai chỗ; test đọc đúng dòng khai báo. Kèm R3 (`test_drill.py` đặt `FAKE_LOG` và khẳng định `compose down -v`) và R4 (annotation `monkeypatch` ở `test_restore.py:254`) · FIX-087 (`f363377`): `APPBACK_API_SWAP_SETTLE_S` khai một lần đầu `lib.sh` (`: "${APPBACK_API_SWAP_SETTLE_S=11}"`), hai chỗ dùng biến trần, test đọc đúng dòng khai và đối chiếu `README.md`. Review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md`. |
| ✅ | NO-121 | 2026-09-23 | 2026-09-23 | `pdf_page_size_pt`/`render_pdf_page` để `pypdfium2.PdfiumError` lọt khỏi gói khi trang hỏng mà tài liệu mở được (tái hiện: `/Type /Page` trang cuối thành `/Type/Bad`, `get_page` ném "Failed to load page", `err_code=None`) → 500 thay vì 422 `FILE_CORRUPT` | `_open_document` bắt `PdfiumError` nhưng `_page_size_pt` (`pdf.py:197`) và `_render_page` (`pdf.py:238`) chỉ có `try/finally`; ca U07 cũ (cắt 50 %) hỏng ngay ở bước mở nên không chạm đường này | B2-05a (`packages/vision/preprocess/pdf.py`) | P1 | Đã sửa `d49b3d1` (nhánh `feature/b2-05a-preprocess-quality`, squash B2-05a): context manager `_open_page` bao trọn đường trang → `FILE_CORRUPT`, 2 test U07 trang hỏng; review lượt 2 tự tái hiện và xác nhận. Trước: vòng sửa 1 của việc gộp B2-05a (review lượt 1 finding 1): bắt `PdfiumError` bao trọn phần chạm pdfium của cả hai hàm → `VisionError("FILE_CORRUPT")`, test U07 trang hỏng cho cả ba hàm công khai |
| ✅ | NO-122 | 2026-09-23 | 2026-09-23 | 48 hàm trong `packages/vision` thiếu docstring (R-01): phần lớn hàm test ở `quality/tests/test_thresholds.py`, `test_metrics.py`, `preprocess/tests/test_raster.py`, và `types.py:37,68,100`, `assess.py:63`, `metrics.py:38`, `errors.py:31`; kèm 5 `# type: ignore[...]` trong test thiếu lý do | Ba worker lớp 1 và việc gộp viết song song, không ai quét R-01 trên hàm test/`__post_init__` | B2-05a (`packages/vision/{preprocess,quality}`) | P2 | Đã sửa `f33e6ba` (squash B2-05a): 50 docstring, quét AST `packages/vision` = 0 thiếu, 7/7 `# type: ignore` có lý do; review lượt 2 xác nhận (review lượt 1 finding 2, Nit 4) |
| ➖ | NO-123 | 2026-09-23 | 2026-09-23 | Nhánh B2-05a ~1.440 dòng logic (> 400, MNT-05) | Khối [10] của B2-05a định nghĩa đúng một đơn vị bàn giao gồm hai gói; nhánh đã dựng từ 3 nhánh lớp 1 | B2-05a | P3 | Chấp nhận (review lượt 1 finding 3): tách nhỏ phá đơn vị bàn giao và thứ tự phụ thuộc B2-04/B2-05b; đường nâng cấp: prompt sau tách theo gói nếu vượt 800 dòng |
| ⬜ | NO-124 | 2026-09-23 | | Mọi `PdfiumError` ở đường trang (`packages/vision/preprocess/pdf.py:79-80`) thành 422 `FILE_CORRUPT`, kể cả lỗi tài nguyên sau khi trang đã nạp (`Failed to get bitmap buffer`, `Failed to get page rotation`); gói không log nên sự cố gần trần 40 MP bị đếm lặng lẽ thành lỗi dữ liệu vào | `PdfiumError` đường trang có `err_code=None`, không có gì để phân loại; gói ảnh không log (thuần) | B2-04 (người gọi W7; bàn giao DEBT-01) | P3 | mở — review B2-05a lượt 2 finding P3; giữ 422 ở gói · **bàn giao** 2026-09-24: đã chép vào `prompts/B2-04.md` (khối [6], dòng 104) — W7 ghi `exc.__cause__` mức warning khi `PdfiumError` đường trang thành `FILE_CORRUPT` |
| ✅ | NO-125 | 2026-09-23 | 2026-09-24 | Đường thân `with _open_page(...)` (`get_size`/`render`/`to_numpy` ném `PdfiumError`) không có test riêng; 100 % coverage không bắt vì dòng `except` đã phủ bởi ca mở trang hỏng | Test U07 vòng sửa 1 chỉ dựng trang không nạp được | B2-05a (`packages/vision/preprocess/tests/test_pdf.py`) | P3 | mở — review B2-05a lượt 2 Nit: thêm ca `monkeypatch.setattr(pdfium.PdfPage, "render", …)` chốt `FILE_CORRUPT`; kèm dòng 132 ký tự ở `test_geometry.py:121` · Đã sửa — FIX-095 (`c3e32b7`). Test test_render_pdf_page_wraps_pdfium_error_raised_inside_open_page chốt nhánh PdfiumError ném trong thân with _open_page(...) (render), không chỉ lúc mở trang; dòng 132 ký tự ở test_geometry.py:121 đã bọc lại. Coverage thuần, reviewer tự chạy lại: packages/vision 100%/100%. Review APPROVE docs/reviews/2026-09-24-fix-debt-01-modules.md (4,78/5), cổng đầy đủ thoát 0 (3764 passed). |
| ✅ | NO-126 | 2026-09-23 | 2026-09-23 | `main` đỏ ở verify bước 5 (13 failed + 66 errors, `tools/contract/tests/*`, `packages/testing/golden/tests/test_recorder.py::test_golden_issue_token__C01`): `node` trong ảnh verify thoát 127 `libatomic.so.1: cannot open shared object file` | PR #3 dependabot (`3c266b8`, gộp `3c9305f`) nâng `node:20-bookworm-slim` → `node:26-bookworm-slim` ở `deploy/docker/verify.Dockerfile:4` (và `web.Dockerfile`); ảnh verify chép riêng binary `node` sang nền Python không có `libatomic1`. PR được gộp khi CI **không chạy** (tài khoản GitHub bị khoá vì billing: "The job was not started because your account is locked due to a billing issue") | B0-08 (`deploy/docker/*.Dockerfile`), B0-09 (`.github/dependabot.yml`) | P1 | mở — phát hiện ở verify tích hợp sau gộp B2-05a (`f797ba9`); B2-05a xanh ở đây (mọi test `packages/vision` qua). Người dùng chọn (2026-09-23): thêm `libatomic1` vào `deploy/docker/verify.Dockerfile`, giữ node 26 · **đang sửa** — FIX-074, nhánh `fix/verify-unblock-c10-node` (chung FIX-073); chặn cổng đầy đủ của B2-01 · **đóng** — `d461ec8` (squash nhánh `fix/verify-unblock-c10-node` @ `950a791`; review lượt 2 APPROVE 4,97/5, verify 8/8, 2796 qua / 0 hỏng) |
| ✅ | NO-127 | 2026-09-23 | 2026-09-23 | `test_common__C10` (`apps/api/core/tests/test_common.py:196-207`) không đạt được với **bất kỳ** route ghi được bảo vệ nào: gửi `json={}` tới đường có id mẫu (`prj_01JABC…`) nên lượt đầu luôn lỗi (422 thân sai, hoặc 404 từ dependency quyền), mà BE-00 §7 xoá dòng idempotency khi handler lỗi → lượt lặp chạy lại thật (khác `requestId`), không có gì để phát lại; `IDEMPOTENCY_KEY_REUSED` cũng không xảy ra | Viết khi repo chưa có route được bảo vệ (tập tham số rỗng) nên chưa từng chạy thật; B2-01 là prompt đầu tiên mount route bảo vệ (`test_common__C10[projects_create_project|projects_update_project|projects_delete_project]` đỏ) | B0-06 (`apps/api/core/tests/test_common.py`) | P1 | **đang sửa** — FIX-073, nhánh `fix/verify-unblock-c10-node` (chung FIX-074/075/076, người dùng chọn 2026-09-23): C10 chung mồi sẵn dòng `completed` như C22 mồi `in_progress`, khẳng định lượt lặp trả đúng response đã lưu và thân khác → 422 `IDEMPOTENCY_KEY_REUSED`; chặn B2-01 và mọi prompt ghi sau · **đóng** — `d461ec8` (squash nhánh `fix/verify-unblock-c10-node` @ `950a791`; review lượt 2 APPROVE 4,97/5, verify 8/8, 2796 qua / 0 hỏng) |
| ✅ | NO-128 | 2026-09-23 | 2026-09-23 | `load_bind_rows` giữ nguyên backtick ở cột **Khoá** (`tools/charter.py:115`: `lock=cells[col_lock].strip()`, không `_strip_markdown` như cột op/chủ), trong khi BE-BIND viết `` `project.create` ``: `apps/api/core/tests/test_routes.py::test_permission_keys_match_bind_rows` đỏ (`'project.create' == '`project.create`'`), `apps/api/access/tests/test_deps.py::test_role_key_on_bind_route_fails_the_route_scan` đỏ | Trước B2-01 chưa route nào mount có Khoá là tên khoá (chỉ `—`, `thành viên`) | B0-01 (`tools/charter.py`) | P1 | **đang sửa** — FIX-075, nhánh `fix/verify-unblock-c10-node` (chung FIX-073/074/076): `lock=_strip_markdown(cells[col_lock])` + test; chặn bước 5 của B2-01 · **đóng** — `d461ec8` (squash nhánh `fix/verify-unblock-c10-node` @ `950a791`; review lượt 2 APPROVE 4,97/5, verify 8/8, 2796 qua / 0 hỏng) |
| ✅ | NO-129 | 2026-09-23 | 2026-09-23 | `test_common__C05` parametrize hai lần (`token` × `operation`) nên id là `test_common__C05[chuoi-rac-projects_create_project]`; `_TEST_COMMON_RE` của `tools/case_gate.py:294` tách op sai → `case_gate` báo thiếu C05 cho **mọi** op được bảo vệ dù test xanh | Viết khi tập tham số `operation` rỗng nên id chưa từng xuất hiện | B0-06 (`apps/api/core/tests/test_common.py:128-133`) | P1 | **đang sửa** — FIX-076, cùng nhánh FIX-073: lặp `BAD_TOKENS` trong thân test, id trở lại `test_common__C05[<op>]`; chặn bước 5b của B2-01 · **đóng** — `d461ec8` (squash nhánh `fix/verify-unblock-c10-node` @ `950a791`; review lượt 2 APPROVE 4,97/5, verify 8/8, 2796 qua / 0 hỏng) |
| ✅ | NO-130 | 2026-09-23 | 2026-09-24 | `[tool.coverage.run]` (`pyproject.toml:102-108`) thiếu `concurrency = ["thread", "greenlet"]`: coverage không ghi dòng chạy ngay sau `await` của SQLAlchemy async (greenlet), nhánh bị tính khuyết — B2-01 A và M phải tách logic hậu-`await` ra hàm sync để đạt 90% nhánh (`apps/api/projects/access.py`, `jobs.py`, `service.py`) | Cấu hình coverage B0-01 không khai greenlet dù `sqlalchemy[asyncio]` kéo `greenlet` (uv.lock) | B0-01 (`pyproject.toml`) | P2 | mở — phát hiện ở B2-01. Chữa: thêm `concurrency = ["thread", "greenlet"]`, chạy cổng đầy đủ so tổng độ phủ trước/sau; sau đó có thể gộp lại các hàm sync tách tạm · FIX-083 (sha 531c690): pyproject.toml [tool.coverage.run] concurrency = ["thread", "greenlet"] — coverage ghi được dòng/nhánh sau await của SQLAlchemy async. Độ phủ tổng trước (main 9f11ddf): 99,07% dòng / 97,74% nhánh; sau (fix/debt-01-tooling 86b31b9): 99,41% dòng / 98,19% nhánh (Δ +0,34pp dòng, +0,45pp nhánh). Review APPROVE docs/reviews/2026-09-24-fix-debt-01-tooling.md. |
| ✅ | NO-131 | 2026-09-23 | 2026-09-23 | 4 test của `packages/db/tests/test_new_revision.py` (dòng 65–107) dùng mã prompt **thật** `B2-01` làm ví dụ trên bản sao thư mục `versions` thật; khi B2-01 có revision `r20260923_b2_01`, luật một-revision-mỗi-prompt (BE-00 §6.1) trả 2 → `test_creates_revision_with_charter_name`, `test_revision_date_read_from_clock_at_call_time`, `test_second_revision_for_same_prompt_is_rejected`, `test_fix_revision_allowed_once` đỏ | Test chọn một mã prompt có thật chưa hợp nhất làm dữ liệu mẫu | B0-03 (`packages/db/tests/test_new_revision.py`) | P1 | **đang sửa** — FIX-077, nhánh `fix/verify-unblock-c10-node` (chung FIX-073..076): đổi sang mã không prompt nào dùng (cùng họ `b9_99` sẵn có); sẽ đỏ trên `main` ngay khi B2-01 gộp · **đóng** — `d461ec8` (squash nhánh `fix/verify-unblock-c10-node` @ `950a791`; review lượt 2 APPROVE 4,97/5, verify 8/8, 2796 qua / 0 hỏng) |
| ✅ | NO-132 | 2026-09-23 | 2026-09-23 | H2 (`tools/ci/h2.py:336-340` `_admin_header`) dùng admin giả `usr_<ULID>` **không có dòng `users`** (docstring tự ghi "id không cần tồn tại thật"); #25 `POST /api/projects` ghi `project_memberships` có FK tới `users` → `ForeignKeyViolationError` → 500 → `tools/ci/tests/test_h2.py::test_main_real_app_passes` đỏ (3014 qua / 1 hỏng ở cổng M của B2-01) | Giả định chỉ đúng khi chưa route ghi nào có FK tới `users`; B2-01 #25 là route đầu tiên (K05: người tạo là thành viên) | B0-09 (`tools/ci/h2.py`) | P1 | **đang sửa** — FIX-078, nhánh `fix/verify-unblock-c10-node`: `_migrate_and_seed()` chèn dòng `users` cho đúng id admin mà `_admin_header()` dùng; chặn bước 5 của B2-01 và mọi route ghi sau · **đóng** — `d461ec8` (squash nhánh `fix/verify-unblock-c10-node` @ `950a791`; review lượt 2 APPROVE 4,97/5, verify 8/8, 2796 qua / 0 hỏng) |
| ✅ | NO-133 | 2026-09-23 | 2026-09-23 | `tools/charter.py` độ phủ nhánh 83,33% (thiếu dòng 40, 57, 66; nhánh 46->48, 48->50): thiếu có sẵn, nhưng FIX-075 chạm file nên nó vào tập file bị chạm (`tools/verify/run.sh:47-51`) và làm bước 5 của nhánh FIX đỏ | Test của `tools/charter.py` chưa phủ các đường lỗi phân tích bảng | B0-01 (`tools/tests/test_charter.py`) | P1 | **đang sửa** — review `docs/reviews/2026-09-23-fix-verify-unblock-c10-node.md` finding 1; vòng sửa nhánh `fix/verify-unblock-c10-node` thêm 5 test (dòng không mở bằng `\|`, thiếu cột, method lạ, ô không mở, ô không đóng) · **đóng** — `d461ec8` (squash nhánh `fix/verify-unblock-c10-node` @ `950a791`; review lượt 2 APPROVE 4,97/5, verify 8/8, 2796 qua / 0 hỏng) |
| ✅ | NO-134 | 2026-09-23 | 2026-09-23 | C10 chung mới (FIX-073, `apps/api/core/tests/test_common.py`) mồi dòng `completed` thân giả `{"mau":"c10"}` status 201; lượt phát lại là 2xx nên bộ ghi golden (`packages/testing/golden/recorder.py`, nhận cả `test_common__<case>[<op>]`) lưu nó làm mẫu `C10-*.json` của op → bước 7 H1 giải bằng `ProjectSchema` hỏng `createdAt` (3 mẫu, 3 op ghi của B2-01) | Hồi quy của chính FIX-073: thân mồi không phải response thật của op | B0-06 (`test_common.py`) / B0-07 (bộ ghi golden) | P1 | **đang sửa** — vòng sửa nhánh `fix/verify-unblock-c10-node`: lượt phát lại của C10 chung không được vào kho golden như mẫu thành công của op (sửa trong `test_common.py` nếu đủ, không thì ở bộ ghi) · **đóng** — `d461ec8` (squash nhánh `fix/verify-unblock-c10-node` @ `950a791`; review lượt 2 APPROVE 4,97/5, verify 8/8, 2796 qua / 0 hỏng) |
| ✅ | NO-135 | 2026-09-23 | 2026-09-24 | `UserOut.avatarUrl` (`apps/api/projects/wire.py` `user_out`) luôn vắng ở v1: `users.avatar_key` có sẵn nhưng đường ký URL thuộc B1-04 | B1-04 chưa hợp nhất (B2-01 [1]: "`UserOut.avatarUrl` vắng ở v1, ghi nợ") | B2-01 → sửa khi B1-04 gộp (`apps/api/projects/wire.py`) | P3 | mở — FE khai `avatarUrl` optional nên không vỡ hợp đồng; chữa: `user_out()` ký URL qua hàm của B1-04 · Đã sửa — FIX-094 (`672b44b`). user_out(row, storage) ký avatarUrl qua apps.api.me.avatar.avatar_url (B1-04) khi service.py có kho từ app.state.storage; None khi gọi thẳng không qua app. Test test_wire.py::test_user_out_* đỏ (4/10 failed, đổi chữ ký) trên main → xanh (10/10). Review APPROVE docs/reviews/2026-09-24-fix-debt-01-modules.md (4,78/5), cổng đầy đủ thoát 0 (3764 passed). Dây avatarUrl chưa có test route qua app.state.storage thật, docstring test_routes_build.py:91 sai: NO-194 (mới). |
| ✅ | NO-136 | 2026-09-23 | 2026-09-24 | `apps/api/projects/service.py` `_checked(None)` (dự án bị xoá mềm giữa cổng quyền và lượt `UPDATE … RETURNING`) không có test: 1 nhánh khuyết; `service.py` 87% dòng vì 12 dòng sau `await` không được coverage ghi (NO-130) | Đường đua khó dựng bằng HTTP; công cụ đo thiếu greenlet | B2-01 (`apps/api/projects/service.py`) | P3 | mở — gói vẫn 97,74% dòng / 96,15% nhánh; chữa: test gọi thẳng service với hai session xen kẽ, và gộp lại logic sau NO-130 · Đã sửa — FIX-094 (`672b44b`). Test test_write_changes_raises_not_found_when_deleted_between_gate_and_update gọi thẳng _write_changes với hai session xen kẽ, chốt nhánh _checked(None) (dự án bị xoá mềm giữa cổng quyền và UPDATE … RETURNING). Coverage thuần, reviewer tự chạy lại qua Postgres thật. Review APPROVE docs/reviews/2026-09-24-fix-debt-01-modules.md (4,78/5), cổng đầy đủ thoát 0 (3764 passed). |
| ✅ | NO-137 | 2026-09-23 | 2026-09-24 | `ProjectAccess.project_name` chưa route nào dùng: cổng quyền khai ở `dependencies=` vì C10/C22 chung ghi đè mọi dependency quyền bằng `lambda: None`, handler đọc `ProjectAccess` sẽ 500 | Máy móc case chung của B0-06 (`_grant_everything`) thay cổng quyền bằng `None` | B0-06 (`apps/api/core/tests/test_common.py`) / B2-02 (người dùng đầu tiên của `project_name`) | P3 | mở — chữa: `_grant_everything` trả một `ProjectAccess` giả đúng kiểu thay vì `None`, để route đọc được `ProjectAccess` qua tham số · Đã sửa — FIX-090 (`301bf67`). _grant_everything(app, principal) trả ProjectAccess đúng kiểu (project_id từ PATH_VALUES) thay vì None. Test test_grant_everything_overrides_with_a_typed_project_access đỏ trên main → xanh. Review lượt 2 APPROVE docs/reviews/2026-09-24-fix-debt-01-core-round-2.md (4,77/5). |
| ✅ | NO-138 | 2026-09-23 | 2026-09-23 | `run_project_purge` (`apps/api/projects/jobs.py:99-105`) lặp vô hạn khi cả một lô hỏng: vòng chỉ thoát khi `len(ids) < batch`, `_select_batch` chọn lại đúng tập cũ nên ≥ `PROJECT_PURGE_BATCH` dự án quá hạn mà kho/DB hỏng thì chạy mãi tới `task_time_limit` 3600 s | Điều kiện dừng dựa vào số dòng chọn được, không dựa vào tiến trình; test chỉ có 1 dự án hỏng với lô 10 | B2-01 (`apps/api/projects/jobs.py`) | P1 | **đang sửa** — review B2-01 lượt 1 finding 1 (`docs/reviews/2026-09-23-feature-b2-01-projects-summaries.md`); vòng sửa: phân trang keyset `id > after`, test `batch + 1` dự án với kho luôn ném · **đóng** — `7cad6ce` (squash B2-01; trên nhánh `ecc4dae`): keyset `id > after`, test `test_purge_does_not_loop_when_a_whole_batch_fails`; review lượt 2 APPROVE 4,97/5 |
| ➖ | NO-139 | 2026-09-23 | 2026-09-23 | Nhánh B2-01 ~4.500 dòng thêm (> 400 dòng logic, MNT-05) | Khối [10] của B2-01 định nghĩa một đơn vị bàn giao gồm 12 module + model + revision + factory; nhánh dựng từ 5 nhánh con | B2-01 | P2 | Chấp nhận (review lượt 1, ghi nhận không đòi tách): tách nhỏ phá đơn vị bàn giao của prompt; như NO-123 |
| ✅ | NO-140 | 2026-09-23 | 2026-09-24 | `on_after_commit` (`packages/db/hooks.py` `_on_commit`, nghe `Session` `after_commit`) chạy callback khi một **savepoint** (`begin_nested`) được release, trước khi giao dịch ngoài commit: SQLAlchemy 2.0 bắn `after_commit` cả cho `SessionTransaction` lồng, `_on_commit` không phân biệt lồng/ngoài. B1-03 D tái hiện bằng listener gắn thẳng `Session` (INSERT trong `begin_nested` → sự kiện bắn trước `commit()` ngoài). Hệ quả: task/thư có thể đi trước commit, rollback sau đó vẫn không thu hồi được (K17, J09) | `test_on_after_commit__J09_savepoint_commit_keeps_inner` (`packages/db/tests/test_hooks.py`) không chạy lệnh nào trong savepoint nên không bắt được; chưa có mã sản phẩm nào dùng `begin_nested` (B1-03 đổi sang `INSERT … ON CONFLICT DO NOTHING` để né) | B0-03 (`packages/db/hooks.py`, `packages/db/tests/test_hooks.py`) | P2 | mở — tiềm ẩn, chưa caller. Chữa: `_on_commit` bỏ qua khi `session.get_transaction()` còn mở / transaction vừa kết thúc có `nested=True` (nghe `after_transaction_end` thay vì `after_commit`), thêm test có INSERT trong savepoint rồi rollback ngoài → callback không chạy · Đã sửa — FIX-088 (`3286027`). `_on_commit` bỏ qua lượt after_commit của SessionTransaction lồng nên nhả SAVEPOINT không còn kích callback; rollback ngoài vẫn bỏ callback qua after_soft_rollback có sẵn. Test test_on_after_commit__J09_savepoint_release_waits_for_the_outer_commit (INSERT thật trong savepoint, Postgres thật) đỏ mã thoát 1 trên main → xanh trên nhánh. Review lượt 2 APPROVE docs/reviews/2026-09-24-fix-debt-01-core-round-2.md (4,77/5), cổng đầy đủ thoát 0 (3792 passed). |
| ✅ | NO-141 | 2026-09-23 | 2026-09-24 | `deploy/compose/base.yml` chỉ truyền `SMTP_URL` (không mã nào đọc) trong khi `packages/mail/settings.py` (B1-03) đọc `MAIL_BACKEND`, `SMTP_HOST`, `SMTP_PORT`, `SMTP_STARTTLS`, `SMTP_USERNAME`, `SMTP_PASSWORD`, `MAIL_FROM`, `SMTP_TIMEOUT_S` theo hợp đồng B1-03 [2]: api/worker dựng từ compose sẽ hỏng khi nạp `MailSettings` (thiếu `SMTP_HOST`/`MAIL_FROM`) ở lượt gửi thư đầu | B0-01/B0-08 khai `SMTP_URL` trước khi có hợp đồng gói thư; `deploy/**` ngoài sở hữu của B1-03 | B0-08 (`deploy/compose/*.yml`, `env.example`, `deploy/tests/test_env_example.py`) | P2 | mở — chữa: thay `SMTP_URL` bằng tám biến trên trong `&app-env`/`env.example` (dev/ci trỏ Mailpit `mailpit:1025`, `SMTP_STARTTLS=false`) · FIX-086 (`47cdab2`): `&app-env`/`env.example` truyền đúng tám biến hợp đồng B1-03 [2] thay `SMTP_URL`; `SMTP_HOST`/`MAIL_FROM` là `${…:?…}`, ba trường có kiểu lấy mặc định đọc thẳng từ `packages/mail/settings.py`. `docker compose up -d --wait` dựng được cả stack. Review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md`. |
| ✅ | NO-142 | 2026-09-23 | 2026-09-24 | `touch_projects` (`apps/api/projects/summaries.py`, gọi từ `remove_user_from_all_projects`) khoá các dòng `projects` bằng một `UPDATE … WHERE id IN (...)` theo thứ tự kế hoạch của Postgres chứ không theo `id` tăng dần như vòng lặp cũ: hai giao dịch gỡ người dùng cùng lúc có ≥ 2 dự án chung có thể deadlock (lý thuyết) | Vòng sửa PERF-01 của B2-01 gộp nhiều `UPDATE` thành một | B2-01 (`apps/api/projects/summaries.py`) | Nit | mở — review B2-01 lượt 2 Nit 1 (`docs/reviews/2026-09-23-feature-b2-01-projects-summaries-round-2.md`); chưa có caller sản phẩm. Chữa khi B1-x cắm luồng xoá người dùng: `SELECT … WHERE id IN (...) ORDER BY id FOR UPDATE` trước `UPDATE`; kèm guard lô rỗng cho `count_projects_of_users` (Nit 2) · Đã sửa (một phần) — FIX-094 (`672b44b`). touch_projects thêm SELECT … ORDER BY id FOR UPDATE (id đã sắp) trước UPDATE, đúng thứ tự khoá theo id tăng dần. Test test_touch_projects_locks_then_updates_ids_in_sorted_order đỏ mã thoát 1 trên main → xanh. Review APPROVE docs/reviews/2026-09-24-fix-debt-01-modules.md (4,78/5), cổng đầy đủ thoát 0 (3764 passed). Guard lô rỗng count_projects_of_users + test hai giao dịch chưa làm (memberships.py ngoài whitelist T4): NO-193 (mới). |
| ✅ | NO-143 | 2026-09-23 | 2026-09-23 | `run_send_token_mail` (`apps/api/auth_recovery/jobs.py:100-116`) ném `PermanentError` ở token hỏng đầu tiên (SMTP 550 hoặc `TOKEN_KEY_ROTATED`) nên token sau nó trong lô không được gửi/đánh dấu; `run_resend_unsent` dựng lại đúng lô mỗi 5 phút → thư của người khác không bao giờ tới (≤ 7 ngày với invite), địa chỉ hard-bounce bị gửi lại 12 lần/giờ. Không test lô [hỏng, tốt] | Prompt [6] ghi lỗi vĩnh viễn là "cho token đó", mã hiện thực ném ngay trong vòng; test J03 chỉ có một token | B1-03 (`apps/api/auth_recovery/jobs.py`, `tests/test_jobs.py`) | P1 | **đóng** — vòng sửa review lượt 1 B1-03 (`df2d349`, squash `ad3aa96`): `_send_one` trả mã lỗi, vòng đi tiếp, `TOKEN_KEY_ROTATED` → `superseded_at`, `MAIL_REJECTED` → `sent_at`, ném một `PermanentError` sau vòng; test `test_send_token_mail__J01_isolates_bad_token_from_batch` (đỏ trên mã cũ → xanh). Review lượt 2 xác nhận. Phần còn lại → NO-149 |
| ✅ | NO-144 | 2026-09-23 | 2026-09-23 | Test C27 của N8 (`apps/api/auth_recovery/tests/test_router_request_reset.py:117-140`) làm ấm bằng một lượt cấp token nên mọi vòng đo rơi vào nhánh cooldown: so PING với PING, chưa từng đo nhánh cấp token (thêm UPDATE + INSERT + `send_task`) | Thiết kế test C27 | B1-03 (`tests/test_router_request_reset.py`) | P2 | **đóng** — `df2d349` (squash `ad3aa96`): mỗi vòng đo C27 dựng người `active` mới nên nhánh có người thật sự cấp token; 40/40 là 204, trung vị lệch < 50 ms giữ nguyên assert |
| ✅ | NO-145 | 2026-09-23 | 2026-09-23 | `_ping_broker` (`apps/api/auth_recovery/router.py:207-213`) dựng `redis.Redis.from_url` (pool mới) mỗi request N8 không cấp token rồi đóng — đúng lưu lượng kẻ dò sinh ra, và connect mới + PING không cân được LPUSH qua pool | `broker_redis_sync()` (`packages/messaging/redis.py:123-129`) trả client mới mỗi lần gọi | B1-03 (`apps/api/auth_recovery/router.py`) | P2 | **đóng** — `df2d349` (squash `ad3aa96`): `_ping_broker` dùng `ProcessLocal[SyncRedis]` (khuôn `packages/messaging/tasks.py`), dựng lại khi đổi PID, timeout kế thừa |
| ✅ | NO-146 | 2026-09-23 | 2026-09-23 | `_seed_token` chép–dán giữa `test_tokens.py:50-75` và `test_jobs.py:71-102` (R-07); thiếu docstring ở `tokens.py:51,65`, `packages/mail/sender.py`, `payloads/auth_recovery.py:18`, `fixtures/mail.py` (R-01); `run_send_token_mail` 59 dòng (R-08); `memory_mailer: Any` | Viết song song ở ba nhánh con | B1-03 | P3 | **đóng** — `df2d349` (squash `ad3aa96`): seeder gom vào `apps/api/auth_recovery/tests/support.py`, bổ sung docstring, `memory_mailer: MemoryMailer`, `run_send_token_mail` 26 dòng |
| ➖ | NO-147 | 2026-09-23 | 2026-09-23 | Cooldown N8 (`_reset_eligible`) suy lúc phát token bằng `expires_at - PASSWORD_RESET_TTL_MIN`: đổi TTL khi token cũ còn sống thì cửa sổ cooldown của chúng dịch theo | `created_at` (`TimestampMixin`) là `func.now()` của DB, không theo `Clock` tiêm được nên không test được bằng `fake_clock` | B1-03 | Nit | Chấp nhận (finding 8 của review `docs/reviews/2026-09-23-feature-b1-03-email-password-reset-invitations.md`): lệch tối đa bằng chênh TTL, chỉ trong ≤ 60 phút sau khi đổi cấu hình. Nâng cấp: thêm cột `issued_at` ghi từ `Clock` |
| ✅ | NO-148 | 2026-09-23 | 2026-09-23 | `mypy --strict` (bước 3 cổng) đỏ trên `main` với 6 lỗi: `packages/messaging/streams.py:143` (3 lỗi `arg-type`/`index`, kiểu trả của `xread`), `packages/messaging/tests/test_locks.py:66` (`arg-type`), `apps/api/core/tests/test_internals.py:251` và `apps/api/auth/tests/test_units.py:180` (`redundant-cast`) — bước 3 của **mọi** prompt đỏ tới khi sửa | Dependabot bump `redis` 6.4.0 → 8.1.0 (`d0bb175`, PR #5 gộp thẳng trên GitHub, không qua cổng) rồi vào `main` qua `4f4f295`; stub của redis-py 8 đổi kiểu trả `xread` (thêm nhánh `dict` của RESP3) và kiểu `Awaitable` của lệnh | B0-05 (`packages/messaging/streams.py`, `packages/messaging/tests/test_locks.py`), B0-06 (`apps/api/core/tests/test_internals.py`), B1-01 (`apps/api/auth/tests/test_units.py`) | P1 | **FIX-080** gộp `a26ee15` (review APPROVE 4,64/5, cổng 8/8 đạt, mypy 6 → 0); nửa "chặn Dependabot" chuyển NO-153 · trước: B4-01 việc A đo: xoá hết file B4-01 rồi chạy mypy vẫn đúng 6 lỗi (`backend/dieu-phoi/chay/B4-01/log-viec-a-tests-1.log`, mục BASELINE MYPY); runtime chưa thấy hỏng (35 test Redis thật của B4-01 xanh). Chữa: thu hẹp kiểu `xread` ở một chỗ trong `streams.py` (không `type: ignore` trần), bỏ `cast` thừa; và chặn Dependabot gộp khi chưa qua `verify` |
| ✅ | NO-149 | 2026-09-23 | 2026-09-24 | `run_send_token_mail` (`apps/api/auth_recovery/jobs.py:118-120,152-166`): (a) `except MailRejectedError` không giữ `smtp_code` — log không phân biệt 550 với 554; (b) mã lỗi vĩnh viễn đầu tiên bị nuốt nếu token **sau** nó ném `TransientError`: lượt thử lại không còn thấy token đã cô lập (`sent_at`/`superseded_at` đã ghi) nên task "thành công", `on_failed` không bao giờ báo. Kèm Nit: `mailer: Any` thay `Mailer`; SELECT lô không `ORDER BY created_at` | Vòng sửa NO-143 gom mã lỗi vào biến cục bộ, ném sau vòng | B1-03 (`apps/api/auth_recovery/jobs.py`) | P3 | mở — finding 1–4 của review lượt 2 `docs/reviews/2026-09-23-feature-b1-03-email-password-reset-invitations-round-2.md`. Chữa: log `token_mail_failed` (id + mã + `smtp_code`) **ngay** chỗ cô lập, không đợi cuối vòng · Đã sửa — FIX-093 (`19fbed6`). Log token_mail_failed (id + mã lỗi + smtp_code) ghi ngay lúc cô lập trong _send_one, không đợi cuối vòng; SELECT lô thêm ORDER BY created_at; mailer: Mailer thay Any. Test test_send_token_mail__logs_isolated_failure_before_later_transient + test_send_token_mail__J03 đỏ mã thoát 1 (2 failed) trên main → xanh. Review APPROVE docs/reviews/2026-09-24-fix-debt-01-modules.md (4,78/5), cổng đầy đủ thoát 0 (3764 passed). Log trùng tên hai dạng (đếm đôi) + fixture thừa/kho giả tự viết: NO-195 (mới). |
| ⬜ | NO-150 | 2026-09-23 | | `one_time_tokens.sent_at` mang hai nghĩa sau NO-143: đã gửi, **hoặc** máy chủ nhận từ chối vĩnh viễn (`MAIL_REJECTED`). Màn lời mời của B1-05 nếu hiện "đã gửi lúc …" từ `sent_at` sẽ báo sai cho invite hard-bounce | Không đổi schema trong vòng sửa (chọn `sent_at` để token rời tập quét bù) | B1-05 (bàn giao DEBT-01) | Nit | mở — finding 5 của review lượt 2 `docs/reviews/2026-09-23-feature-b1-03-email-password-reset-invitations-round-2.md`. Nâng cấp: cột `failed_at`/`failure_code` riêng · **bàn giao** 2026-09-24: đã chép vào `prompts/B1-05.md` (khối [6], dòng 86) — không đọc `sent_at` là "đã gửi"; cần trạng thái gửi thì cột `failed_at`/`failure_code` |
| ✅ | NO-151 | 2026-09-23 | 2026-09-24 | Docstring `_stream_entries` (`packages/messaging/streams.py:81-82`, lặp ở `packages/messaging/tests/test_streams.py:77`) nói client streams "không đặt `protocol=3` nên luôn nhận RESP2" — sai sau bump redis-py 8.1: mặc định RESP3 trên dây (`redis/utils.py:283`), dạng list của `xread` còn giữ chỉ nhờ `legacy_responses=True` (`redis/connection.py:830`, upstream ghi là đích di trú) | FIX-080 viết lý do theo hành vi redis-py 6.4 | B0-05 (`packages/messaging/streams.py`, `packages/messaging/tests/test_streams.py`) | P2 | mở — review FIX-080 (`docs/reviews/2026-09-23-fix-b0-05-redis8-mypy.md` finding 1). Chữa: sửa hai docstring, nêu `legacy_responses=False` sẽ làm `read_after` ném `TypeError`; cân nhắc ghim `legacy_responses=True` tường minh ở `packages/messaging/redis.py` · Đã sửa — FIX-089 (`19648c3`). Đo lại trên redis-py 8.1: dây là RESP3 dù không đặt protocol=3 (docstring cũ nêu sai lý do); packages/messaging/redis.py nay ghim legacy_responses=True tường minh cho mọi client. Test test_streams_client_pins_the_legacy_list_form_of_xread khẳng định cả cờ lẫn protocol==3, đỏ (ImportError) trên main → xanh. Review lượt 2 APPROVE docs/reviews/2026-09-24-fix-debt-01-core-round-2.md (4,77/5). |
| ✅ | NO-152 | 2026-09-23 | 2026-09-24 | Retry mặc định của redis-py đổi khi bump 6.4 → 8.1 (`Retry(ExponentialWithJitterBackoff(base=1,cap=10), retries=3)` → `retries=10, base=0.01, cap=1`, `redis/_defaults.py:37-39`) mà không test nào bắt: `packages/messaging/redis.py:119-127`, `:130-136` không đặt retry tường minh, nên trần `SYNC_TIMEOUT_S` 0,5 s trên đường trả response thực tế là tới 11 lượt + backoff trước khi thành 503 | Client dựa vào retry mặc định của thư viện (R-24); gap có từ trước, bump làm nặng thêm | B0-05 (`packages/messaging/redis.py`) | P2 | mở — review FIX-080 finding 2. Chữa: đặt `retry` tường minh theo ngân sách từng vai (broker/cache/streams/sync) + test đo số lượt thử khi Redis chết · (2026-09-24) ô Nguyên nhân gốc mô tả sai hành vi redis-py 8.1: Redis.from_url() truyền connection_pool= vào Redis.__init__ nên nhánh bơm Retry(ExponentialWithJitterBackoff, retries=10) không chạy; Connection nhận retry_on_error=SENTINEL → Retry(NoBackoff(), 0), tức 0 lượt thử lại thật trên đường from_url, không phải 10 như mô tả. Mặc định 10 lượt chỉ có trên đường Redis(host=…), repo không dùng — reviewer core đo lại (docs/reviews/2026-09-24-fix-debt-01-core.md). · Đã sửa (mã) — FIX-089 (`19648c3`). Mỗi vai khai ngân sách thử lại riêng (async 2, streams 1, sync 1; backoff 0,05–0,2s), retry_on_error=[ConnectionError, TimeoutError]. Test test_a_dead_redis_costs_exactly_the_configured_number_of_retries trên Redis thật (ephemeral_broker, admin.shutdown) đỏ → xanh. Review lượt 2 APPROVE docs/reviews/2026-09-24-fix-debt-01-core-round-2.md (4,77/5). ô Nguyên nhân gốc sai: redis-py 8.1 from_url mặc định Retry(NoBackoff(), 0) (không thử lại lượt nào); FIX-089 khai ngân sách thử lại theo vai (R-24). |
| ✅ | NO-153 | 2026-09-23 | 2026-09-24 | Dependabot gộp được bản **major** thẳng vào `main` không qua cổng (nguyên nhân NO-148): `redis` khai không ràng buộc phiên bản (`packages/messaging/pyproject.toml:7`), `.github/dependabot.yml:8-19` không `ignore` `version-update:semver-major`, và PR được gộp dù `ci.yml` chạy trên `pull_request` | Không có ràng buộc phiên bản + không có luật bảo vệ nhánh buộc CI xanh | B0-09 (`.github/dependabot.yml`), B0-05 (`packages/messaging/pyproject.toml`), người điều phối (luật bảo vệ nhánh GitHub) | P2 | mở — review FIX-080 finding 3. Chữa: ràng buộc `redis>=8.1,<9`, `ignore` semver-major trong dependabot, bật required status check `verify` cho `main` · (2026-09-24) phần mã xong: FIX-084 dependabot ignore semver-major (sha 844fea3), FIX-085 redis>=8.1,<9 (sha 5579b09); chờ bật required status check trên GitHub. · **đóng** — phần mã đã vào `main` ở `5f948ab` (FIX-084 dependabot `ignore` semver-major, FIX-085 `redis>=8.1,<9`); người dùng duyệt (2026-09-24), người điều phối bật branch protection `main` qua `gh api`: required status checks `lint`, `typecheck`, `unit`, `integration`, `ml`, `contract`, `build`, `commits` (strict=false, enforce_admins=false). Lưu ý: GitHub Actions của tài khoản đang bị khoá vì billing (annotation "account is locked due to a billing issue"), nên mọi PR sẽ không gộp được tới khi mở lại; admin đẩy thẳng vẫn qua. Bản minor ảnh nền: NO-178 |
| ⬜ | NO-154 | 2026-09-23 | | Sau `bump_token_version` (đổi vai), cookie luồng `appback_stream` cũ nhận 401 ở `GET /api/streams/*` tới khi FE refresh (lượt API kế tiếp hoặc refresh chủ động); `EventSource` không đọc được status nên FE chỉ thấy nối lại thất bại lặp với giãn cách 1–30 s | Token luồng mang `ver`; `check_session` từ chối `ver` lệch (K34) — đúng B4-01 [6] bước 2, phía BE không cấp lại cookie trong luồng | F-01b (bàn giao DEBT-01; `src/lib/auth/refresh.ts`, `src/lib/realtime/eventChannel.ts`) | P3 | mở — B4-01 ghi theo prompt; review B4-01 lượt 1 (`docs/reviews/2026-09-23-feature-b4-01-sse-hub.md`). Chữa phía FE: khi luồng lỗi liên tiếp, gọi `POST /api/auth/refresh` (cấp lại cookie luồng) trước lần nối kế · **bàn giao** 2026-09-24 (người dùng chọn): FIX-097 thử nối SSE vào `refreshSingleFlight`, review lượt 2 REQUEST CHANGES `docs/reviews/2026-09-24-fix-debt-01-fe-round-2.md` — R2-1 P1 lỗi SSE tạm thời → refresh lỗi mạng/502 → đăng xuất mọi thẻ; R2-2 P2 chốt chống lặp chết theo kênh (29 POST/30 phút/luồng `progressStream`); R2-3 P2 chờ refresh không hạn giờ; R2-4 P3 `configureAuth` chỉ lúc đăng nhập. Gốc nằm ở `refresh.ts` (phân loại lỗi tạm — F-01b 4.2). FIX-097 bỏ khỏi nhánh gộp; tham khảo nhánh `fix/debt-01-fe-with-fix-097`. **Đã chép vào `prompts/F-01b.md`** (khối [4] mục 4.8) |
| ✅ | NO-155 | 2026-09-23 | 2026-09-24 | Bốn nhánh `TimeoutError` riêng của `next_frames`/`raw_until`/`wait_closed` trong `packages/testing/fixtures/streams.py` (dòng 156, 174, 187, 211) chưa có test: hạn hết đúng lúc vòng lặp quay lại | Đua thời gian khó dựng tất định; fixture vẫn 98 % dòng / 93 % nhánh | B4-01 (`packages/testing/fixtures/streams.py`) | P3 | mở — review B4-01 lượt 1 (`docs/reviews/2026-09-23-feature-b4-01-sse-hub.md`). Chữa: tiêm đồng hồ/`_POLL_TICK_S` vào fixture để ép hạn hết giữa hai lượt kéo · Đã sửa (một phần) — FIX-096 (`6caadb8`). 3/4 nhánh TimeoutError của packages/testing/fixtures/streams.py có test tất định qua monkeypatch _OPEN_TIMEOUT_S (khuôn test_disconnect_times_out_when_the_app_never_exits): dòng 174/187 (_await_start), dòng 211 (raw_until). Review APPROVE docs/reviews/2026-09-24-fix-debt-01-modules.md (4,78/5), cổng đầy đủ thoát 0 (3764 passed). Dòng 156 (_pull: app ASGI thoát không gửi thêm message → RuntimeError) vẫn Miss trên toàn apps/api/streams/tests: NO-192 (mới). |
| ✅ | NO-156 | 2026-09-23 | 2026-09-24 | Ba tài nguyên giữ ở `open_stream` (chỗ ZSET, chỗ `stream_slots`, kết nối pool SSE) chỉ trả trong `finally` của generator `stream_body`; nếu `AppRoute._finish` ném sau khi handler trả `StreamingResponse` thì generator không bao giờ chạy và cả ba rò (`apps/api/streams/sse.py:254` ↔ `apps/api/core/routing.py:236-245`) | Quyền sở hữu tài nguyên chuyển sang generator trước khi response chắc chắn được gửi; hôm nay gần như không chạm tới (commit là no-op cho route GET công khai) | B4-01 (`apps/api/streams/sse.py`) | P3 | mở — review B4-01 lượt 1 finding 4; vòng sửa để nợ (cách sửa gọn duy nhất là ép generator bắt đầu trước response). Chữa: giữ tài nguyên trong một đối tượng có `aclose` gắn `BackgroundTask`/`Response` cleanup, hoặc để B0-06 cho `AppRoute` báo lỗi sau-handler · Đã sửa — FIX-092 (`99bc366`). open_stream ghi StreamContext lên request.state; StreamRoute (apps/api/streams/router.py, mới) gọi release_held khi bất kỳ bước nào sau handler ném — trả cả chỗ ZSET, chỗ toàn cục lẫn kết nối pool SSE. Test test_a_failure_after_the_handler_returns_every_held_resource (STREAM_MAX_GLOBAL=2, 5 lượt, _finish ép ném) đỏ mã thoát 1 (2 failed) trên main → xanh. Review lượt 2 APPROVE docs/reviews/2026-09-24-fix-debt-01-core-round-2.md (4,77/5). Phần AsyncSession không rollback/close khi _finish ném vẫn mở: NO-186. |
| ✅ | NO-157 | 2026-09-23 | 2026-09-24 | `_id_key` (tách id stream thành `(ms, seq)`) viết lại ở `apps/api/streams/sse.py:81`, trùng bản private `packages/messaging/streams.py:93` (R-07) | B0-05 không phơi hàm này; B4-01 không được sửa `packages/messaging/**` ([12]) | B0-05 (`packages/messaging/streams.py`), rồi B4-01 bỏ bản sao | P3 | mở — review B4-01 lượt 1 finding 6. Chữa: B0-05 phơi `event_id_key` công khai, B4-01 nhập lại · Đã sửa — FIX-089 (`19648c3`) phơi event_id_key công khai ở packages/messaging/streams.py, FIX-092 (`99bc366`) xoá bản sao _id_key ở apps/api/streams/sse.py và nhập lại. Test test_event_id_key_orders_ids_by_milliseconds_then_sequence + test_sse_reuses_the_shared_event_id_key đỏ (ImportError) trên main → xanh. Review lượt 2 APPROVE docs/reviews/2026-09-24-fix-debt-01-core-round-2.md (4,77/5). |
| ✅ | NO-158 | 2026-09-23 | 2026-09-24 | Test `test_expire_stale_uploads__J01` (`apps/api/streams/tests/test_jobs.py`) không ép được nhiều lượt `SCAN`: seed 3 khoá với `BATCH=3`, nên nhánh quay lại vòng quét `apps/api/streams/jobs.py:91->85` chỉ chạy khi DB streams dùng chung tình cờ còn khoá khác — độ phủ nhánh gói dao động (tác giả đo 100 %, reviewer đo 98,57 % trên cùng sha `31e65fa`); kèm Nit: `registry.default_providers()` không đi qua `_check` nên luật bắt buộc `policy` chưa áp cho bản mặc định | Dữ liệu seed không vượt cỡ lô; test phụ thuộc trạng thái DB dùng chung | B4-01 (`apps/api/streams/tests/test_jobs.py`, `apps/api/streams/registry.py`) | P3 | mở — review B4-01 lượt 2 (`docs/reviews/2026-09-23-feature-b4-01-sse-hub-round-2.md`, APPROVE 4,75/5). Chữa: seed nhiều khoá hơn `BATCH` (hoặc tiêm `BATCH` nhỏ) để vòng `SCAN` luôn quay ≥ 2 lượt; cho `default_providers()` qua `_check` · Đã sửa — FIX-096 (`6caadb8`). registry.default_providers() nay đi qua _check nên luật bắt buộc policy áp cho bản mặc định; test_expire_stale_uploads__J01 seed thêm 40 khoá đệm không hạn ép SCAN quay ≥2 lượt tất định (không đổi assertion). Test test_default_providers_without_policy_is_rejected_by_check đỏ (DID NOT RAISE) trên main → xanh. Review APPROVE docs/reviews/2026-09-24-fix-debt-01-modules.md (4,78/5), cổng đầy đủ thoát 0 (3764 passed). |
| ✅ | NO-159 | 2026-09-23 | 2026-09-24 | Fixture `api_env` (`packages/testing/fixtures/api.py`) để `METRICS_PORT` mặc định 9464, nên mọi app test chạy `metrics_lifespan` và mở exporter thật: thêm ~1,8 s trên 215 test module khác (đo review B7-01 lượt 2: 61,46 s so với 59,69 s khi `=0`) | B7-01 gắn `lifespan=metrics_lifespan` vào router công khai (khối [6]); fixture là của B0-06, B7-01 không được sửa `packages/testing/**` ([12]) | B0-06 (`packages/testing/fixtures/api.py`) | P3 | mở — review B7-01 lượt 2 finding 14 (`docs/reviews/2026-09-23-feature-b7-01-telemetry-flags-metrics-round-2.md`). Chữa: `api_env` đặt `METRICS_PORT=0`; test lifespan của B7-01 tự đặt cổng tự do · Đã sửa — FIX-090 (`301bf67`). packages/testing/fixtures/api.py đặt METRICS_PORT=0. Test test_api_env_keeps_the_metrics_exporter_off đỏ trên main → xanh. Review lượt 2 APPROVE docs/reviews/2026-09-24-fix-debt-01-core-round-2.md (4,77/5). |
| ✅ | NO-160 | 2026-09-23 | 2026-09-24 | Chưa có metric HTTP chung (RED: số request, lỗi, thời gian theo route) | B7-01 chỉ dựng khung `packages/observability` và metric của riêng module; móc đo mọi request nằm ở middleware/`AppRoute` của B0-06 (khối [1] "Không làm" của B7-01) | B0-06 (`apps/api/core/middleware.py` hoặc `routing.py`) | P2 | mở — khối [11].5 của B7-01. Chữa: middleware gọi `histogram("appback_http_request_duration_ms", labels=("route","method","status"))`, nhãn `route` lấy mẫu đường (không đường thật) · Đã sửa — FIX-090 (`301bf67`, vòng sửa `b25530b`). AccessLogMiddleware ghi histogram appback_http_request_duration_ms{route,method,status}, route là mẫu đường qua _route_label. Vòng sửa round 2: _red_count khẳng định đúng một chuỗi nhãn trọn vẹn (bỏ phụ thuộc thứ tự chạy — finding P1 lượt 1) và kẹp nhãn method vào 9 method RFC 9110 + '-' (finding P2 lượt 1). Đỏ (ImportError rồi 1 failed phụ thuộc thứ tự ở cổng đầy đủ lượt 1) → xanh cổng đầy đủ lượt 2 (3792 passed). Review lượt 2 APPROVE docs/reviews/2026-09-24-fix-debt-01-core-round-2.md (4,77/5). |
| ✅ | NO-161 | 2026-09-23 | 2026-09-24 | Worker Celery và app `ml` chưa có exporter `/metrics` | `metrics_lifespan` chỉ gắn vào tiến trình API; tiến trình Celery/ml không có lifespan FastAPI | B0-05 (`apps/worker`), B5-01 (`apps/ml`) | P2 | mở — khối [11].5 của B7-01. Chữa: gọi `start_exporter(host=…, port=…)` lúc khởi động tiến trình (tín hiệu `worker_process_init` của Celery), `stop()` lúc tắt · Đã sửa — FIX-089 (`19648c3`, vòng sửa `917ce5a`). Exporter /metrics gắn ở worker_process_init trong packages/messaging/celery_app.py (chung cho apps/worker và apps/ml qua create_celery). Vòng sửa round 2 thêm messaging_env METRICS_PORT=0 + xoá cache ObservabilitySettings hai đầu + receiver worker_process_shutdown, nên không còn rò exporter thật vào tiến trình pytest (finding P1 lượt 1) và mỗi tiến trình con prefork có cổng riêng (finding P2 lượt 1). Đỏ (ImportError rồi 4 failed rò exporter ở cổng đầy đủ lượt 1) → xanh (3792 passed, cổng đầy đủ lượt 2). Review lượt 2 APPROVE docs/reviews/2026-09-24-fix-debt-01-core-round-2.md (4,77/5). worker prefork phơi dải cổng METRICS_PORT…+N−1 (metrics_port_for_process); target scrape: NO-201. |
| ✅ | NO-162 | 2026-09-23 | 2026-09-24 | Cổng exporter 9464 chưa có cấu hình scrape, và chưa kiểm compose không công bố nó ra ngoài | Hạ tầng thu thập metric (Prometheus/agent) chưa có trong `deploy/` | B0-08 (`deploy/**`) | P2 | mở — khối [11].5 của B7-01. Chữa: khai job scrape nội bộ trỏ `api:9464` (và worker/ml khi NO-161 xong); test compose khẳng định 9464 không nằm trong `ports:` · FIX-086 (`47cdab2`): thêm `deploy/observability/prometheus.yml` khai job `appback-api` scrape `api:9464`; test khẳng định 9464 không nằm trong `ports:` của dev/ci/prod. Chưa dựng dịch vụ thu thập — chờ NO-161. Review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md`. |
| ✅ | NO-163 | 2026-09-23 | 2026-09-23 | `apps/api/core/tests/test_app.py::test_discover_routers_finds_real_modules` khẳng định mỗi module đúng một router (`names == sorted(set(names))`), làm cổng đỏ khi `apps/api/telemetry` khai hai router | Test chặt hơn hợp đồng của chính B0-06: `discover_routers` nhận `ROUTERS: tuple[APIRouter, ...]`, và lớp route (bảo vệ/công khai) gắn theo router nên module có cả hai loại route buộc phải có hai router | B0-06 (`apps/api/core/tests/test_app.py`) | P1 | **FIX-081** (`f15437c`, gộp cùng B7-01 `--no-ff`) — assert `names == sorted(names)` và `sorted(set(names)) == on_disk`; review B7-01 lượt 1 finding 3, lượt 2 đóng |
| ✅ | NO-164 | 2026-09-23 | 2026-09-24 | `bash tools/verify/run.sh gc` thoát 1 ngay: `tr: range-endpoints of '_-\012' are in reverse collating sequence order` — không dọn venv/mypy cache của worktree đã xoá | `tools/verify/run.sh:150` `tr -c 'a-z0-9_-\n' '-'`: `_-\n` bị đọc là dải `_`…`\n` (ngược thứ tự); dòng 29 cùng mẫu nhưng không có `\n` nên không lỗi | B0-01 (`tools/verify/run.sh`) | P3 | mở — gặp khi dọn worktree B7-01; đã xoá tay hai volume `appback-verify-b7-01-*_appback-work`. Chữa: đặt `-` cuối tập (`'a-z0-9_\n-'`) · Cùng bản sửa với NO-101 — FIX-083 (sha 531c690): tools/verify/run.sh:150 đổi tr -c cho ký tự '-' đứng cuối tập. Test test_gc_lowercases_and_normalizes_worktree_names đỏ trên main → xanh trên nhánh. Review APPROVE docs/reviews/2026-09-24-fix-debt-01-tooling.md. |
| ✅ | NO-165 | 2026-09-24 | 2026-09-24 | `PATCH /api/me` với `"language": null` tường minh → 500 `INTERNAL` (dựng lại bằng HTTP thật) | `apps/api/me/schemas.py:47` `language` là `Literal \| None` không validator; `_profile_values` (`router.py:115-122`) ghi `language=NULL` vào cột NOT NULL → 23502, `translate_db_error` không ánh xạ → 500 | B1-04 (`apps/api/me`) | P1 | đã sửa ở vòng sửa 1 `9365372` (review lượt 2 APPROVE 4.72/5), vào `main` trong commit gộp B1-04 |
| ✅ | NO-166 | 2026-09-24 | 2026-09-24 | PNG ảnh đại diện mã hoá lại vẫn mang ICC profile của ảnh gốc (588 B vào → 588 B ra, Pillow 12.3.0) — trái bước 5 N14 | `apps/api/me/avatar.py:198-204`: `convert`/`thumbnail` chép `info`, bộ ghi PNG của Pillow đọc `im.info['icc_profile']` | B1-04 (`apps/api/me`) | P2 | đã sửa ở vòng sửa 1 `9365372` (review lượt 2 APPROVE 4.72/5), vào `main` trong commit gộp B1-04 |
| ✅ | NO-167 | 2026-09-24 | 2026-09-24 | Thiếu 4 test khối [8] B1-04: N14 storage dừng → 503 + `avatar_key` không đổi; N14 `Idempotency-Key` lặp → một object; N14 PNG 4096×4096 16-bit; N13 DB dừng → 503 | ngoài ma trận `case_gate` nên cổng không bắt | B1-04 (`apps/api/me/tests`) | P2 | đã sửa ở vòng sửa 1 `9365372` (review lượt 2 APPROVE 4.72/5), vào `main` trong commit gộp B1-04 |
| ⬜ | NO-168 | 2026-09-24 | | `packages/core/ids.py` không phơi hàm sinh ULID trần, trong khi `keys.avatar` đòi ULID trần → `apps/api/me/router.py:252-258` dựng `new_id("tpl", …)` rồi cắt tiền tố | B0-02 chỉ phơi `new_id(prefix, clock)` và `is_ulid` | B0-02 (`packages/core/ids.py`) | P3 | mở — chữa: thêm `new_ulid(clock)` công khai, đổi `_new_ulid` của B1-04 sang gọi nó · 2026-09-24 thêm bản thứ hai: `apps/api/floors/lookup.py` `new_level_id` (B2-03, `"L-"` + ULID cắt từ `new_id("job", …)`, review `docs/reviews/2026-09-24-feature-b2-03-floors.md` finding 2); chữa chung đổi cả hai chỗ |
| ⬜ | NO-169 | 2026-09-24 | | Kiểm ký tự Cc/bidi (U+202A–202E, U+2066–2069) của tên người dùng bị chép ở hai nơi: `apps/api/auth_recovery/router.py:64-73` và `apps/api/me/schemas.py:22-27` | chưa có hàm dùng chung; hàm của `auth_recovery` là private | B0-02 (`packages/core/text.py`) | P3 | mở — chữa: đưa về `packages/core/text.py`, hai module gọi lại · 2026-09-24 thêm bản thứ ba: luật tên tầng `apps/api/floors/schemas.py` (B2-03, hàm thuần dùng chung cho #10/#34 và hook #25); chữa chung đổi cả ba chỗ |
| ⬜ | NO-170 | 2026-09-24 | | `apps/api/me/avatar.py:192` gán `ImageFile.LOAD_TRUNCATED_IMAGES = False` từ luồng phụ mỗi request — BE-00 §11 cấm gán biến toàn cục này | prompt B1-04 [6] bước 4 bảo gán, trái hiến chương (hiến chương thắng); giá trị trùng mặc định Pillow nên hiện không đổi hành vi | B1-04 (`apps/api/me/avatar.py`); câu prompt: người điều phối | P3 | mở — review lượt 2 (`docs/reviews/2026-09-24-feature-b1-04-profile-password-avatar-round-2.md` #2). Chữa: xoá dòng gán, giữ test khẳng định giá trị mặc định |
| ⬜ | NO-171 | 2026-09-24 | | N14 `convert` 8-bit nguyên cỡ trước `thumbnail` cả khi ảnh đã là `RGB`/`RGBA` (4096² RGBA: RSS đỉnh ~293 MB, dưới semaphore 2) | sửa lỗi `thumbnail` vỡ với `I;16` ở tỉ lệ thu lớn (vòng sửa 1) áp cho mọi mode | B1-04 (`apps/api/me/avatar.py:196-203`) | P3 | mở — chữa khi hồ sơ bộ nhớ API thành vấn đề: chỉ convert trước khi thu nhỏ khi mode không phải `RGB`/`RGBA` |
| ⬜ | NO-172 | 2026-09-24 | | Docstring đầu 5 file test route tầng vẫn viết route "chưa hợp nhất trên nhánh này" (`apps/api/floors/tests/test_routes_create.py:3-5`, `test_routes_delete.py:3`, `test_routes_list.py:3`, `test_routes_patch.py:3`, `test_routes_reorder.py:3`) | T viết test mù trước khi có route; vòng sửa 1 chỉ vá đúng hai chỗ review lượt 1 nêu tên (vá triệu chứng, R-19) | B2-03 (`apps/api/floors/tests/**`) | P3 | mở — review lượt 2 finding #9 (`docs/reviews/2026-09-24-feature-b2-03-floors-round-2.md`). Chữa: viết lại cả 5 câu theo trạng thái đã gộp; soát `grep -rn "chưa hợp nhất" apps/api/floors` = 0 |
| ⬜ | NO-173 | 2026-09-24 | | Docstring `FloorReorderIn` (`apps/api/floors/schemas.py:122-124`) nói lỗi resolver "luôn" thắng; sai với thân `application/json` hỏng: FastAPI giải JSON trước mọi dependency nên trả 400 `MALFORMED_JSON` (đúng W8, BE-BIND #13 không đòi mã riêng) chứ không 422 `field:"floorIds"` | Vòng sửa 1 cho #13 nhận `body: FloorReorderIn` (finding API-04) mà không cập nhật câu mô tả thứ tự lỗi (`apps/api/core/routing.py:100-105` đã ghi luật) | B2-03 (`apps/api/floors/schemas.py`) | P3 | mở — review lượt 2 finding #10. Chữa: sửa câu docstring (resolver thắng mọi lỗi **sau** bước giải JSON); không cần test mới (`apps/api/core/tests/test_routing.py:160` phủ luật chung) |
| ✅ | NO-174 | 2026-09-24 | 2026-09-24 | Ảnh `web` không build được trên `main`: tầng `build` chạy `corepack enable` thoát 127 | Dependabot `3c266b8` nâng `node:26-bookworm-slim` (v26.9.0, npm 11.19.1) — bản này không còn kèm `corepack`; `deploy/docker/web.Dockerfile` vẫn `corepack enable && corepack prepare pnpm@9.4.0`. Job `build` của CI đỏ trên `main` | B0-08 (`deploy/docker/web.Dockerfile`) | P1 | 🔧 DEBT-01 T2 giữ, **FIX-100** trên nhánh `fix/b0-08-web-openssl-cve` (phát hiện khi làm FIX-070) · **FIX-100** (`792e98b`): `RUN npm install -g pnpm@9.4.0` thay `corepack` (AppFront @ `9cf0b0bf` không khai `packageManager`). Test `deploy/tests/test_dockerfiles.py::test_dockerfile_web_installs_pnpm_without_corepack` đỏ trên `main` (AssertionError) → xanh; `docker build` web thoát 1 (`corepack: not found`, 127) → 0 (reviewer build lại `--no-cache`). Review lượt 2 APPROVE |
| ✅ | NO-175 | 2026-09-24 | 2026-09-24 | `run.sh lock` sinh `uv.lock` khác định dạng bản trên `main`: thêm `marker = "platform_machine == 'x86_64' and sys_platform == 'linux'"` vào ~316 dòng phụ thuộc, không gói nào đổi bản | `deploy/docker/verify.Dockerfile` `FROM ghcr.io/astral-sh/uv:python3.12-bookworm-slim` không ghim bản/digest: uv trong ảnh verify trôi theo bản mới nhất, khác uv của dependabot sinh lock gần nhất (`d0bb175`) | B0-01 (`deploy/docker/verify.Dockerfile`) | P3 | 🔧 DEBT-01 T1 giữ, **FIX-102** (ghim uv theo bản + digest); lock đợt này nhận diff định dạng một lần (FIX-085) · FIX-102 (sha 17774f7): deploy/docker/verify.Dockerfile ghim FROM node và uv theo tag+digest hiện tại — run.sh lock nay tất định. Test test_every_from_line_pins_tag_and_digest đỏ trên main (FROM chưa ghim @sha256) → xanh; reviewer đo lại hai điều kiện: 0 gói đổi bản, run.sh lock chạy lại diff rỗng (sha256 giống hệt). Review APPROVE docs/reviews/2026-09-24-fix-debt-01-tooling.md. |
| ⬜ | NO-176 | 2026-09-24 | | `docs/fixes.md` thiếu dòng mục lục và khối 7 mục của FIX-070 … FIX-081 (sổ nhảy FIX-069 → FIX-082), dù các mã đã phát và có commit (`e73af37`, `d461ec8`, `5ec6505`, `591eaf6`, …); FIX-079 không thấy commit nào | Người điều phối cấp mã trong phiên B0-09/B7-01 mà không ghi sổ (FIX.md luật 5) | người điều phối (`docs/fixes.md`) | P3 | mở — review lượt 2 `fix/b0-08-web-openssl-cve` finding #2. DEBT-01 ghi FIX-070 cùng lần gộp; FIX-071 … FIX-081 để đợt sau (sau NO-164) |
| ✅ | NO-177 | 2026-09-24 | 2026-09-24 | Ba ảnh Python (`api`, `worker`, `ml`) trên `main` không chạy: tầng chạy là CPython 3.14.7 nhưng venv `/opt/venv` dựng cho 3.12 (C extension cp312) — `compose run migrate` → `ModuleNotFoundError: alembic`; job `build` của CI (bước `import_all`) đỏ | Dependabot `44b299f` nâng **tầng chạy** `python:3.12-slim-bookworm` → `python:3.14-slim-bookworm` ở `api.Dockerfile:34`, `worker.Dockerfile:31`, `ml.Dockerfile:74`, trong khi tầng dựng vẫn `uv:python3.12-bookworm-slim` và repo ghim `requires-python ==3.12.*` | B0-08 (`deploy/docker/*.Dockerfile`) | P1 | 🔧 DEBT-01 T2 giữ, **FIX-104** trên nhánh `fix/debt-01-deploy` (lùi tầng chạy về 3.12 ghim digest + test tĩnh minor Python tầng dựng = tầng chạy = `requires-python`) · FIX-104 (`3f2e14d`): ba tầng chạy Dockerfile lùi về `python:3.12-slim-bookworm` đúng digest sống, kèm test tĩnh minor tầng dựng = tầng chạy = `requires-python`. Tác giả và reviewer đo độc lập: `python -V` = 3.12.14, `import alembic` đạt, `docker compose run --rm migrate` thoát 0. Review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md`. |
| ⬜ | NO-178 | 2026-09-24 | | Dependabot gộp được bản **minor** ảnh nền Docker làm hỏng ảnh (python 3.12 → 3.14, NO-177; node bỏ `corepack`, NO-174) — `ignore` semver-major của FIX-084 không chặn được vì đổi tag `3.12` → `3.14` là minor | `.github/dependabot.yml` nhóm `docker-patches` không `ignore` `version-update:semver-minor` cho `python`/`node`, và PR dependabot được gộp khi CI chưa là required check (NO-153) | B0-09 (`.github/dependabot.yml`) | P2 | mở — phát hiện khi làm DEBT-01 T2. Chữa: `ignore` semver-minor cho ảnh `python`, `node` (đổi minor là việc có chủ đích, kèm relock); required status check (NO-153) chặn phần còn lại |
| ⬜ | NO-179 | 2026-09-24 | | `tools/ci/tests/test_h2.py::test_pinned_service_images_match_packages_testing` (dòng 243-250) giờ trùng lặp theo cấu trúc — cả hai vế cùng đọc `tools.pinned_images` nên không còn chặn được lệch nào, nhưng docstring vẫn khẳng định "R-28 … nên không tự gom về một chỗ được", điều FIX-083 vừa chứng minh là sai | NO-106 (FIX-083/FIX-084) dời hằng ảnh ghim sang `tools/pinned_images.py` làm nguồn chung cho `services.py` và `h2.py`, nhưng test cũ (viết khi hai nơi còn chép tay) và docstring của nó không được cập nhật theo | B0-09 (`tools/ci/tests/test_h2.py`) | P3 | mở — review APPROVE `docs/reviews/2026-09-24-fix-debt-01-tooling.md` finding F1/MNT-05. Chữa: xoá test (NO-106 nay là bảo đảm cấu trúc) hoặc đổi thành kiểm hai nơi nhập cùng một module, viết lại docstring |
| ⬜ | NO-180 | 2026-09-24 | | `job_build_smoke` (`tools/ci/job.sh:378-386`, FIX-084) bỏ `export API_HOST_PORT`, nhưng `deploy/compose/ci.yml:87` trên nhánh `fix/debt-01-tooling` vẫn publish `127.0.0.1:${API_HOST_PORT:-8000}:8000` cho `api` (việc bỏ `ports:` là của nhánh deploy/B0-08); test `tools/ci/tests/test_workflows.py:189-191` khẳng định thẳng `"API_HOST_PORT" not in text`, khoá luôn cách vá an toàn | FIX-084 (T1) và FIX-086 (T2) sửa hai nửa của cùng NO-118 trên hai nhánh khác nhau; khoảng giữa hai lần gộp, `job.sh build` bind cổng host 8000 | B0-09 (`tools/ci/job.sh`) + B0-08 (nửa `deploy/compose/ci.yml`) | P3 | mở — review APPROVE tooling finding F2/OPS-03. Tự hết khi nhánh deploy gộp, nhưng assert cần nới thành "không curl nào dùng API_HOST_PORT" |
| ⬜ | NO-181 | 2026-09-24 | | `test_job_build_check_nginx_version_fails_below_minimum` (`tools/ci/tests/test_workflows.py:456-458`, FIX-084) chỉ khẳng định `returncode != 0`, nên xanh cả trên `main` nơi hàm `job_build_check_nginx_version` chưa tồn tại (đo được: thoát 127 `command not found`) — không chứng minh phép so phiên bản thật sự từ chối nginx cũ | Assert lỏng (`!= 0`) thay vì khẳng định đúng mã thoát 1 mà `sort -C` trả khi bản dưới sàn | B0-09 (`tools/ci/tests/test_workflows.py`) | P3 | mở — review APPROVE tooling finding F3/TEST-04. Chữa: `assert result.returncode == 1` |
| ⬜ | NO-182 | 2026-09-24 | | `types: [..., edited]` (`.github/workflows/ci.yml:14`, FIX-084/NO-110) chạy lại **cả chín job** (kể cả `build` timeout 45 phút, `integration`/`contract` 30 phút) mỗi lần sửa tiêu đề hoặc thân PR, dù NO-110 chỉ cần job `commits`; không job nào có `if:` | `types:` áp cho toàn bộ `on.pull_request` của workflow, không tách được theo job bằng trigger; sửa nhanh nhất (thêm `edited`) không giới hạn phạm vi | B0-09 (`.github/workflows/ci.yml`) | P3 | mở — review APPROVE tooling finding F4/PERF-07. Chữa: `if: github.event.action != 'edited'` cho tám job còn lại, hoặc tách `commits` sang workflow riêng nghe `edited` |
| ⬜ | NO-183 | 2026-09-24 | | `NGINX_MIN_VERSION="1.30.4"` (`tools/ci/job.sh:36-40,356-363`, FIX-084/NO-113) là **một** sàn duy nhất so bằng `sort -V` dù ảnh chạy nhánh mainline (nếu một bản vá ra đồng thời ở 1.30.5 và 1.31.6 mà ảnh còn 1.31.5, `sort -V` kết luận sai → xanh giả); kèm `ignore` semver-major trần cho cả ba hệ Dependabot (FIX-084/NO-153) — hệ `docker` không có kênh security-update nên bản major ảnh nền sẽ không bao giờ được đề xuất, trong khi digest đã ghim cứng | Biện pháp bù CVE nginx chọn một hằng số sàn duy nhất thay vì ánh xạ theo nhánh phát hành; `ignore` Dependabot áp `update-types` như nhau cho mọi ecosystem dù `docker` thiếu kênh vá bảo mật riêng | B0-09 (`tools/ci/job.sh`, `.github/dependabot.yml`) | P3 | mở — review APPROVE tooling finding F5/SEC-11. Chữa: ghim sàn theo từng nhánh phát hành (`1.30 → 1.30.4`, `1.31 → 1.31.x`) hoặc buộc ảnh `web` ở nhánh stable |
| ⬜ | NO-184 | 2026-09-24 | | `packages/testing/fixtures/services.py:29-31` nhập `tools/pinned_images.py` (mới, FIX-083/NO-106) — đảo chiều tầng (tooling là lớp ngoài cùng); `.importlinter` chỉ có `root_packages = packages, apps`, nên không gì giữ cho `tools.pinned_images` khỏi mọc phụ thuộc về sau | NO-106 chọn `tools/pinned_images.py` (module mới ngoài `packages.testing`) để né luật R-28 cấm mã không phải test nhập `packages.testing`, nhưng không có hợp đồng `.importlinter` nào bọc `tools` để giữ chiều phụ thuộc | B0-01 (`packages/testing/fixtures/services.py`, `tools/pinned_images.py`, `.importlinter`) | P3 | mở — review APPROVE tooling finding F6/MNT-04. Chữa: dời ba hằng sang `packages/core`, hoặc thêm `tools` vào `root_packages` kèm một hợp đồng `forbidden` |
| ⬜ | NO-185 | 2026-09-24 | | Mọi lượt verify từ nay in cảnh báo `volume "appback-work" already exists but was created for project "appback-verify-concurrency-test-a"` (`deploy/compose/verify.yml:47-50`, NO-108) — vô hại nhưng là cảnh báo vĩnh viễn trong mọi log cổng, dễ bị đọc thành lỗi | Volume `appback-work` được tạo lần đầu bởi một project compose tên `appback-verify-concurrency-test-a` (phép thử dùng-một-lần của tác giả FIX-083); Docker gắn nhãn project lúc tạo, các lượt verify sau dùng lại volume vật lý nhưng project khác nên cảnh báo lặp lại | B0-01 (`deploy/compose/verify.yml`) | Nit | mở — review APPROVE tooling N1. Chữa: xoá `appback-work` một lần khi không còn lượt verify nào đang chạy, hoặc khai `external: true` và tạo volume tường minh |
| ⬜ | NO-186 | 2026-09-24 | | Trên đường `AppRoute` (`apps/api/core/routing.py:238-245`), `except BaseException: await _abort(...)` chỉ bọc `original(request)`; nếu `_finish` ném (đường mà test mới của NO-156/FIX-092 ép chạy) thì `AsyncSession` không rollback, không close | `try/except` chỉ quấn quanh `original(request)`, coi `_finish` là "chắc chắn không ném"; FIX-092 (SSE) mới ép được nhánh `_finish` ném 5 lượt nên đường này lộ ra là có thật | B0-06 (`apps/api/core/routing.py`) | P3 | mở — review REQUEST CHANGES `docs/reviews/2026-09-24-fix-debt-01-core.md`, mục "Nợ" #1 (finding 5/CON-04). Chữa: bọc cả `_finish` vào `try/except` gọi `_abort` |
| ⬜ | NO-187 | 2026-09-24 | | `retry_on_error=[ConnectionError, TimeoutError]` (`packages/messaging/redis.py:59,145,158`) áp cho mọi vai, kể cả `streams_redis` — client mà `EventBus.publish` dùng để `XADD`. Một lệnh hết giờ có thể đã chạy xong ở server; lượt thử lại ghi thêm một mục với id mới, FE thấy sự kiện hai lần (`STREAM_RETRIES=1` nên tối đa một bản dư) | Ngân sách thử lại được cấu hình theo **vai** (broker/cache/streams/sync) chứ không theo **loại lệnh** (đọc/ghi); mặc định thư viện trước đó là 0 lượt (không phải hồi quy) nhưng khi ngân sách đã là của ta thì cần tách đọc/ghi | B0-05 (`packages/messaging/redis.py`) | P3 | mở — review REQUEST CHANGES core, mục "Nợ" #2 (finding 6/CON-03). Chữa: tách client chỉ-đọc (thử lại cả timeout) khỏi đường ghi (chỉ thử lại lỗi kết nối) |
| ⬜ | NO-188 | 2026-09-24 | | Sau NO-097 vẫn còn gương vai thứ ba: `ROLES: Final = ("admin","engineer","viewer")` ở `packages/db/models/auth.py:29`, trùng `packages.domain.permissions.ROLES`; cộng thêm hai hàm `_role` gần giống nhau ở `apps/api/core/auth.py` và `apps/api/auth/sessions.py` | Mỗi module tự khai lại kiểu vai/hàm suy vai thay vì nhập từ một nguồn chung (`packages.domain.permissions`), vì ranh giới sở hữu file giữa B0-06/B1-01/`packages/db` (K27) không cho phép sửa chéo trong lượt vá gốc trước | B0-06 (`apps/api/core/auth.py`, `packages/db/models/auth.py`) + B1-01 (`apps/api/auth/sessions.py`) | Nit | mở — review REQUEST CHANGES core, mục "Nợ" #3 (finding 7/R-07); gộp đề nghị mục 6.2 báo cáo T1 tooling. Chữa: `packages/db/models/auth.py` nhập `ROLES` từ `packages.domain.permissions`; gộp hai `_role` gần giống về một hàm dùng chung |
| ⬜ | NO-189 | 2026-09-24 | | `deploy/scripts/smoke.sh` dùng `python3 -c 'import json…'` cho kiểm `/api/nope`; trên máy đã đo của `ENV.md`, `python3` là shim rỗng của Microsoft Store (in hướng dẫn cài rồi thoát khác 0), nên `deploy.sh`/`rollback.sh`/`drill.sh` luôn hỏng ở bước smoke và rollback nhầm | Script chọn `python3` để phân tích JSON thay vì công cụ bash sẵn có, không tính tới máy Windows không có `python3` thật trên `PATH` | B0-10 (`deploy/scripts/smoke.sh`) | P3 | mở — `backend/dieu-phoi/chay/DEBT-01/bao-cao-t2-deploy.md`, mục "Nợ mới phát hiện" NO-B. Chữa: kiểm khoá `"code"` bằng `grep`/`case` của bash thay vì `python3`, hoặc gọi `python3` trong một container như các bước khác |
| ⬜ | NO-190 | 2026-09-24 | | `bash tools/verify/run.sh shell` làm việc trên bản chép `/tmp/w` trong container, không phải worktree thật; ví dụ `ruff format deploy/` báo "4 files reformatted" nhưng worktree không đổi (bước 1 vẫn đỏ ở lượt sau). Không phải lỗi, nhưng không tài liệu nào nói, nên dễ mất giờ | `shell` (khác `lock`) không có bước chép kết quả từ `/tmp/w` ngược ra bind-mount của worktree; không có dòng tài liệu nào cảnh báo | B0-01 (`tools/verify/*`) | P3 | mở — `backend/dieu-phoi/chay/DEBT-01/bao-cao-t2-deploy.md`, mục "Nợ mới phát hiện" NO-C. Chữa: ghi một dòng trong `tools/verify/README` hoặc `ENV.md` rằng `shell` chỉ đọc, kết quả phải chép ra như `lock` đang làm |
| ⬜ | NO-191 | 2026-09-24 | | `tools/ci/job.sh` tự `cd "$REPO_ROOT"` ngay khi được nguồn (dòng 16) — mọi đường tương đối trong `job.sh` (kể cả `.cache/trivy` của nhánh không có `RUNNER_TEMP`) luôn tính từ gốc repo thật, không phải `cwd` của tiến trình gọi nó; nhánh "máy cục bộ, không có `RUNNER_TEMP`" của `job_build_trivy` vì vậy không kiểm cô lập được bằng `subprocess(cwd=...)` | `job.sh` được viết để chạy đúng khi CI `source` nó từ bất kỳ đâu, nên tự `cd` về gốc repo ngay khi nạp; test SARIF mới của FIX-084 chỉ kiểm được nhánh `RUNNER_TEMP` (CI thật luôn đặt biến này) | B0-09 (`tools/ci/job.sh`) | P3 | mở — `backend/dieu-phoi/chay/DEBT-01/bao-cao-t1-tooling.md` mục 4 "Nợ mới phát hiện"; review APPROVE tooling mục 6.2 dòng 7. Không phải lỗi hành vi (CI luôn đặt `RUNNER_TEMP`), chỉ là giới hạn test |
| ⬜ | NO-192 | 2026-09-24 | | Phần còn lại của NO-155 (đóng một phần bởi FIX-096): dòng `packages/testing/fixtures/streams.py:156` (`_pull`: app ASGI thoát mà không gửi thêm message → `RuntimeError`) vẫn chưa có test — đo trên toàn `apps/api/streams/tests` (118 passed) vẫn `Miss 156` | FIX-096 chỉ thêm 3/4 ca đã biết (`_await_start` hai nhánh, `raw_until`); chưa có ca app kết thúc sớm không gửi thân | B4-01 (`packages/testing/fixtures/streams.py`) | P3 | mở — review APPROVE `docs/reviews/2026-09-24-fix-debt-01-modules.md`, mục "Nợ" #1 (finding 1/TEST-01/R-19); tách từ NO-155. Chữa: thêm ca app gửi `http.response.start` kiểu `text/event-stream` rồi `return` không gửi thân; `next_frames(1)` phải ném `RuntimeError("…kết thúc mà không gửi thêm…")` |
| ⬜ | NO-193 | 2026-09-24 | | Phần còn lại của NO-142 (đóng một phần bởi FIX-094, phần `touch_projects`): guard lô rỗng cho `count_projects_of_users` (`apps/api/projects/memberships.py:115-126`) chưa làm, và chưa có test hai giao dịch chứng minh hết khoá chéo (test mới chỉ kiểm hình dạng SQL: có 2 câu, câu đầu chứa `FOR UPDATE`/`ORDER BY`) | `memberships.py` nằm ngoài whitelist file của T4 (`apps/api/projects/{summaries,service,wire}.py`); test mới không dựng lại race hai session | B2-01 (`apps/api/projects/memberships.py`, `apps/api/projects/tests/test_summaries.py`) | Nit | mở — review APPROVE modules, mục "Nợ" #3 (finding 3/CON-01/TEST-06); tách từ NO-142. Chữa: guard lô rỗng ở `count_projects_of_users`; thêm test hai session gọi `touch_projects` với thứ tự id ngược nhau chứng minh không deadlock |
| ⬜ | NO-194 | 2026-09-24 | | Dây `UserOut.avatarUrl` (ký qua `apps.api.me.avatar.avatar_url`, FIX-094) không có test ở mức route: không route nào đặt `avatar_key` cho một thành viên rồi kiểm `GET /api/projects` trả `avatarUrl`; `_storage_of(app)` đọc `app.state.storage` qua `getattr` hai tầng, lặng lẽ trả `None` nếu thiếu/đổi tên — ảnh đại diện biến mất khỏi dây mà mọi test vẫn xanh; docstring `apps/api/projects/tests/test_routes_build.py:91` ("không `avatarUrl`, B1-04 chưa cắm") nay sai vì B1-04 đã cắm | FIX-094 chỉ chứng minh ở mức đơn vị với kho giả (`user_out(row, storage)`), chưa có test route qua `app.state.storage` thật | B2-01 (`apps/api/projects/service.py`, `apps/api/projects/tests/test_routes_build.py`) | P3 | mở — review APPROVE modules, mục "Nợ" #2 (finding 2/TEST-01/R-17). Chữa: thêm test route (`api_client` + kho local), đặt `avatar_key`, kiểm `avatarUrl` xuất hiện; khi `app is not None` đọc thẳng `app.state.storage` như `deps.storage` (hỏng thì ném ngay thay vì `None` lặng lẽ); sửa docstring dòng 91 |
| ⬜ | NO-195 | 2026-09-24 | | Một token hỏng sinh hai bản ghi log `token_mail_failed` cùng tên khác dạng trường (`token_id`+`smtp_code` lúc cô lập trong `_send_one`, `token_ids` của cả lô ở `on_failed` khi `PermanentError` nổi lên) — luật cảnh báo đếm theo tên sự kiện sẽ đếm đôi. Kèm ba Nit khác của review modules: fixture `memory_mailer` khai thừa ở test NO-149b (bị đè ngay bởi `_TransientMailer`); `test_wire.py` tự viết kho giả `_FakeAvatarStorage` thay vì dùng fixture `local_storage` có sẵn (B1-04); nhiều test mới đặt tên một gạch dưới thay vì `test_<hàm>__<điều kiện>` (`case_gate` không bắt) | FIX-093 dùng chung tên sự kiện cho log cô lập và log tóm tắt lô; test mới không tái dùng fixture/khuôn tên sẵn có | B1-03 (`apps/api/auth_recovery/jobs.py`, `tests/test_jobs.py`) + B2-01 (`apps/api/projects/tests/test_wire.py`) | Nit | mở — review APPROVE modules, mục "Nợ" #4 (finding 4/OBS-01) + #5 (finding 5/TEST-08/R-07, finding 6 tên test). Chữa: đặt tên riêng cho log cô lập (vd `token_mail_isolated`) hoặc ghi rõ docstring `on_failed` là bản tóm tắt lô; bỏ `memory_mailer` thừa, dùng lại đường lỗi tạm thời như J02; dùng fixture `local_storage` thay kho giả; đổi tên test theo `test_<hàm>__<điều kiện>` khi chạm lại file |
| ⬜ | NO-196 | 2026-09-24 | | `SMTP_HOST`/`MAIL_FROM` trong `deploy/compose/base.yml:125,131` (`&app-env`, FIX-086) là `${…:?…}` bắt buộc, mà `prod.yml` `extends` `base.yml` → mọi lệnh `docker compose` trên prod (`pull`, `run --rm migrate`, `swap_api`, `rollback.sh`) hỏng ngay lúc nội suy nếu `appback.env` của VPS chưa có hai biến; bảng "Biến môi trường" của `deploy/scripts/README.md:260-275` không liệt kê hai dòng này, §12 "việc người phải làm trước M1" cũng không | FIX-086 đổi `SMTP_URL` (một biến, có sẵn trong `env.example`) thành tám biến bắt buộc/có kiểu của `MailSettings`, nhưng không cập nhật runbook vận hành đi kèm | B0-08 (`deploy/compose/base.yml`) + B0-10 (`deploy/scripts/README.md`) | P2 | mở — review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md` finding #1 (OPS-01). Chữa: thêm `SMTP_HOST`, `MAIL_FROM` (nên cả sáu biến `SMTP_*`/`MAIL_*` còn lại) vào bảng biến môi trường + §12 của `deploy/scripts/README.md` |
| ⬜ | NO-197 | 2026-09-24 | | FIX-086 chuyển quyết định tắt log **hẳn** sang mức `location /api/files/`, mất lớp chắn cho lỗi **xảy ra trước khi chọn location**: nginx ghi nguyên `/api/files/<token>` vào access log mặc định. Đo thật trên `appback-web:ci`: `GET /api/files/<token>` kèm header hỏng → 400, `grep -c <token>` trong `docker logs` = 1; 414 lọt tương tự. Bản cũ (trước FIX-086) lại lọt ba dạng URI méo qua `map $request_uri` (đúng NO-095) — đổi một lớp rò lấy một lớp rò khác, chưa phải "sửa gốc" trọn vẹn | `access_log off` chỉ đặt trong `location`, không còn lớp thứ hai ở mức server (map `$request_uri`) làm lưới dự phòng cho lỗi tiền-location | B0-08 (`deploy/nginx/snippets/app_locations.conf:37-44`, `deploy/nginx/templates/{dev,prod}/app.conf.template`) | P3 | mở — review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md` finding #2 (SEC-06/OBS-02). Chữa: giữ cả hai lớp — trả lại `access_log … if=$appback_access_log` mức server bên cạnh `access_log off` mức location, nới `test_nginx_files_log_decision_is_location_scoped_not_request_uri` để cho phép map như lớp thứ hai thay vì cấm hẳn |
| ⬜ | NO-198 | 2026-09-24 | | Lý do bác `upstream{}` trong comment `proxy_common.conf:18-21`, docstring và assert của `test_nginx.py:469-476,491-493` (FIX-086) sai với phiên bản đang dùng: nginx.org ghi `resolve` **đã có ở OSS từ 1.27.3**, còn `web.Dockerfile` ghim nginx 1.30.5 — assert `not find_directive(…, "upstream")` khoá một phương án hợp lệ (`upstream` + `zone` + `server … resolve`) bằng tiền đề không đúng; phương án đã chọn (biến + `resolver` + `proxy_next_upstream`) vẫn đúng và đã đo hoạt động (NO-117) | Tác giả tra tài liệu nginx bản cũ hoặc bản thương mại, không kiểm lại với `nginx.org/en/docs/http/ngx_http_upstream_module.html` (đọc 2026-09-24: "Prior to version 1.27.3, this parameter was available only as part of our commercial subscription") | B0-08 (`deploy/nginx/snippets/proxy_common.conf`, `deploy/tests/test_nginx.py`) | P3 | mở — review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md` finding #3 (RES-02/TEST-03). Chữa: sửa comment + docstring cho đúng (OSS ≥ 1.27.3 có `resolve` nhưng cần `zone`; vẫn chọn biến + `resolver` vì đơn giản hơn và đã đo); đổi assert thành "nếu có `upstream{}` thì phải kèm `zone` và `server … resolve`" |
| ⬜ | NO-199 | 2026-09-24 | | `revoke_stale_users()` (FIX-086, `deploy/minio/init.sh:46-58,67-77`) hỏng ngầm theo hướng mở: `mc admin policy entities … 2>/dev/null` không kiểm mã thoát và pipeline không có `pipefail`, nên mc lỗi/đổi định dạng/đổi tên cờ ⇒ `users_with_policy()` trả rỗng ⇒ không thu hồi gì, `init.sh` vẫn thoát 0 và không ai biết khoá cũ còn sống; `test_minio_init_revokes_identities_left_over_from_key_rotation` chỉ so khớp văn bản tĩnh nên không bắt được | Script tự đọc output `mc` (đúng định dạng đo thật) nhưng không kiểm mã thoát/định dạng còn hiệu lực trước khi tin kết quả rỗng nghĩa là "không còn ai" | B0-08 (`deploy/minio/init.sh`) | P3 | mở — review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md` finding #4 (SEC-07). Chữa: kiểm mốc — đòi thấy dòng `Policy -> Entity Mappings:` trong output (hoặc giữ mã thoát của `mc`), không thấy thì `exit 1` |
| ⬜ | NO-200 | 2026-09-24 | | Hai vết trong FIX-087: (a) docstring của `test_deploy_default_swap_settle_s_covers_nginx_resolver_ttl` (`deploy/scripts/tests/test_deploy.py:299-300`) vẫn viết "đối chiếu với mặc định `${APPBACK_API_SWAP_SETTLE_S:-N}` trích thẳng từ `lib.sh`", trong khi thân test nay **cấm** đúng dạng đó; (b) `: "${APPBACK_API_SWAP_SETTLE_S=11}"` (`lib.sh:25,126`) giữ nguyên chuỗi rỗng — `APPBACK_API_SWAP_SETTLE_S=` trong `appback.env` làm `sleep ""` hỏng **giữa** `swap_api` (đã `--scale api=2`, chưa xoá container cũ), `set -e` bỏ dở | (a) Docstring không được cập nhật cùng lượt siết assert; (b) dạng `=` (chọn để giữ `0`, bài học NO-114) cũng vô tình giữ chuỗi rỗng, mà `:=` cũng giữ được `0` nên không cần đánh đổi này | B0-10 (`deploy/scripts/tests/test_deploy.py`, `deploy/scripts/lib.sh`) | P3 | mở — review APPROVE WITH COMMENTS `docs/reviews/2026-09-24-fix-debt-01-deploy.md` finding #5 (MNT-02/R-02) + Nit #6 (RES-01). Chữa: (a) sửa một dòng docstring; (b) thêm ngay sau dòng khai `[ -n "$APPBACK_API_SWAP_SETTLE_S" ] \|\| APPBACK_API_SWAP_SETTLE_S=11` |
| ⬜ | NO-201 | 2026-09-24 | | `deploy/observability/prometheus.yml` (FIX-086) chỉ khai job scrape `api:9464`, nhưng worker Celery chạy prefork phơi `/metrics` ở dải cổng `METRICS_PORT … METRICS_PORT+N-1` (nhánh core FIX-089, `metrics_port_for_process`) — không có target nào cho worker trong file scrape | `prometheus.yml` viết trước khi nhánh core (FIX-089) định hình cách worker công bố `/metrics` theo dải cổng theo tiến trình; hai nhánh chạy song song không đồng bộ | B0-08 (`deploy/observability/prometheus.yml`) | P3 | mở — phát hiện khi đối chiếu FIX-086 với FIX-089 (nhánh `fix/debt-01-core`) lúc ghi sổ gộp `fix/debt-01-deploy`. Chữa: thêm target/dải cổng worker vào `prometheus.yml` sau khi NO-161 (exporter worker/ml) xong, dùng `metrics_port_for_process` làm nguồn dải cổng |
| ⬜ | NO-202 | 2026-09-24 | | Bộ test AppFront chập chờn dưới tải: `pnpm verify` bước 4 đỏ 6–8 test mỗi lượt, tập file khác nhau giữa các lượt, chạy riêng thì xanh (`Viewer3DPanels`, `Viewer3DOverlays`, …) — cổng không xanh ổn định | `waitFor` 5000 ms hết hạn trên skeleton của `import()` lười khi vitest chạy song song nhiều worker trên máy tải cao (`Viewer3DOverlays.test.tsx:20` tự ghi nguyên nhân) | AppFront (hạ tầng test: `vite.config.ts`/`scripts/verify.mjs`, chủ các test Viewer3D) | P2 | mở — review DEBT-01 FE lượt 1 và 2 (`docs/reviews/2026-09-24-fix-debt-01-fe*.md`). Chữa: hạn chờ riêng cho lazy import (`LAZY_WAIT`) hoặc hạ `maxWorkers` của bước verify; không retry, không hạ ngưỡng. Gộp `fix/debt-01-fe` (DEBT-01) miễn cổng bước 4 vì flake này — người điều phối ghi lời miễn trong commit gộp |
| ⬜ | NO-203 | 2026-09-24 | | `create_storage(settings, None, clock)` bỏ qua luật khác origin (BE-00 §8, K15); một caller API mới truyền `None` thì kho S3 vẫn ký URL mà không ai hay | FIX-105 cho `core_settings` tuỳ chọn để `ml` (không ký URL) dựng kho không cần `SECRET_KEY`/`PUBLIC_BASE_URL`; không có hàng rào nào giới hạn `None` cho `ml` | B0-04 (`packages/storage/factory.py:50,57`) | P3 | mở — review lượt 2 `docs/reviews/2026-09-24-fix-debt-01-deploy-round-2.md` finding P3. Chữa: hàm riêng cho kho không ký URL, hoặc test tĩnh chỉ cho `None` dưới `apps/ml/` |
| ⬜ | NO-204 | 2026-09-24 | | Hai Nit của review deploy lượt 2: `packages/storage/tests/test_settings.py:71` không `reset_settings_cache()` (có thể xanh nhờ cache ấm); lệnh kiểm `ml.env` ở `deploy/scripts/README.md:54` dùng danh sách cấm hẹp hơn mẫu | Viết cùng FIX-105/FIX-087 | B0-04 (test), B0-10 (runbook) | Nit | mở — đề xuất: reset cache trong test; runbook kiểm theo danh sách cho phép `grep -vE '^(#\|$\|ML_\|METRICS_)'` rỗng |
| ⬜ | NO-205 | 2026-09-24 | | `bash tools/verify/run.sh gc` thoát 128, không in gì, trên Git Bash Windows (sau FIX-083) — không dọn được ảnh/volume nào | `run.sh:7` `export MSYS_NO_PATHCONV=1` làm `git -C "$REPO_ROOT" worktree list` nhận nguyên đường MSYS `/f/AppBack` (git Windows không hiểu) → git thoát 128, `set -euo pipefail` dừng script ở phép gán `valid_names`; test của FIX-083 chạy trong container Linux nên không bắt được | B0-01 (`tools/verify/run.sh:145-154`) | P3 | mở — đo 2026-09-24 khi dọn sau DEBT-01 (`bash -x run.sh gc`: `git -C /f/AppBack …` rồi thoát). Chữa: dùng `pwd -W`/`cygpath -m` cho `REPO_ROOT` ở nhánh `gc` (như `VERIFY_SRC_DIR`), test chạy `run.sh gc` với `MSYS_NO_PATHCONV=1`. Tạm: `backend/dieu-phoi/chay/B2-03/don-docker.sh` |
| ⬜ | NO-206 | 2026-09-24 | | `USER_LAST_ADMIN` (B1-05) **không phát được qua API**: `lock_admin_set` đòi người thực hiện nằm trong tập admin `active`, nên tập 1 người ⇒ mục tiêu là chính họ ⇒ `USER_SELF_MODIFICATION` ném trước ở #41/#42/#46; nhánh raise chỉ phủ bằng test dựng `WriteScope` ở trạng thái không thể có (`tests/test_service.py:22-27`) | Prompt [6] đòi lớp chốt này; bất biến "≥ 1 admin `active`" vẫn giữ (bên thua C14 nhận 403) | B1-05 (`apps/api/users/service.py:153-160`) | P3 | mở — finding 3 review lượt 1 `docs/reviews/2026-09-24-feature-b1-05-user-admin.md`. Prompt sau đừng trông chờ mã này trên dây; giữ làm lớp chốt |
| ⬜ | NO-207 | 2026-09-24 | | `avatar_url` ký presigned URL **đồng bộ** trên vòng sự kiện (`packages/storage/s3.py:224-246`, gọi từ `apps/api/me/avatar.py:237-247`): #38 của B1-05 với `USERS_LIST_MAX`=1000 người có ảnh ký 1000 URL trong một request | `presigned_get_object` của minio là hàm sync; `avatar_url` không chạm DB nên "không N+1" vẫn đạt | B1-04 (`apps/api/me/avatar.py`) / B0-04 (`packages/storage/s3.py`) | Nit | mở — finding 4 review lượt 1 B1-05. Nâng cấp: ký lô trong `asyncio.to_thread`, hoặc chỉ ký khi FE cần |
| ⬜ | NO-208 | 2026-09-25 | | `e2e/viewer3d.spec.ts` chưa được thẩm định lại dưới bộ mẫu: F-01b mục 4.10 bật `VITE_USE_MOCK_API=true` cho máy chủ e2e, nên mỗi lượt chạy là **người dùng lần đầu có bạn cộng tác giả**, và ít nhất BA lớp phủ mọc lên ở ba thời điểm khác nhau — `EditorTour.tsx:237` (lớp hướng dẫn), lớp `role=status` "đang dựng mô hình" của `Viewer3D.tsx:67` (đã chữa bằng `settleViewer`), con trỏ cộng tác giả `<span aria-label="Người dùng thử">` từ `src/api/__mocks__/client.ts:320` (lộ ra sau khi chữa lớp trên). Hai bài đỏ: `viewer3d.spec.ts:479` (P2 ViewCube) và `:521` (Q2 tìm phòng); **không** bài nào đỏ vì ảnh chuẩn. 13/15 giữ nguyên qua cả ba lượt reviewer chạy độc lập | Bộ spec được viết cho máy chủ KHÔNG mock (trước đây chặn `/auth/*` bằng `page.route`); đổi chế độ chạy làm mọi trạng thái "lần đầu" của sản phẩm hiện lên thật, mà spec không có bước nào đóng chúng. Vá từng lớp thì lộ lớp kế tiếp — đây là thẩm định lại cả bộ spec, không phải một vá mối nối | AppFront F-01b (`e2e/viewer3d.spec.ts`, `scripts/run-playwright.mjs`) → cần một prompt riêng | P2 | mở — khối [11].4 của prompt F-01b cho phép ghi nợ thay vì sửa ("spec đỏ … ghi tên và lỗi, không sửa spec ngoài 4.10"); review lượt 3 APPROVE kèm nợ này. **Manh mối đắt nhất cho người sửa:** `handleSkip` (`useEditorTour.ts:555`) đóng tour hẳn cả phiên nên MỘT cú bấm là đủ — vấn đề của Q2 là THỜI ĐIỂM (tour hiện SAU khi `settleViewer` đã chờ xong, giữa lúc `findOneRoom` chạy), không phải cách đóng. Chữa: cho spec chờ trạng thái "đã yên" thay vì chờ từng lớp, hoặc một fixture bộ mẫu "người dùng đã xem tour, không có bạn cộng tác" |
| ⬜ | NO-209 | 2026-09-25 | | `resolveExpiresAt` (`src/lib/auth/refresh.ts`) dựng `expiresAt` từ đồng hồ **CỤC BỘ** khi thân trả `expiresIn`, trong khi `remainingMs` của lịch hẹn lại trừ `serverOffsetMs` (hiệu chỉnh lệch đồng hồ, F-01b mục 4.3) — hai bên dùng hai gốc thời gian khác nhau, nên với đồng hồ máy khách nhanh 9 phút thì quãng đời token bị ước lượng sai nguyên cả phần lệch | Hàm đọc thân W16 có từ trước F-01b và chỉ biết đồng hồ cục bộ; F-01b thêm `serverOffsetMs` ở phía lịch hẹn mà không chạm nhánh `expiresIn` (khối [6] của prompt cấm mở rộng bộ đọc W16 sẵn có) | AppFront F-01b (`src/lib/auth/refresh.ts`, hàm `resolveExpiresAt`) | P3 | mở — review lượt 2 mục [D], lượt 3 APPROVE kèm nợ. **Chỉ nhánh thân trả `expiresIn` mà KHÔNG có `expiresAt` tới được**, và hợp đồng W16 (B1-01) luôn gửi `expiresAt`, nên BE thật không bao giờ đi nhánh ấy — chỉ bộ mẫu FE dùng `expiresIn`. Chữa: trừ `serverOffsetMs` luôn ở nhánh `expiresIn`, hoặc ghi rõ nhánh ấy là đường chỉ-dành-cho-bộ-mẫu |
| 🔧 | NO-210 | 2026-09-25 | | Fixture `mailpit` (`packages/testing/fixtures/services.py:97-110`) chạy Mailpit với rDNS bật: mỗi kết nối SMTP Mailpit tra PTR địa chỉ máy khách qua DNS của máy chủ; trên máy này PTR `172.17.x` mất ~11 s (đo `Resolve-DnsName`, trong container `nslookup 172.17.0.1` 4,3 s) ≥ `SMTP_TIMEOUT_S` 10 → 5 test mail (`packages/mail`, `auth_recovery` e2e ×2, `tools/tests/test_services.py`) hỏng `SMTPServerDisconnected timed out`, cả trên `main` `4f89245`; cổng đầy đủ đỏ cho mọi nhánh | test phụ thuộc mạng/DNS của máy (trái luật "không tải mạng trong test"); Mailpit mặc định bật rDNS | B0-01 | P1 | FIX-106: `.with_env("MP_SMTP_DISABLE_RDNS", "true")` — thử tay 3/3 thư 0,03 s (trước: 10 s/thư, 1/3 hỏng). Phát hiện ở B2-02 (lớp gộp). |
| ⬜ | NO-211 | 2026-09-25 | | `apps/api/auth_recovery/tests/test_e2e.py` (`test_invitation_accept_e2e`, `test_password_reset_e2e`) hỏng khi chạy **lẻ** (`pytest packages/mail/tests apps/api/auth_recovery/tests/test_e2e.py tools/tests/test_services.py packages/testing`): worker Celery thật báo `Received unregistered task of type 'default.auth_recovery.send_token_mail'` → không thấy thư trong 15 s; trong cổng đầy đủ thì xanh (log `backend/dieu-phoi/chay/B2-02/verify-gop2.log`) | task chỉ được đăng ký khi module task đã được một test khác nhập trước — test phụ thuộc thứ tự chạy (TEST-02) | B1-03 | P2 | Phát hiện ở FIX-106 (log `backend/dieu-phoi/chay/B2-02/fix106/e.log`). Chữa: fixture/e2e tự nhập module task (hoặc worker app `include`) trước khi dựng worker. |
| ⬜ | NO-212 | 2026-09-25 | | `deploy/compose/ci.yml:52` và `deploy/compose/dev.yml:55` chạy Mailpit `axllent/mailpit:v1.20.0` còn bật rDNS: máy có DNS PTR chậm (~11 s, NO-210) sẽ làm thư của stack dev/ci chậm/hết giờ như test trước FIX-106 | FIX-106 chỉ sửa fixture test (`packages/testing/fixtures/services.py`); `deploy/**` thuộc B0-08 | B0-08 | P3 | Review FIX-106 (`docs/reviews/2026-09-25-fix-b0-01-mailpit-rdns.md`). Chữa: `MP_SMTP_DISABLE_RDNS: "true"` trong `environment` của dịch vụ mailpit ở hai file. |
