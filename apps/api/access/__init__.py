"""Quyền cấp hệ thống và nhật ký hoạt động (B1-02, BE-00 §5).

Không nhập module con ở đây: `kinds.py`, `activity.py`, `jobs.py` phải nhập được
khi `fastapi` bị chặn trong `sys.modules` (worker dùng); `deps.py` mới nhập `fastapi`.
"""
