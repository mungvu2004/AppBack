# Bảng case bắt buộc thêm/miễn của module chất lượng (B2-05b [8], CASE.md §2.3).
#
# `quality_read_assessment` (Đ) không cần khai: case của Loại đã đủ. C10 của #31/#32 là case
# "+ngoài" (route xếp `pipeline.orchestrate.start` qua `start_run`): cổng chỉ nhận test
# **riêng** của op, nên có hai test `…__C10_queue_once` và `…__C10_no_key_replay` mỗi op.

[[endpoint]]
op = "quality_set_corners"
waive = { C16 = "thân chỉ có toạ độ số, không có chuỗi người nhập" }

[[endpoint]]
op = "quality_straighten"
waive = { C16 = "thân rỗng, không có chuỗi người nhập" }
