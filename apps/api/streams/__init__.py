"""Trung tâm SSE (B4-01): luồng tiến độ tải lên (S1) và luồng thông báo (S2).

Chỉ docstring: `discover` của `apps.api.core.extensions` nhập gói này để dò
`stream_providers.py`, nên mọi tác dụng phụ ở đây sẽ chạy lúc khởi động.
"""
