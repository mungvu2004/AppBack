# B0-01 — Khởi tạo repo và hiến chương

- Workspace uv ảo: 10 gói + 3 app (phụ thuộc đúng BE-00 §13.1), `uv.lock` 155 gói (torch CPU, không `opencv-python`); cấu hình ruff/mypy/pytest/coverage ở `pyproject.toml` gốc; `.importlinter` 9 hợp đồng.
- Cổng `bash tools/verify/run.sh verify` chạy 8 bước (+5b) trong container (`deploy/docker/verify.Dockerfile`, `deploy/compose/verify.yml`); thêm việc `lock`, `openapi`, `merge-heads`, `shell`, `gc`.
- Công cụ cổng có test: `tools/{charter,coverage_gate,case_gate,lint_migrations}.py`, `tools/verify/steps.py`; fixture dịch vụ thật `packages/testing/fixtures/services.py`.
- Lệch: hiến chương chép thẳng từ `backend/prompts/_charter/` cục bộ (AppFront chưa từng commit bộ này — người dùng xác nhận), không `git archive`/so hash với AppFront.
- Lệch: `[tool.coverage.paths]` dùng `/home/runner/work/*/AppBack` thay `*/*` (coverage cấm mẫu kết thúc bằng `*`); `ignore_imports` đặt trong từng hợp đồng (import-linter bỏ qua ở mục gốc); MinIO lấy từ `quay.io` (Docker Hub ngừng phát hành).
- Định dạng `CASE_TRACE_FILE` (B0-06 phải ghi): JSON Lines `{"test", "op", "status", "code"}` — xem docstring `tools/case_gate.py`.
