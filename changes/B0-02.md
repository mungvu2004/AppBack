# B0-02 — Lõi (`packages/core`)

- `settings.py` (`CoreSettings`, không đọc `.env`), `keys.py` (HKDF-SHA256 theo mục đích, khoá cũ chỉ để kiểm).
- `errors.py` + `error_codes.py`: sổ mã lỗi kiểm luật lúc nhập (mẫu mã, status, 401 chỉ nhóm Phiên, `retry_after` 1–10 cho 429/503), 26 mã lõi, `VersionConflictError`/`RemoteFieldChange`.
- `logging.py`: log JSON có `requestId` (contextvar), che khoá bí mật, JWT, `Bearer`, chữ ký S3; cắt chuỗi > 2.000 ký tự.
- `ids.py` (ULID có tiền tố, id không gian, id phép đo), `instants.py` (ngày giờ dây `.sssZ`), `text.py` (NFC, `normalize_email`), `clock.py`, `pipeline.py`.
- Fixture `fake_clock` ở `packages/testing/fixtures/clock.py`.
