# B0-03 — CSDL và migration (`packages/db`)

- `settings.py` (`DatabaseSettings`, nơi duy nhất đọc `DATABASE_URL`), `engine.py` (engine async có timeout, `session_scope`, `worker_sessionmaker` theo vòng sự kiện).
- `base.py`: `Base` có quy ước đặt tên và `timestamptz`, `TimestampMixin`, `SoftDeleteMixin`, `mm_column`, `area_column`, `unique_active`.
- `hooks.py`: `on_after_commit` (J09) chạy ngoài vòng sự kiện trên executor riêng, trần 2 s mỗi callback; `after_commit_idle`; cờ `DB_AFTER_COMMIT_INLINE`, `DB_DROP_AFTER_COMMIT`.
- `errors.py`: `translate_db_error` (C13 → 503 `DEPENDENCY_UNAVAILABLE`), `unique_violation`.
- Model và seed **tự dò** (`models/`, `seeds/`); Alembic (`alembic.ini`, `migrations/`), revision gốc rỗng, `new_revision.py`, `migrate_check.py` (bước 6 của verify nay **áp dụng**).
- Fixture `db_template`, `db_url`, `db_sessionmaker`, `db_session`, `drop_after_commit` ở `packages/testing/fixtures/db.py`.
