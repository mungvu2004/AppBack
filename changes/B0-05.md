# B0-05 — Redis, Celery, bus sự kiện

- `packages/messaging`: cấu hình Redis 4 vai (broker, streams, trạng thái an toàn, cache),
  app Celery dùng chung, `define_task`/`send_task`/`registered_tasks`, `EventBus`/`SyncEventBus`,
  `SafeLock` có token rào, sổ lịch `periodic`/`beat_schedule`.
- `apps/worker/celery_main.py`: điểm vào worker và beat; kiểm `maxmemory-policy` lúc khởi động.
- `packages/testing/fixtures/messaging.py`: client Redis thật theo từng DB, app Celery thử,
  worker Celery thật theo hàng.
- Không có endpoint, không có bảng CSDL, không có mã lỗi mới: gói dùng `DEPENDENCY_UNAVAILABLE`.
- Bước 6 (migration) không đụng tới; bước 7, 8 vẫn do B0-07/B0-06 quyết định.
