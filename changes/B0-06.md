# B0-06 — Khung API (`apps/api/core`)

- `apps/api/core`: `create_app` dò `apps/api/*/router.py` (`ROUTERS`), gộp `lifespan` của từng
  router, mount `/api`; `AppRoute` giữ thứ tự xác thực → session → dependency → guard
  (W21, 428, nhận việc idempotency) → hoàn tất + commit + `after_commit_idle`.
- `wire.py` (`WireModel`, `KeepNull`, `WireDatetime`, `NfcStr`, `versioned`), `pagination.py`
  (cursor ký HMAC), `errors.py` (thân W7), `middleware.py` (bốn lớp ASGI), `auth.py`,
  `deps.py`, `ratelimit.py`, `origin.py`, `permissions.py`, `extensions.py`, `openapi.py`.
- Route: `GET /api/health`, `GET /api/ready`, `GET /api/files/{token}`.
- Bảng `idempotency_records` + revision `r20260920_b0_06`; lịch `default.idempotency.purge_expired`.
- `packages/testing/fixtures/api.py`: `api_app`, `make_api_client`, `auth_headers`,
  `API_RESPONSE_OBSERVERS` và observer ghi vết case (`CASE_TRACE_FILE`).
- Bước 8 (`openapi`) từ nay chạy thật; bước 7 vẫn `không áp dụng` (B0-07 chưa hợp nhất).
