# B0-06 — Khung API (`apps/api/core`)

- `create_app` dò `apps/api/*/router.py` (`ROUTERS`), mount `/api`; `AppRoute`: xác thực → session → dependency → guard (W21, 428, idempotency) → commit → `after_commit_idle`.
- `wire`, `pagination`, `errors` (W7), `middleware` (5 lớp ASGI), `auth`, `deps`, `ratelimit`, `origin`, `permissions`, `extensions`, `openapi`.
- Route: `GET /api/health`, `GET /api/ready`, `GET /api/files/{token}`.
- Bảng `idempotency_records` + revision `r20260920_b0_06`; lịch `default.idempotency.purge_expired`.
- `packages/testing/fixtures/api.py`: `api_app`, `make_api_client`, `auth_headers`, observer vết case (`CASE_TRACE_FILE`).
- Bước 8 (`openapi`) từ nay chạy thật; bước 7 `không áp dụng` (B0-07 chưa hợp nhất).
- FIX-003..005: test của B0-01, B0-03, B0-05 bỏ khẳng định "repo rỗng" (`docs/fixes.md`).
