# B0-07 — Harness hợp đồng (`tools/contract`)

- Bước 7 (`python -m tools.contract.check`) từ nay chạy thật: Smoke, bản đồ đủ, thao tác đã mount, H1, H1 ngữ cảnh, H3, H4, H5.
- Runner Node (`runner.ts`: `smoke`, `decode`, `permissions`, `rules`) giải mẫu bằng chính zod của AppFront @ `APPFRONT_SHA`; `zod@3.23.8` + `tsx@4.23.15` ghim trong `package-lock.json`, cài một lần mỗi băm lock vào `$CONTRACT_NODE_DIR`.
- `schema-map.ts`: 83 mục = mọi dòng không v2 của BE-BIND §1 (kể cả S1, S2) + §2; `auth_login` là 204; `auth_refresh` giải bằng `strict/refresh.ts` (W16, K04).
- Bộ ghi golden (`packages/testing/golden`, gắn qua `packages/testing/fixtures/golden.py`): test `test_<op>__<case>` ghi `$CONTRACT_SAMPLES_DIR/<op>/<case>[_hậu tố]-<n>.json`.
- Giao diện cho prompt sau: `attach_context(response, **ctx)`, `record_stream_event(op, case, data)`, `sse.parse_sse(raw)`, fixture `appfront_dir`, `contract_build`.
- Gương H3 đọc `packages.domain.permissions` (`ROLES`, `PERMISSION_KEYS`, `PERMISSION_MATRIX`); H4 đọc `apps.api.rules.catalog` (`RULE_CODES`, `THRESHOLD_SPECS` với `rule_code`/`min`/`max`).
