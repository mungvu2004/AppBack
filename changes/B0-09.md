# B0-09 — CI GitHub Actions (`.github/workflows/ci.yml`)

- 8 job song song độc lập (`lint`, `typecheck`, `unit`, `integration`, `ml`, `contract`, `build`, `commits`) cộng job
  gộp `coverage` (ngoại lệ duy nhất có `needs:`); mỗi job gọi `tools/ci/job.sh <job>`, cùng mã cổng với `just verify`.
- Chia test `--ci-split=unit|integration|ml` (plugin `packages/testing/fixtures/ci_split.py`), gộp junit và vết case.
- H2 schemathesis trên app thật (Testcontainers), job `commits` kiểm Conventional Commits, tên nhánh, trailer `Prompt:`.
- SAST CodeQL (`codeql.yml`), Dependabot (`uv`, `github-actions`, `docker`), gitleaks, pip-audit, trivy.
- Hướng dẫn cài đặt repo GitHub cho người: `tools/ci/README.md`.
- Job `typecheck` chạy bước 8 (openapi) ở **chế độ xuất**, không so với bản đã commit: `openapi.json`
  bị `.gitignore`/cấm commit (BE-00 §13.2) nên chế độ so trên `main` luôn đỏ — quyết định đã duyệt
  2026-09-22 (hop-dong.md §4), người điều phối ghi NO khi hợp nhất.
