# B1-02 — Ma trận quyền và nhật ký hoạt động

- Gương ma trận quyền `packages/domain/permissions` (`ROLES`, `PERMISSION_KEYS`, `PERMISSION_MATRIX`, `can`, `SYSTEM_SCOPED_KEYS`), so từng ô với FE bằng H3 (B0-07).
- Dependency quyền cấp hệ thống `require_role`, `require_permission`, `require_admin` ở `apps/api/access/deps.py` — không dùng cho tài nguyên trong dự án (K08); quyền dự án thuộc `require_project` (B2-01).
- Bảng `activity_log` (`packages/db/models/access.py`) + revision `r20260922_b1_02`.
- `record_activity`, `SYSTEM_PIPELINE = "system:pipeline"`, bảng mã `ActivityKind` (27 hằng, `apps/api/access/kinds.py`, đối chiếu BE-BIND) ở `apps/api/access/activity.py`.
- Lịch dọn rác `default.access.purge_activity` (giữ 180 ngày) ở `apps/api/access/jobs.py`.
- Fixture `activity_rows`, `assert_one_activity` ở `packages/testing/fixtures/access.py`.
- Không có endpoint.
