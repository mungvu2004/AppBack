# B1-03 — quên mật khẩu, nhận lời mời, thư token một lần

- Bảng `one_time_tokens` (`packages/db/models/auth_recovery.py`) + revision `r20260923_b1_03`
  (head duy nhất, `down_revision=r20260923_b2_01`).
- Ba route công khai (loại C, 204): `POST /api/auth/password-reset` (N8, chống dò C27/C28),
  `POST /api/auth/password-reset/confirm` (N9), `POST /api/auth/invitations/accept` (N10) —
  `apps/api/auth_recovery/router.py`. Mã lỗi riêng `PASSWORD_RESET_TOKEN_INVALID`,
  `INVITATION_TOKEN_INVALID` khai bằng `ERRORS.define` trong module. Cả ba dùng chung hạn mức
  `recovery_ip`.
- Task `default.auth_recovery.send_token_mail` + hai lịch `resend_unsent` (5 phút),
  `purge_tokens` (24 giờ) — `apps/api/auth_recovery/jobs.py`, `messages.py`.
- Gói `packages/mail` (SMTP + bộ nhớ), fixture test `packages/testing/fixtures/mail.py`
  (`mailpit_inbox`, `extract_token`, `smtp_reject_server`, `memory_mailer`).
- Giao diện dữ liệu `apps/api/auth_recovery/tokens.py`: `issue_token`, `find_active_token`,
  `consume_token`, `revoke_tokens`, `latest_invitations`, `active_clause`.
- `test_e2e.py`: Mailpit thật + worker Celery thật cho cả hai luồng N8→N9 và N10.
- Vòng sửa review 1 (NO-143..146): `run_send_token_mail` cô lập lỗi vĩnh viễn **theo từng token**
  (token hỏng rời tập quét bù, token khác trong lô vẫn gửi); C27 đo đúng nhánh `issue` (người mới
  mỗi vòng); `_ping_broker` dùng một client Redis dùng chung tiến trình (`ProcessLocal`); helper
  `_seed_token`/`_seed_reset_token`/`_seed_invite_token` gộp vào `apps/api/auth_recovery/tests/support.py`.
