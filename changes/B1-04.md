# B1-04 — hồ sơ, mật khẩu, ảnh đại diện

- Không migration; dùng cột hồ sơ có sẵn của `users` (B1-01).
- Bốn route Bearer trong `apps/api/me`: `GET /api/me` (N11), `PATCH /api/me` (N12),
  `POST /api/me/password` (N13), `PUT /api/me/avatar` (N14). Mã lỗi riêng
  `CURRENT_PASSWORD_INCORRECT`, `AVATAR_TYPE_UNSUPPORTED`, `AVATAR_DIMENSIONS_EXCEEDED`.
- Hàm công khai `apps/api/me/avatar.py: avatar_url` cho B1-05.
- Lịch `default.me.purge_avatars` (24 giờ) dọn ảnh đại diện cũ/mồ côi — `apps/api/me/jobs.py`.
