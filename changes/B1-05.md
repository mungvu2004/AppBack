# B1-05 — quản trị người dùng

- Không migration, không bảng mới; đọc/ghi `users` (B1-01), token một lần (B1-03), nhật ký (B1-02).
- Chín route `user.manage` trong `apps/api/users`: #38–#46 (danh sách, thành viên dự án, nhật ký,
  đổi vai, vô hiệu, bật lại, mời, gửi lại lời mời, xoá mềm).
- Mã lỗi riêng `USER_SELF_MODIFICATION`, `USER_LAST_ADMIN`, `USER_EMAIL_TAKEN`, `USER_NOT_PENDING`,
  `USER_CONFIRM_EMAIL_MISMATCH`.
- Cổng có điều kiện: bước 7 (golden/H1/H3) áp dụng cho 9 op; không migration nên bước 6 không áp dụng.
