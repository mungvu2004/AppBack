# B2-01 — Dự án, thành viên (bảng), tóm tắt

- Ba bảng `projects`, `project_memberships`, `project_floor_summaries` (`packages/db/models/projects.py`) + revision `r20260923_b2_01`; mọi FK tới `projects.id` là `ON DELETE CASCADE`.
- Model response `UserOut`, `DrawingOut`, `FloorOut`, `ProjectOut`, `ProjectSummaryOut` và kiểu gộp `ProjectRollup` ở `apps/api/projects/wire.py` (`DrawingOut`, `FloorOut` do B2-03/B2-04 dựng).
- Thân ghi `ProjectCreateIn`, `ProjectUpdateIn`, `FloorDraftIn` ở `apps/api/projects/schemas.py`: trim + NFC, cấm ký tự điều khiển và định hướng, `null` → 422, bốn khoá W18 nhận rồi bỏ.
- Hai cổng mở rộng `project.floors`, `floor.drawings`, `project.create_floors` ở `apps/api/projects/parts.py` (`view_parts.py`/`PARTS`).
- `ProjectsSettings` (`apps/api/projects/settings.py`); factory `make_project` (`packages/testing/factories/projects.py`).
- Sáu route `#23`-`#27` và `N1` đã mount (`apps/api/projects/router.py`, nghiệp vụ ở `service.py`), kèm `access.py` (`require_project`), `memberships.py`, `summaries.py`, `jobs.py` (lịch dọn), `cli.py` và `cases.toml` — **bước 7 (golden/hợp đồng) và bước case đã áp dụng và đạt** cho cả 6 `op`.
