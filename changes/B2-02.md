# B2-02 — Thành viên và cài đặt dự án

- Hai module mới: `apps/api/project_members` (N3 thêm, N4 gỡ thành viên, cổng `ProjectInviteSink`) và `apps/api/project_settings` (N5 đọc, N6 ghi có version).
- Một bảng mới `project_settings` (`packages/db/models/project_settings.py`) + một revision `r<ngày>_b2_02_project_settings`, FK `projects.id` ON DELETE CASCADE.
- Bốn `op` mới: `members_add_member`, `members_remove_member`, `settings_read_settings`, `settings_replace_settings` (N6 là route GV đầu tiên).
- N3/N4 dùng bảng `project_memberships` của B2-01, không bảng riêng.
- Có `cases.toml` cho cả hai module; bước golden/hợp đồng và case áp dụng cho bốn `op`.
