# B2-03 — Tầng (nền dữ liệu, việc D)

- Bảng `floors` (`packages/db/models/floors.py`, `FloorRow`) + revision `r20260923_b2_03`: FK `projects.id` CASCADE, 5 CHECK, `uq_floors_level`, 3 index một phần.
- `apps/api/floors/{settings,errors,lookup}.py`: `FloorsSettings`, 4 mã lỗi riêng, `lookup.get_floor/floor_outs/floor_outs_by_project/lock_project_floors/new_level_id`.
- Factory `packages/testing/factories/floors.py` (`make_floor`); `apps/api/floors/cases.toml`.
- **Chưa có** `router.py`/`schemas.py`/`service.py`/`resolvers.py` (việc R) hay `view_parts.py`/`jobs.py` (việc N) — route và lịch dọn chưa mount, `just verify` bước case/golden của 5 `op` chưa áp dụng được tới khi hai việc đó hợp nhất.
