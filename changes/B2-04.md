# B2-04 — Tải bản vẽ, lượt pipeline, `Progress`

- Module mới `apps/api/drawings`: 5 route (#5 init, #6 khúc, #7 complete, #8 progress, N7 lượt mới nhất),
  luật `Progress` (BE-BIND §4), cổng `drawing_scales` và `drawing_pages`, view part `floor.drawings`.
- Bốn bảng mới `uploads`, `upload_chunks`, `pipeline_runs`, `drawings` (`packages/db/models/drawings.py`)
  + revision `r20260925_b2_04`; mọi FK `ON DELETE CASCADE`.
- Luồng SSE S1 `upload_progress` (`StreamProvider`), `PipelineStartPayload` ở
  `packages/messaging/payloads/drawings.py`; task `pipeline.orchestrate.start` **chỉ gửi**, khai ở B5-06a.
- Ba lịch nền: `default.drawings.purge_uploads`, `default.drawings.purge_orphans`,
  `default.drawings.requeue_runs`.
- Factory `packages/testing/factories/drawings.py`; `apps/api/drawings/cases.toml`.
