# B2-05a — Tiền xử lý và chất lượng ảnh (`packages/vision/preprocess`, `packages/vision/quality`)

- Gói thuần, không endpoint, không migration, không object: bước 6 (migration), 7 (case theo operationId), 8 (openapi) `không áp dụng`.
- `preprocess`: `sniff_kind`, PDF qua `pypdfium2` dưới `PDF_LOCK`, raster Pillow chặn bom (U01–U07), góc/khung, `rectify`, `deskew` kèm homography, trần đầu ra `max_pixels`.
- `quality`: gương ngưỡng FE (`thresholds.ts`), 4 số đo trên bản làm việc ≤ 2.000 px, 5 mã chất lượng HOP-DONG-MOI §7.4.
- Test hiệu năng `@pytest.mark.perf` chạy ở bước 5b; ảnh/PDF dựng trong test, không commit nhị phân.
