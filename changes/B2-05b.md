# B2-05b — API chất lượng ảnh đầu vào

- Module mới `apps/api/quality` (#30 đọc, #31 áp bốn góc, #32 nắn nghiêng); lớp dữ liệu ở đây gồm
  `schemas.py` (wire `ImageQualityAssessment`, thân `SetCornersIn`/`StraightenIn`) và `assessments.py`.
- Bảng mới `quality_assessments` (`packages/db/models/quality.py`) + revision `r20260926_b2_05b`;
  `floor_pk` PK + FK `ON DELETE CASCADE`.
- `save_assessment`/`load_assessment`/`AssessmentRow` cho B5-06a (khoá lượt bằng `lock_run` của B2-04).
- `read_view` dựng response 200 của cả ba route bằng một truy vấn.
- `router.py` (#30 đọc, #31 bốn góc, #32 nắn) và `service.py`: xử lý ảnh trong executor riêng
  (`processing.py`), nhả kết nối DB trước khi đọc storage, giao dịch cuối khoá theo BE-00 §7 rồi `start_run`.
- `cases.toml`: `waive C16` cho #31/#32; test C10 riêng (`+ngoài`) cho cả hai op.
