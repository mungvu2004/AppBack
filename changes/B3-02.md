# B3-02 — Lưu và đọc lớp không gian

- Module mới `apps/api/spatial_read`: 3 route (#33 `spatial_read_floor`, N15 `spatial_read_graph`,
  N16 `spatial_read_layer`), `codec` (nơi duy nhất đổi jsonb ↔ mô hình B3-01), `documents`,
  `entity_ids`, `counts`, `assemble`, `wire`, `graph`.
- Ba bảng mới `floor_documents`, `floor_change_log`, `floor_entity_ids`
  (`packages/db/models/spatial.py`) + revision `r20260926_b3_02`; mọi FK `ON DELETE CASCADE`.
- Cổng BE-00 §2.2: **cài** `drawing_scales` (`apps/api/spatial_read/drawing_scales.py`),
  **khai** `drawing_pages` (`pages.py`, B2-04 cài).
- Lịch nền `default.spatial_read.reconcile_counts` (`jobs.py`) đối chiếu `project_floor_summaries`.
- CLI `python -m apps.api.spatial_read.cli check-documents`; seed `packages/db/seeds/spatial.py`
  (`ORDER = 50`, dev/ci); factory `packages/testing/factories/spatial.py`;
  `apps/api/spatial_read/cases.toml`.
