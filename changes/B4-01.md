# B4-01 — Trung tâm SSE (`apps/api/streams`)

- Hai route công khai (Loại S): `GET /api/streams/projects/{project_id}/uploads/{upload_id}/progress` (`streams_open_progress`) và `GET /api/streams/notifications` (`streams_open_notifications`); xác thực bằng cookie `appback_stream` + `check_session`, `Origin` lạ → 403 (không đòi `Origin`).
- Dây W15: khung `id:`/`data:` không `event:`, heartbeat `: ping`, nối lại bằng `?lastEventId=`; luồng tiến độ gửi ảnh chụp khi mở mới, luồng thông báo không phát lại (K32).
- Cổng nhà cung cấp `apps/api/streams/providers.py` (`StreamProvider`, `StreamAccessPolicy`, `SnapshotProvider`), dò qua `extensions.discover("stream_providers", "PROVIDERS")`; B2-04 và B4-02 cắm vào không sửa file này.
- Giới hạn kết nối: ZSET `streams:conn:{user_id}` ở `redis-cache` (`STREAM_MAX_PER_USER`), pool Redis SSE có trần `STREAM_MAX_GLOBAL` → 429 `Retry-After: 5`.
- Hàm cho bên phát `finalize_upload_stream(_sync)` (`lifecycle.py`, không nhập `fastapi`); lịch `default.streams.expire_stale_uploads` (6 giờ).
- Fixture `sse_open`, `stream_cookie`, `publish_progress`, `publish_notification`, `record_stream_frames` (`packages/testing/fixtures/streams.py`).
- Không có migration. Kiểm H5 (mẫu S1, S2 giải bằng schema FE) áp dụng từ prompt này.
