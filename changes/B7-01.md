# B7-01 — Cờ tính năng, telemetry, metrics

- `apps/api/telemetry`: #9 `GET /api/feature-flags` (protected, không quyền, tính theo vai mỗi request), #37 `POST /api/telemetry` (public, lõi thuần `ingest.py` không nhận `Request`).
- `packages/observability`: `metrics.py` (registry Prometheus text 0.0.4 tự viết, không `prometheus_client`), `exporter.py` (`ThreadingHTTPServer` đếm tham chiếu, `metrics_lifespan` gắn vào `public` router của telemetry).
- Không sửa `apps/api/core`, `deploy/**`, `docs/charter/*`.
