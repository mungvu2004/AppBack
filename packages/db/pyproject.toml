[project]
name = "appback-db"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "sqlalchemy[asyncio]",
    "asyncpg",
    "alembic",
    "appback-core",
]

[tool.uv]
package = false

[tool.uv.sources]
appback-core = { workspace = true }
