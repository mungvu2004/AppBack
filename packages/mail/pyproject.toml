[project]
name = "appback-mail"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "appback-core",
]

[tool.uv]
package = false

[tool.uv.sources]
appback-core = { workspace = true }
