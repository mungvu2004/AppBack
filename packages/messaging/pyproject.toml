[project]
name = "appback-messaging"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "celery[redis]",
    "redis",
    "appback-core",
]

[tool.uv]
package = false

[tool.uv.sources]
appback-core = { workspace = true }
